# 토론모임 1조각-B (물어보기 + 측정) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
> **선행 의존:** Plan A(서재 기반, `2026-07-12-토론모임-1조각A-서재기반.md`)가 먼저 구현돼 있어야 한다. 이 플랜은 Plan A의 `books`/`library_entries`, `src/lib/library.ts`, `BookSearchInput`, `LibraryGrid`, `LibrarySection`, 마이페이지 삽입을 전제로 그 위에 얹는다.

**Goal:** 정기모임에 참여한 회원에게 "무슨 책 읽으셨어요?" 인라인 strip을 1건씩 띄워 서재에 `source='ask'`로 담게 하고(콜드스타트 씨앗), 담음/닫음/미응답 3상태를 GA4 + admin 미니 카운트로 측정해 북극성 지표(물어보기 응답률)를 얻는다. 전체는 Plan A와 같은 `library_enabled` 플래그 뒤에 격리한다.

**Architecture:** 신규 `book_asks` 테이블(회원×모임 ask 해소 원장, status=answered/dismissed). 트리거 = 정기모임(`meeting_type='regular'`) 참여 판정(결제완료 confirmed + 모임일 경과)을 **최근 60일 윈도우**로 제한해 과거 소급 스팸을 막는다(스펙 §6-0 "앞으로의 정기모임만"). strip은 미해소 최근 1건만. 답변 시 Plan A의 `upsertBookAndEntry(source='ask')` + book_asks(answered), 닫기 시 book_asks(dismissed). 측정은 GA4 3이벤트 + `/admin/library` 미니 카운트. 소개 strip은 localStorage per-device.

**Tech Stack:** Next.js 16 App Router, React 19, TypeScript, Tailwind v4, Supabase(anon RLS + service_role), Vitest, GA4(`trackEvent`). Plan A 산출물 재사용.

---

## 배경 규칙 (구현 중 항상 지킬 것)

- **문서 언어는 한국어, 코드는 영어.** 세미콜론 없음, 작은따옴표, 함수 컴포넌트만. 아이콘은 인라인 SVG(라이브러리 금지).
- **API 응답 포맷:** `{ status: 'success' | 'error', message?, data? }`.
- **KST:** 날짜는 `src/lib/kst.ts`만 사용, `new Date()` 직접 금지. 윈도우 판정은 기존 `getDaysUntil(target, from)` 재사용(음수 = 과거).
- **참여 = 결제완료 = `status='confirmed'`.** `pending_transfer`(미입금)는 참여 아님 → strip 트리거·분모에서 제외(마이페이지 "참여 완료" 배지 규칙과 동일).
- **토론모임엔 절대 노출 금지** — 트리거·분모 쿼리에 `meeting_type='regular'` 필터 필수(스펙 §9-12).
- **안 만든 2조각 기능 미리 약속 금지** — 문구에 "투표/다음 토론책" 언급 금지(스펙 §9-11). strip 문구는 확정본 그대로.
- **마이그레이션은 코드 확정 후 사용자에게 수동 안내**(Supabase SQL Editor). SQL 파일은 만들되 실행은 사용자 몫.
- **(main)/(admin) 페이지는 로그인 필요 → 로컬 테스트 불가.** 검증은 `npm run prelaunch` + Vercel preview. 순수 로직만 `vitest run`.
- **작업 브랜치:** Plan A와 동일 브랜치(`feat/discussion-slice1`)에 이어서 커밋.
- **less-ornament-first**(`feedback_ux_simplicity_first`) — strip·소개배너는 얇게, 외피 중첩 금지.

---

## File Structure

**신규 (Create)**
- `supabase/migration-library-asks.sql` — book_asks 테이블 + RLS + 인덱스
- `supabase/migration-library-asks-rollback.sql` — 롤백
- `src/types/ask.ts` — `BookAsk`, `PendingAsk` 타입
- `src/lib/asks.ts` — `ASK_WINDOW_DAYS`, `isAskEligibleMeeting()`, `askSourceLabel()`(순수) + `getPendingAsk()`, `verifyEligibleParticipation()`, `recordAskAnswered()`, `recordAskDismissed()`, `getAskStats()`(서버)
- `src/lib/__tests__/asks.test.ts` — 순수 함수 단위 테스트
- `src/app/api/library/ask/route.ts` — 답변/닫기 라우트(자격 검증)
- `src/components/library/AskStrip.tsx` — 물어보기 strip (client)
- `src/components/library/AskStripSection.tsx` — 서버(플래그 게이트 + 미해소 1건 페치)
- `src/components/library/LibraryIntroStrip.tsx` — 소개 strip (client, localStorage)
- `src/app/(admin)/admin/library/page.tsx` — 응답률 미니 카운트

**수정 (Modify)**
- `src/types/book.ts` — `LibraryEntryWithBook`에 `source_meeting_date?: string | null` 추가
- `src/lib/library.ts` — `getMyLibrary`가 출처 모임 날짜 enrich
- `src/components/library/BookSearchInput.tsx` — `askMeetingId?` prop(답변 시 ask 라우트 호출 + GA4)
- `src/components/library/LibraryGrid.tsx` — 출처 라벨 "N월 정기모임에서"
- `src/app/(main)/my/page.tsx` — 소개 strip + 물어보기 strip 삽입(서재 위)
- `src/components/admin/adminMenu.ts` — "서재 응답률" 메뉴 항목

---

## Task 1: DB 마이그레이션 (book_asks)

**Files:**
- Create: `supabase/migration-library-asks.sql`
- Create: `supabase/migration-library-asks-rollback.sql`

- [ ] **Step 1: 마이그레이션 SQL 작성**

`supabase/migration-library-asks.sql`:

```sql
-- ============================================================
-- 토론모임 개편 · 1조각-B — 물어보기 해소 원장 (book_asks)
-- ============================================================
-- 변경 요약:
--   book_asks : 회원 × 정기모임 ask 해소 기록 (answered / dismissed)
--               strip이 같은 모임을 다시 안 띄우게 + 측정 분자(닫음/담음)
--
-- 안전성:
--   - forward-compatible: 신규 테이블만 추가, 기존 코드/테이블 무영향
--   - Plan A(books/library_entries) 이후 실행 전제
--   - 플래그 OFF 시 화면 노출 0
--
-- 실행 환경: Supabase SQL Editor (prod: ycqqzzvyixvtdorjxkrn)
-- 멱등성: IF NOT EXISTS → 재실행 안전
-- 롤백: migration-library-asks-rollback.sql
-- ============================================================

CREATE TABLE IF NOT EXISTS public.book_asks (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES public.profiles(id) ON DELETE CASCADE,
  meeting_id UUID NOT NULL REFERENCES public.meetings(id) ON DELETE CASCADE,
  status TEXT NOT NULL CHECK (status IN ('answered', 'dismissed')),
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  UNIQUE (user_id, meeting_id)   -- 모임 1건당 1회만 해소
);

CREATE INDEX IF NOT EXISTS idx_book_asks_status
  ON public.book_asks(status);

ALTER TABLE public.book_asks ENABLE ROW LEVEL SECURITY;

-- 본인 것만 조회. 쓰기는 service_role만(정책 없음)
DROP POLICY IF EXISTS "book_asks_select_own" ON public.book_asks;
CREATE POLICY "book_asks_select_own"
  ON public.book_asks FOR SELECT
  USING (auth.uid() = user_id);

-- 확인용 (실행 후 주석 해제)
-- SELECT status, count(*) FROM public.book_asks GROUP BY status;
```

- [ ] **Step 2: 롤백 SQL 작성**

`supabase/migration-library-asks-rollback.sql`:

```sql
-- 롤백: 1조각-B 물어보기 원장 제거
-- ⚠️ book_asks 데이터 삭제됨(해소 이력). 운영 데이터 있으면 신중히.
DROP TABLE IF EXISTS public.book_asks;
```

- [ ] **Step 3: 커밋 (실행은 사용자 몫이므로 파일만)**

```bash
git add supabase/migration-library-asks.sql supabase/migration-library-asks-rollback.sql
git commit -m "feat(asks): 물어보기 해소 원장 마이그레이션 (book_asks)"
```

> **사용자 수동 작업 (구현 착수 시 안내):** Plan A의 `migration-library.sql` 실행 후 이 `migration-library-asks.sql`을 Supabase SQL Editor(prod)에서 실행.

---

## Task 2: 타입 정의 (ask)

**Files:**
- Create: `src/types/ask.ts`
- Modify: `src/types/book.ts`

- [ ] **Step 1: ask 타입 작성**

`src/types/ask.ts`:

```typescript
export type BookAsk = {
  id: string
  user_id: string
  meeting_id: string
  status: 'answered' | 'dismissed'
  created_at: string
}

// strip에 띄울 미해소 정기모임 1건
export type PendingAsk = {
  meetingId: string
  meetingDate: string // "YYYY-MM-DD"
}

// admin 미니 카운트
export type AskStats = {
  denominator: number // 정기 참여 건 (최근 윈도우, 분모)
  answered: number    // 담음
  dismissed: number   // 닫음
  pending: number     // 미응답(분모 - 담음 - 닫음, 음수 방지 clamp)
  responseRate: number // 담음 / 분모 (분모 0이면 0)
}
```

- [ ] **Step 2: `LibraryEntryWithBook`에 출처 모임 날짜 필드 추가**

`src/types/book.ts`의 `LibraryEntryWithBook`를 아래로 교체:

```typescript
export type LibraryEntryWithBook = LibraryEntry & {
  books: Book
  // Plan B: source='ask'인 항목의 출처 정기모임 날짜(라벨 "N월 정기모임에서"용). 없으면 null
  source_meeting_date?: string | null
}
```

- [ ] **Step 3: 타입 체크**

Run: `npx tsc --noEmit`
Expected: PASS

- [ ] **Step 4: 커밋**

```bash
git add src/types/ask.ts src/types/book.ts
git commit -m "feat(asks): ask 타입 + 서재 항목 출처 날짜 필드"
```

---

## Task 3: 순수 헬퍼 (자격 판정 + 출처 라벨, TDD)

`meeting_type='regular'` + `status='active'` + 과거 + 최근 60일 윈도우면 물어보기 대상. 라벨은 날짜에서 "N월 정기모임에서".

**Files:**
- Create: `src/lib/asks.ts` (이 태스크에선 순수 함수만)
- Test: `src/lib/__tests__/asks.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

`src/lib/__tests__/asks.test.ts`:

```typescript
import { describe, it, expect } from 'vitest'
import { isAskEligibleMeeting, askSourceLabel, ASK_WINDOW_DAYS } from '@/lib/asks'

const today = '2026-07-12'

describe('isAskEligibleMeeting', () => {
  it('정기 + active + 어제 모임은 대상', () => {
    expect(
      isAskEligibleMeeting({ date: '2026-07-11', meeting_type: 'regular', status: 'active' }, today),
    ).toBe(true)
  })

  it('토론모임은 제외', () => {
    expect(
      isAskEligibleMeeting({ date: '2026-07-11', meeting_type: 'discussion', status: 'active' }, today),
    ).toBe(false)
  })

  it('미래 모임(오늘 포함)은 제외', () => {
    expect(
      isAskEligibleMeeting({ date: today, meeting_type: 'regular', status: 'active' }, today),
    ).toBe(false)
    expect(
      isAskEligibleMeeting({ date: '2026-07-20', meeting_type: 'regular', status: 'active' }, today),
    ).toBe(false)
  })

  it('active 아닌 모임(deleted 등)은 제외', () => {
    expect(
      isAskEligibleMeeting({ date: '2026-07-11', meeting_type: 'regular', status: 'deleted' }, today),
    ).toBe(false)
  })

  it('윈도우 밖(60일 초과 과거)은 제외', () => {
    // 61일 전
    expect(
      isAskEligibleMeeting({ date: '2026-05-12', meeting_type: 'regular', status: 'active' }, today),
    ).toBe(false)
  })

  it('윈도우 경계(정확히 60일 전)는 대상', () => {
    // 2026-07-12 - 60일 = 2026-05-13
    expect(
      isAskEligibleMeeting({ date: '2026-05-13', meeting_type: 'regular', status: 'active' }, today),
    ).toBe(true)
  })
})

describe('askSourceLabel', () => {
  it('날짜에서 N월 라벨을 만든다', () => {
    expect(askSourceLabel('2026-07-11')).toBe('7월 정기모임에서')
    expect(askSourceLabel('2026-12-01')).toBe('12월 정기모임에서')
  })

  it('null이면 null', () => {
    expect(askSourceLabel(null)).toBeNull()
    expect(askSourceLabel(undefined)).toBeNull()
  })
})

describe('ASK_WINDOW_DAYS', () => {
  it('60일', () => {
    expect(ASK_WINDOW_DAYS).toBe(60)
  })
})
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npx vitest run src/lib/__tests__/asks.test.ts`
Expected: FAIL (모듈/함수 없음)

- [ ] **Step 3: 순수 함수 구현**

`src/lib/asks.ts` (우선 순수 부분만):

```typescript
import { getDaysUntil } from '@/lib/kst'

/** 물어보기 대상 모임의 최근 윈도우(일). 과거 소급 스팸 방지 — "앞으로의 정기모임만"의 실용 근사 */
export const ASK_WINDOW_DAYS = 60

type EligibleMeetingShape = {
  date: string
  meeting_type: string
  status: string
}

/**
 * 물어보기(및 분모) 대상 정기모임인지 순수 판정.
 * 정기 + active + 과거 + 최근 windowDays 이내.
 */
export function isAskEligibleMeeting(
  meeting: EligibleMeetingShape,
  kstToday: string,
  windowDays = ASK_WINDOW_DAYS,
): boolean {
  if (meeting.meeting_type !== 'regular') return false
  if (meeting.status !== 'active') return false
  const days = getDaysUntil(meeting.date, kstToday) // 과거면 음수
  return days < 0 && days >= -windowDays
}

/** "2026-07-11" → "7월 정기모임에서". null/빈 값은 null */
export function askSourceLabel(date: string | null | undefined): string | null {
  if (!date) return null
  const month = Number(date.slice(5, 7))
  if (!month) return null
  return `${month}월 정기모임에서`
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npx vitest run src/lib/__tests__/asks.test.ts`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add src/lib/asks.ts src/lib/__tests__/asks.test.ts
git commit -m "feat(asks): 자격 판정 + 출처 라벨 순수 헬퍼 + 테스트"
```

---

## Task 4: 서버 헬퍼 (미해소 1건 / 자격 검증 / 기록 / 통계)

**Files:**
- Modify: `src/lib/asks.ts` (Task 3 파일에 서버 함수 추가)

- [ ] **Step 1: 서버 함수 추가**

`src/lib/asks.ts` 하단에 아래를 추가(기존 순수 함수 유지):

```typescript
import { createServiceClient } from '@/lib/supabase/admin'
import { getKSTToday } from '@/lib/kst'
import type { SupabaseClient } from '@supabase/supabase-js'
import type { PendingAsk, AskStats } from '@/types/ask'

type RegRow = {
  meeting_id: string
  user_id: string
  meetings: { date: string; meeting_type: string; status: string } | null
}

/**
 * 이 회원에게 지금 띄울 물어보기 1건(미해소·최근·정기).
 * confirmed 정기모임 중 자격 통과 & book_asks에 없는 것 → 가장 최근 1건. 없으면 null.
 */
export async function getPendingAsk(userId: string): Promise<PendingAsk | null> {
  const admin = createServiceClient()
  const kstToday = getKSTToday()

  const { data: regs } = await admin
    .from('registrations')
    .select('meeting_id, user_id, meetings(date, meeting_type, status)')
    .eq('user_id', userId)
    .eq('status', 'confirmed')

  const eligible = ((regs ?? []) as RegRow[])
    .filter((r) => r.meetings && isAskEligibleMeeting(r.meetings, kstToday))

  if (eligible.length === 0) return null

  const { data: asks } = await admin
    .from('book_asks')
    .select('meeting_id')
    .eq('user_id', userId)

  const resolved = new Set((asks ?? []).map((a) => a.meeting_id as string))

  const open = eligible
    .filter((r) => !resolved.has(r.meeting_id))
    .sort((a, b) => (b.meetings!.date).localeCompare(a.meetings!.date))

  const top = open[0]
  if (!top || !top.meetings) return null
  return { meetingId: top.meeting_id, meetingDate: top.meetings.date }
}

/**
 * (user, meeting)이 실제 물어보기 자격이 있는 건인지 서버 재검증(통계 조작 방지).
 * confirmed 등록 존재 + 정기 자격 통과.
 */
export async function verifyEligibleParticipation(
  admin: SupabaseClient,
  userId: string,
  meetingId: string,
): Promise<boolean> {
  const kstToday = getKSTToday()
  const { data } = await admin
    .from('registrations')
    .select('meeting_id, meetings(date, meeting_type, status)')
    .eq('user_id', userId)
    .eq('meeting_id', meetingId)
    .eq('status', 'confirmed')
    .limit(1)
    .maybeSingle()

  const row = data as { meetings: { date: string; meeting_type: string; status: string } | null } | null
  if (!row || !row.meetings) return false
  return isAskEligibleMeeting(row.meetings, kstToday)
}

/** book_asks upsert(해소 기록). 반환: 성공 여부 */
async function recordAsk(
  admin: SupabaseClient,
  userId: string,
  meetingId: string,
  status: 'answered' | 'dismissed',
): Promise<boolean> {
  const { error } = await admin
    .from('book_asks')
    .upsert({ user_id: userId, meeting_id: meetingId, status }, { onConflict: 'user_id,meeting_id' })
  return !error
}

export function recordAskAnswered(admin: SupabaseClient, userId: string, meetingId: string) {
  return recordAsk(admin, userId, meetingId, 'answered')
}

export function recordAskDismissed(admin: SupabaseClient, userId: string, meetingId: string) {
  return recordAsk(admin, userId, meetingId, 'dismissed')
}

/**
 * admin 미니 카운트. 분모 = 최근 윈도우 정기 참여(중복 (user,meeting) dedupe),
 * 분자 = book_asks answered/dismissed. 250명 규모라 전량 스캔 OK.
 */
export async function getAskStats(): Promise<AskStats> {
  const admin = createServiceClient()
  const kstToday = getKSTToday()

  const { data: regs } = await admin
    .from('registrations')
    .select('meeting_id, user_id, meetings(date, meeting_type, status)')
    .eq('status', 'confirmed')

  const denomSet = new Set<string>()
  for (const r of (regs ?? []) as RegRow[]) {
    if (r.meetings && isAskEligibleMeeting(r.meetings, kstToday)) {
      denomSet.add(`${r.user_id}:${r.meeting_id}`)
    }
  }
  const denominator = denomSet.size

  const { data: asks } = await admin.from('book_asks').select('status')
  let answered = 0
  let dismissed = 0
  for (const a of asks ?? []) {
    if (a.status === 'answered') answered += 1
    else if (a.status === 'dismissed') dismissed += 1
  }

  const pending = Math.max(0, denominator - answered - dismissed)
  const responseRate = denominator === 0 ? 0 : Math.round((answered / denominator) * 100)

  return { denominator, answered, dismissed, pending, responseRate }
}
```

- [ ] **Step 2: 타입 체크**

Run: `npx tsc --noEmit`
Expected: PASS

- [ ] **Step 3: 커밋**

```bash
git add src/lib/asks.ts
git commit -m "feat(asks): 서버 헬퍼 (미해소 1건/자격검증/기록/통계)"
```

---

## Task 5: getMyLibrary 출처 모임 날짜 enrich

Plan A의 `getMyLibrary`가 `source='ask'` 항목에 출처 정기모임 날짜를 붙인다(라벨용).

**Files:**
- Modify: `src/lib/library.ts`

- [ ] **Step 1: `getMyLibrary` 교체**

`src/lib/library.ts`의 `getMyLibrary`를 아래로 교체(다른 함수는 유지):

```typescript
export const getMyLibrary = cache(async (userId: string): Promise<LibraryEntryWithBook[]> => {
  const admin = createServiceClient()
  const { data } = await admin
    .from('library_entries')
    .select('*, books(*)')
    .eq('user_id', userId)
    .order('created_at', { ascending: false })

  const entries = (data ?? []) as LibraryEntryWithBook[]

  // source='ask' 항목의 출처 모임 날짜 붙이기 (라벨 "N월 정기모임에서")
  const meetingIds = Array.from(
    new Set(entries.filter((e) => e.source_meeting_id).map((e) => e.source_meeting_id as string)),
  )
  if (meetingIds.length > 0) {
    const { data: meetings } = await admin.from('meetings').select('id, date').in('id', meetingIds)
    const dateMap = new Map((meetings ?? []).map((m) => [m.id as string, m.date as string]))
    for (const e of entries) {
      e.source_meeting_date = e.source_meeting_id ? dateMap.get(e.source_meeting_id) ?? null : null
    }
  }

  return entries
})
```

- [ ] **Step 2: 타입 체크**

Run: `npx tsc --noEmit`
Expected: PASS

- [ ] **Step 3: 커밋**

```bash
git add src/lib/library.ts
git commit -m "feat(asks): 서재 조회에 출처 정기모임 날짜 enrich"
```

---

## Task 6: 물어보기 API 라우트 (답변/닫기)

**Files:**
- Create: `src/app/api/library/ask/route.ts`

- [ ] **Step 1: 라우트 구현**

`src/app/api/library/ask/route.ts`:

```typescript
import { NextResponse, type NextRequest } from 'next/server'
import { createServerClient } from '@supabase/ssr'
import { createServiceClient } from '@/lib/supabase/admin'
import { isLibraryEnabled, upsertBookAndEntry } from '@/lib/library'
import { verifyEligibleParticipation, recordAskAnswered, recordAskDismissed } from '@/lib/asks'
import type { BookSearchResult } from '@/types/book'

type AnswerBody = { action: 'answer'; meetingId: string; book: BookSearchResult }
type DismissBody = { action: 'dismiss'; meetingId: string }
type AskBody = AnswerBody | DismissBody

export async function POST(request: NextRequest) {
  if (!(await isLibraryEnabled())) {
    return NextResponse.json({ status: 'error', message: '사용할 수 없는 기능입니다' }, { status: 403 })
  }

  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    { cookies: { getAll: () => request.cookies.getAll(), setAll: () => {} } },
  )

  const {
    data: { user },
  } = await supabase.auth.getUser()
  if (!user) {
    return NextResponse.json({ status: 'error', message: '로그인이 필요합니다' }, { status: 401 })
  }

  let body: AskBody
  try {
    body = await request.json()
  } catch {
    return NextResponse.json({ status: 'error', message: '잘못된 요청입니다' }, { status: 400 })
  }

  if (!body || typeof body.meetingId !== 'string' || !body.meetingId) {
    return NextResponse.json({ status: 'error', message: '잘못된 요청입니다' }, { status: 400 })
  }

  const admin = createServiceClient()

  // 자격 재검증 (통계 조작 방지)
  const eligible = await verifyEligibleParticipation(admin, user.id, body.meetingId)
  if (!eligible) {
    return NextResponse.json({ status: 'error', message: '대상 모임이 아닙니다' }, { status: 400 })
  }

  if (body.action === 'dismiss') {
    const ok = await recordAskDismissed(admin, user.id, body.meetingId)
    if (!ok) {
      return NextResponse.json({ status: 'error', message: '처리에 실패했습니다' }, { status: 500 })
    }
    return NextResponse.json({ status: 'success' })
  }

  if (body.action === 'answer') {
    if (!body.book || typeof body.book.title !== 'string' || !body.book.title) {
      return NextResponse.json({ status: 'error', message: '책 정보가 없습니다' }, { status: 400 })
    }
    const result = await upsertBookAndEntry(
      admin,
      user.id,
      {
        isbn13: body.book.isbn13 ?? null,
        title: body.book.title,
        authors: body.book.authors ?? null,
        publisher: body.book.publisher ?? null,
        thumbnail: body.book.thumbnail ?? null,
      },
      'ask',
      body.meetingId,
    )
    if (result === 'error') {
      return NextResponse.json({ status: 'error', message: '서재에 담지 못했습니다' }, { status: 500 })
    }
    // 담기 성공/이미담김 모두 응답으로 간주 → answered 기록
    await recordAskAnswered(admin, user.id, body.meetingId)
    return NextResponse.json({ status: 'success', data: { result } })
  }

  return NextResponse.json({ status: 'error', message: '알 수 없는 요청입니다' }, { status: 400 })
}
```

- [ ] **Step 2: 타입 체크**

Run: `npx tsc --noEmit`
Expected: PASS

- [ ] **Step 3: 커밋**

```bash
git add src/app/api/library/ask/route.ts
git commit -m "feat(asks): 물어보기 답변/닫기 API 라우트 (자격 검증)"
```

---

## Task 7: BookSearchInput에 물어보기 경로 추가

Plan A `BookSearchInput`을 물어보기 strip에서도 재사용. `askMeetingId`가 있으면 `/api/library/ask`(answer)로 보내고 GA4 `ask_answered` 발화.

**Files:**
- Modify: `src/components/library/BookSearchInput.tsx`

- [ ] **Step 1: props + handleAdd 분기 추가**

`src/components/library/BookSearchInput.tsx` 상단 import에 추가:

```tsx
import { trackEvent } from '@/lib/analytics'
```

`Props` 타입을 아래로 교체:

```tsx
type Props = {
  /** 담기 성공 후 콜백 (없으면 router.refresh만) */
  onAdded?: () => void
  /** 물어보기 strip에서 재사용 시: 이 정기모임 출처로 ask 답변 처리 */
  askMeetingId?: string
}
```

컴포넌트 시그니처를 아래로 교체:

```tsx
export default function BookSearchInput({ onAdded, askMeetingId }: Props) {
```

`handleAdd` 안의 fetch 블록(현재 `/api/library/add` 호출부)을 아래로 교체:

```tsx
    try {
      const res = askMeetingId
        ? await fetch('/api/library/ask', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ action: 'answer', meetingId: askMeetingId, book }),
          })
        : await fetch('/api/library/add', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify(book),
          })
      const json = await res.json()
      if (json.status === 'success') {
        if (askMeetingId) trackEvent('ask_answered', { meeting_id: askMeetingId })
        setQuery('')
        setResults([])
        if (onAdded) onAdded()
        router.refresh()
      } else {
        setError('담지 못했어요. 다시 시도해주세요.')
      }
    } catch {
      setError('담지 못했어요. 다시 시도해주세요.')
    } finally {
      setAddingKey(null)
    }
```

- [ ] **Step 2: 타입 체크 + 린트**

Run: `npx tsc --noEmit && npm run lint`
Expected: PASS

- [ ] **Step 3: 커밋**

```bash
git add src/components/library/BookSearchInput.tsx
git commit -m "feat(asks): BookSearchInput 물어보기 경로(askMeetingId) + GA4"
```

---

## Task 8: AskStrip (client) + AskStripSection (server)

**Files:**
- Create: `src/components/library/AskStrip.tsx`
- Create: `src/components/library/AskStripSection.tsx`

- [ ] **Step 1: AskStrip (client) 작성**

`src/components/library/AskStrip.tsx`:

```tsx
'use client'

import { useEffect, useState } from 'react'
import { useRouter } from 'next/navigation'
import BookSearchInput from '@/components/library/BookSearchInput'
import { trackEvent } from '@/lib/analytics'

type Props = {
  meetingId: string
  /** "N월 정기모임 · 서재에 기록해두세요" 의 앞부분 라벨. 예: "7월 정기모임" */
  meetingLabel: string
}

export default function AskStrip({ meetingId, meetingLabel }: Props) {
  const router = useRouter()
  const [open, setOpen] = useState(false)
  const [closing, setClosing] = useState(false)

  useEffect(() => {
    trackEvent('ask_strip_view', { meeting_id: meetingId })
  }, [meetingId])

  async function handleDismiss() {
    setClosing(true)
    try {
      await fetch('/api/library/ask', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ action: 'dismiss', meetingId }),
      })
      trackEvent('ask_dismissed', { meeting_id: meetingId })
      router.refresh()
    } finally {
      setClosing(false)
    }
  }

  return (
    <section className="mt-6 rounded-[var(--radius-md)] border border-primary-200 bg-primary-50 p-3">
      <div className="flex items-start gap-3">
        <div className="flex h-8 w-8 shrink-0 items-center justify-center rounded-lg border border-primary-200 bg-white text-primary-600">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">
            <path d="M4 19.5A2.5 2.5 0 0 1 6.5 17H20" />
            <path d="M6.5 2H20v20H6.5A2.5 2.5 0 0 1 4 19.5v-15A2.5 2.5 0 0 1 6.5 2z" />
          </svg>
        </div>
        <div className="min-w-0 flex-1">
          <p
            className="text-sm font-bold text-primary-700"
            style={{ fontFamily: 'var(--font-display)' }}
          >
            무슨 책 읽으셨어요?
          </p>
          <p className="mt-0.5 text-caption text-primary-600 break-keep">
            {meetingLabel} · 서재에 기록해두세요
          </p>
        </div>
        <button
          onClick={handleDismiss}
          disabled={closing}
          aria-label="닫기"
          className="shrink-0 text-neutral-400 disabled:opacity-50"
        >
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.4" strokeLinecap="round">
            <path d="M18 6 6 18" />
            <path d="m6 6 12 12" />
          </svg>
        </button>
      </div>

      {open ? (
        <div className="mt-3">
          <BookSearchInput askMeetingId={meetingId} />
        </div>
      ) : (
        <button
          onClick={() => setOpen(true)}
          className="mt-3 w-full rounded-[var(--radius-md)] bg-primary-500 py-2 text-sm font-semibold text-white"
        >
          책 기록하기
        </button>
      )}
    </section>
  )
}
```

- [ ] **Step 2: AskStripSection (server) 작성**

`src/components/library/AskStripSection.tsx`:

```tsx
import { getUser } from '@/lib/auth'
import { isLibraryEnabled } from '@/lib/library'
import { getPendingAsk, askSourceLabel } from '@/lib/asks'
import AskStrip from '@/components/library/AskStrip'

export default async function AskStripSection() {
  if (!(await isLibraryEnabled())) return null

  const user = await getUser()
  if (!user) return null

  const pending = await getPendingAsk(user.id)
  if (!pending) return null

  // askSourceLabel → "7월 정기모임에서" → strip 라벨은 "7월 정기모임"만 필요
  const label = (askSourceLabel(pending.meetingDate) ?? '정기모임에서').replace('에서', '')

  return <AskStrip meetingId={pending.meetingId} meetingLabel={label} />
}
```

- [ ] **Step 3: 타입 체크 + 린트**

Run: `npx tsc --noEmit && npm run lint`
Expected: PASS

- [ ] **Step 4: 커밋**

```bash
git add src/components/library/AskStrip.tsx src/components/library/AskStripSection.tsx
git commit -m "feat(asks): 물어보기 strip (client) + 서버 게이트 섹션"
```

---

## Task 9: 소개 strip (localStorage, per-device)

기능 첫 노출 때 얇은 안내 1줄. 닫으면 그 기기에서 다시 안 뜸. 전면 모달·투어 금지(스펙 §5).

**Files:**
- Create: `src/components/library/LibraryIntroStrip.tsx`

- [ ] **Step 1: 작성**

`src/components/library/LibraryIntroStrip.tsx`:

```tsx
'use client'

import { useEffect, useState } from 'react'

const STORAGE_KEY = 'library_intro_dismissed'

export default function LibraryIntroStrip() {
  const [visible, setVisible] = useState(false)

  useEffect(() => {
    if (localStorage.getItem(STORAGE_KEY) !== '1') setVisible(true)
  }, [])

  if (!visible) return null

  function dismiss() {
    localStorage.setItem(STORAGE_KEY, '1')
    setVisible(false)
  }

  return (
    <div className="mt-6 flex items-start gap-2 rounded-[var(--radius-md)] bg-surface-50 border border-neutral-200 p-3">
      <p className="min-w-0 flex-1 text-caption text-neutral-600 break-keep">
        새로 생긴 <b className="text-primary-600">내 서재</b> — 모임에서 읽은 책을 담아두면 여기 쌓여요.
      </p>
      <button
        onClick={dismiss}
        aria-label="안내 닫기"
        className="shrink-0 text-neutral-400"
      >
        <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.4" strokeLinecap="round">
          <path d="M18 6 6 18" />
          <path d="m6 6 12 12" />
        </svg>
      </button>
    </div>
  )
}
```

> **참고:** 소개 strip은 측정 대상 아님(단순 UI 안내) → DB 아닌 localStorage로 충분(스펙 §5 "유저별 닫힘 기억"의 저비용 구현). SSR 후 마운트 시 판정이라 첫 프레임 깜빡임은 `visible` 초기값 false로 최소화.

- [ ] **Step 2: 타입 체크 + 린트**

Run: `npx tsc --noEmit && npm run lint`
Expected: PASS

- [ ] **Step 3: 커밋**

```bash
git add src/components/library/LibraryIntroStrip.tsx
git commit -m "feat(asks): 서재 소개 strip (localStorage per-device)"
```

---

## Task 10: 마이페이지에 소개 strip + 물어보기 strip 삽입

순서: 프로필 → **소개 strip → 물어보기 strip** → 내 서재 → 내 신청. strip을 서재 위(눈에 잘 띄게)에 둔다.

**Files:**
- Modify: `src/app/(main)/my/page.tsx`

- [ ] **Step 1: page.tsx 교체**

`src/app/(main)/my/page.tsx` 전체를 아래로 교체(Plan A 상태에 strip 2개 삽입):

```tsx
import { Suspense } from 'react'
import MyRegistrationContent from '@/components/my/MyRegistrationContent'
import RegistrationsSkeleton from '@/components/skeletons/RegistrationsSkeleton'
import ProfileSection from '@/components/my/ProfileSection'
import ProfileSkeleton from '@/components/skeletons/ProfileSkeleton'
import LibrarySection from '@/components/library/LibrarySection'
import LibrarySkeleton from '@/components/skeletons/LibrarySkeleton'
import LibraryIntroStrip from '@/components/library/LibraryIntroStrip'
import AskStripSection from '@/components/library/AskStripSection'

export default function MyPage() {
  return (
    <div className="px-5 pt-6">
      <Suspense fallback={<ProfileSkeleton />}>
        <ProfileSection />
      </Suspense>

      <LibraryIntroStrip />

      <Suspense fallback={null}>
        <AskStripSection />
      </Suspense>

      <Suspense fallback={<LibrarySkeleton />}>
        <LibrarySection />
      </Suspense>

      <h1 className="mt-8 text-xl font-extrabold text-neutral-800 tracking-tight" style={{ fontFamily: 'var(--font-display)' }}>내 신청</h1>
      <Suspense fallback={<RegistrationsSkeleton />}>
        <MyRegistrationContent />
      </Suspense>
    </div>
  )
}
```

> **참고:** 플래그 OFF면 `AskStripSection`·`LibrarySection` 모두 `null` 반환. `LibraryIntroStrip`은 클라이언트 localStorage 판정이라 플래그와 무관하게 렌더되지만, 서재가 안 보이는 상태에서 소개만 뜨면 어색 → **플래그 게이팅을 소개 strip에도 적용**해야 함. 다음 스텝에서 처리.

- [ ] **Step 2: 소개 strip을 플래그 뒤로 이동(서버 게이트로 감싸기)**

`LibraryIntroStrip` 직접 렌더 대신 서버 게이트로 감싸기 위해 `AskStripSection`처럼 처리하지 않고, `LibrarySection`이 플래그 ON일 때만 소개도 의미가 있으므로 **소개 strip을 `LibrarySection` 내부 최상단으로 이동**한다. `src/app/(main)/my/page.tsx`에서 `<LibraryIntroStrip />` 줄과 그 import를 제거:

```tsx
// 삭제할 import
import LibraryIntroStrip from '@/components/library/LibraryIntroStrip'
```

```tsx
// 삭제할 렌더 (ProfileSection 아래 줄)
      <LibraryIntroStrip />
```

그리고 `src/components/library/LibrarySection.tsx`에서 `import`에 추가:

```tsx
import LibraryIntroStrip from '@/components/library/LibraryIntroStrip'
```

`LibrarySection`의 `return (` 직후 `<section ...>` 여는 태그 바로 다음 줄에 소개 strip을 넣는다. 즉 `<section className="mt-8">` 아래에:

```tsx
      <LibraryIntroStrip />
```

- [ ] **Step 3: 타입 체크 + 린트**

Run: `npx tsc --noEmit && npm run lint`
Expected: PASS

- [ ] **Step 4: 커밋**

```bash
git add "src/app/(main)/my/page.tsx" src/components/library/LibrarySection.tsx
git commit -m "feat(asks): 마이페이지 물어보기 strip + 소개 strip(플래그 게이트) 삽입"
```

---

## Task 11: LibraryGrid 출처 라벨 "N월 정기모임에서"

Plan A는 `source==='ask'`에 "모임에서 담음" 간이 라벨. Plan B에서 출처 모임 월로 정밀화.

**Files:**
- Modify: `src/components/library/LibraryGrid.tsx`

- [ ] **Step 1: import + 라벨 교체**

`src/components/library/LibraryGrid.tsx` 상단 import에 추가:

```tsx
import { askSourceLabel } from '@/lib/asks'
```

`entry.source === 'ask'` 라벨 블록(Plan A의 "모임에서 담음")을 아래로 교체:

```tsx
            {entry.source === 'ask' && (
              <p className="mt-0.5 text-[10px] text-neutral-400 leading-tight break-keep">
                {askSourceLabel(entry.source_meeting_date) ?? '모임에서 담음'}
              </p>
            )}
```

> **참고:** `askSourceLabel`은 순수 함수라 client 컴포넌트에서 import 안전(`@/lib/asks`의 서버 함수는 이 파일에서 호출 안 함 — tree-shaking이 아니라 함수 자체가 순수라 문제 없음. 단 `@/lib/asks`가 `createServiceClient`를 top-level import하므로, client 번들 오염 방지를 위해 순수 헬퍼를 별도 파일로 분리해야 하면 아래 스텝 2에서 처리).

- [ ] **Step 2: client 번들 오염 방지 — 순수 헬퍼 분리 여부 확인**

`src/lib/asks.ts`는 top-level에서 `import { createServiceClient } from '@/lib/supabase/admin'`를 한다. 이걸 client 컴포넌트(`LibraryGrid`)가 import하면 서버 전용 코드가 client 번들에 섞여 빌드/런타임 경고가 날 수 있다. 이를 피하기 위해 **순수 헬퍼(`ASK_WINDOW_DAYS`, `isAskEligibleMeeting`, `askSourceLabel`)를 `src/lib/asks-pure.ts`로 분리**하고 `asks.ts`는 그걸 re-export한다.

`src/lib/asks-pure.ts` 생성 — Task 3의 순수 함수 3개를 이 파일로 이동:

```typescript
import { getDaysUntil } from '@/lib/kst'

export const ASK_WINDOW_DAYS = 60

type EligibleMeetingShape = {
  date: string
  meeting_type: string
  status: string
}

export function isAskEligibleMeeting(
  meeting: EligibleMeetingShape,
  kstToday: string,
  windowDays = ASK_WINDOW_DAYS,
): boolean {
  if (meeting.meeting_type !== 'regular') return false
  if (meeting.status !== 'active') return false
  const days = getDaysUntil(meeting.date, kstToday)
  return days < 0 && days >= -windowDays
}

export function askSourceLabel(date: string | null | undefined): string | null {
  if (!date) return null
  const month = Number(date.slice(5, 7))
  if (!month) return null
  return `${month}월 정기모임에서`
}
```

`src/lib/asks.ts` 상단에서 순수 함수 정의를 지우고 re-export로 교체:

```typescript
export { ASK_WINDOW_DAYS, isAskEligibleMeeting, askSourceLabel } from '@/lib/asks-pure'
import { isAskEligibleMeeting } from '@/lib/asks-pure'
```

(이후 `asks.ts`의 서버 함수들은 그대로 `isAskEligibleMeeting`을 사용.)

`src/lib/__tests__/asks.test.ts`의 import를 `@/lib/asks-pure`로 변경:

```typescript
import { isAskEligibleMeeting, askSourceLabel, ASK_WINDOW_DAYS } from '@/lib/asks-pure'
```

`LibraryGrid.tsx`와 `AskStripSection.tsx`의 `askSourceLabel` import도 `@/lib/asks-pure`로 변경:

```tsx
import { askSourceLabel } from '@/lib/asks-pure'
```

- [ ] **Step 3: 타입 체크 + 린트 + 테스트**

Run: `npx tsc --noEmit && npm run lint && npx vitest run src/lib/__tests__/asks.test.ts`
Expected: PASS

- [ ] **Step 4: 커밋**

```bash
git add src/lib/asks-pure.ts src/lib/asks.ts src/lib/__tests__/asks.test.ts src/components/library/LibraryGrid.tsx src/components/library/AskStripSection.tsx
git commit -m "feat(asks): 출처 라벨 N월 정기모임 + 순수 헬퍼 분리(client 번들 안전)"
```

---

## Task 12: admin 응답률 미니 카운트 + 메뉴

**Files:**
- Create: `src/app/(admin)/admin/library/page.tsx`
- Modify: `src/components/admin/adminMenu.ts`

- [ ] **Step 1: 페이지 작성**

`src/app/(admin)/admin/library/page.tsx`:

```tsx
import { isLibraryEnabled } from '@/lib/library'
import { getAskStats } from '@/lib/asks'

export default async function AdminLibraryPage() {
  const enabled = await isLibraryEnabled()

  if (!enabled) {
    return (
      <div>
        <h1 className="text-xl font-extrabold text-neutral-800 tracking-tight">서재 응답률</h1>
        <p className="mt-4 text-sm text-neutral-600">
          서재 기능이 꺼져 있습니다. (`site_settings.library_enabled = 'on'`)
        </p>
      </div>
    )
  }

  const stats = await getAskStats()

  const cards: { label: string; value: string; hint?: string }[] = [
    { label: '물어보기 응답률', value: `${stats.responseRate}%`, hint: '담음 ÷ 정기 참여(최근 60일)' },
    { label: '정기 참여(분모)', value: `${stats.denominator}건` },
    { label: '담음(응답)', value: `${stats.answered}건` },
    { label: '닫음', value: `${stats.dismissed}건` },
    { label: '미응답', value: `${stats.pending}건`, hint: '분모 − 담음 − 닫음' },
  ]

  return (
    <div>
      <h1 className="text-xl font-extrabold text-neutral-800 tracking-tight">서재 응답률</h1>
      <p className="mt-1 text-caption text-neutral-500">
        북극성: 물어보기 응답률 · 폐기선: 4주 내 참여자 30% 미응답
      </p>

      <div className="mt-5 grid grid-cols-2 gap-3 sm:grid-cols-3">
        {cards.map((c) => (
          <div key={c.label} className="rounded-[var(--radius-md)] border border-neutral-200 bg-white p-4">
            <p className="text-caption text-neutral-500">{c.label}</p>
            <p className="mt-1 text-2xl font-extrabold text-neutral-800">{c.value}</p>
            {c.hint && <p className="mt-1 text-[11px] text-neutral-400 break-keep">{c.hint}</p>}
          </div>
        ))}
      </div>

      <p className="mt-4 text-[11px] text-neutral-400 break-keep">
        “봄·안 담음(노출됐지만 미응답)”의 정밀 구분은 GA4 이벤트(ask_strip_view / ask_answered / ask_dismissed)에서 확인.
      </p>
    </div>
  )
}
```

- [ ] **Step 2: adminMenu에 항목 추가**

`src/components/admin/adminMenu.ts`의 '운영' 그룹 `items` 배열 끝에 추가:

```typescript
      { label: '서재 응답률', href: '/admin/library' },
```

- [ ] **Step 3: 타입 체크 + 린트**

Run: `npx tsc --noEmit && npm run lint`
Expected: PASS

- [ ] **Step 4: 커밋**

```bash
git add "src/app/(admin)/admin/library/page.tsx" src/components/admin/adminMenu.ts
git commit -m "feat(asks): admin 서재 응답률 미니 카운트 + 메뉴"
```

---

## Task 13: 전체 QA + 플래그/측정 문서화

**Files:**
- (검증만, 코드 변경 없음)

- [ ] **Step 1: 전체 파이프라인 통과 확인**

Run: `npm run prelaunch`
Expected: lint + tsc + test + build 전부 PASS

- [ ] **Step 2: 플래그/측정 사용법 확인 (커밋 메시지로 남김)**

- 플래그: Plan A와 동일 `site_settings.library_enabled`. ON = 서재 + 물어보기 + 소개 strip + admin 카운트 전부 활성. OFF = 즉시 전부 사라짐(배포 불필요).
- GA4 3이벤트: `ask_strip_view`(노출), `ask_answered`(담음), `ask_dismissed`(닫음). GA4에서 `ask_answered ÷ ask_strip_view` = "본 사람 중 응답률".
- admin `/admin/library`: 분모=최근 60일 정기 참여 건, 분자=담음/닫음, 응답률=담음÷분모.
- 북극성 = 물어보기 응답률 · 폐기선 = 4주 내 참여자 30% 미응답 → 방식 재검토.

- [ ] **Step 3: 최종 커밋 (verification 완료 마킹)**

```bash
git commit --allow-empty -m "chore(asks): 1조각-B 물어보기+측정 prelaunch 통과"
```

> **사용자 수동 작업 (배포 후):** Vercel preview에서 `library_enabled='on'`으로 켜고 (1) 최근 60일 내 정기모임 confirmed 참여 계정으로 `/my` 접속 → 물어보기 strip 노출·책 기록·닫기 확인, (2) 담은 책이 서재에 "N월 정기모임에서" 라벨로 뜨는지, (3) 소개 strip 닫으면 다시 안 뜨는지, (4) `/admin/library` 카운트가 증가하는지, (5) GA4 실시간에서 3이벤트 수신 확인. 토론모임 참여 계정에는 strip이 **안 뜨는지** 반드시 확인.

---

## Self-Review 체크 (플랜 작성자 확인 완료)

- **스펙 커버리지:** 물어보기 트리거=정기 참여 판정(Task 4 `getPendingAsk`, `meeting_type='regular'`+confirmed 필터) / 인라인 strip·1건만·✕ 닫기·1모임 1회(Task 8 + book_asks UNIQUE) / 문구 확정본(Task 8, 투표 언급 없음) / 자동담음 source='ask'+출처 라벨(Task 6·11) / 소개 strip 닫힘 기억(Task 9) / GA4 3상태(Task 7·8) / admin 응답률 3분해(Task 12) / 토론 제외(자격 판정 regular 필터) / 플래그 격리(전 라우트·섹션). **과거 소급 방지**는 60일 윈도우로 근사(스펙 §6-0 "앞으로의 정기모임만") — 분모/트리거 동일 윈도우라 지표 정합.
- **플레이스홀더 스캔:** 없음. 모든 스텝에 실제 코드/명령/기대결과.
- **타입 일관성:** `PendingAsk`(meetingId/meetingDate) getPendingAsk→AskStripSection→AskStrip 일관. `BookSearchResult` ask 답변 경로 재사용(Plan A와 동일). `LibraryEntryWithBook.source_meeting_date` 조회(Task 5)→라벨(Task 11) 일관. `AskStats` getAskStats→admin page 일관. API 응답 전부 `{status,...}`. **client 번들 안전:** 순수 헬퍼를 `asks-pure.ts`로 분리해 client(`LibraryGrid`)가 service_role import를 끌어오지 않게(Task 11).
- **의존성 순서:** Plan A 완료 전제 → Task 1(스키마)→2(타입)→3(순수 TDD)→4(서버헬퍼)→5(enrich)→6(라우트)→7(입력 재사용)→8(strip)→9(소개)→10(삽입)→11(라벨+분리)→12(admin)→13(QA). 앞 산출물만 참조.
- **주의(실행자용):** Task 3에서 순수 함수를 `asks.ts`에 넣었다가 Task 11에서 `asks-pure.ts`로 옮긴다. 실행 순서상 정상이나, 서브에이전트가 Task 11을 건너뛰면 client 번들 경고가 남으니 Task 11 스텝 2를 반드시 수행할 것.

---

## 1조각 = Plan A + Plan B 통합 배포 노트

- 두 플랜은 **같은 브랜치(`feat/discussion-slice1`) 순차 커밋**, **하나의 플래그(`library_enabled`)** 뒤에 격리. A만 켜고 B만 끄는 분리 없음(1조각 = 서재+물어보기 한 덩이).
- 배포 계층 = **회원**(스펙 §4). CLAUDE.md 브랜치 전략상 회원 변화는 가장 신중 → Vercel preview 검증 필수 후 prod 플래그 ON.
- 마이그레이션 2개(`migration-library.sql` → `migration-library-asks.sql`) 순서대로 먼저 실행. env `KAKAO_REST_API_KEY` 1개 등록. 모두 forward-compatible이라 코드 배포 전 실행해도 화면 영향 0.
- 롤백: 플래그 OFF(즉시) → 필요 시 코드 revert → 스키마는 남겨도 무해(미소비).
