# 제품기획 로그

## 2026-09-11 — 런북 §4 예고 문구 확정본 반영 (초안 → 확정, 4종)

**한 일**

- `검토문서/2026-08-18-켜는날-런북.md` **§4 전면 교체** — 초안 3종 → 확정 4종
  - **§4-1 예고 스트립: 1줄 → 2단 표** (10/1~10/8 `10월 11일, …` / 10/9~10/10 `이번 일요일, …` / 10/11 아침 비움). 왜 2단인가(닫기 키가 문구별) · 왜 10일인가 · 왜 「이번 주」를 뺐나 · 렌더 실측을 함께 적음
  - **§4-1-b 카톡 채널 예고 신설** — 원래 런북에 없던 절. 10/9 또는 10/10 중 하루 **1회만**. `👉 brainy-club.com`·`똑똑!`을 뺀 이유 명기
  - **§4-2 「이야기」 한 줄 교체** — `토론모임 발제문에 미리 생각을 남겨요` → `토론모임 이야기를 나누는 곳이에요`. **확정 사안 변경**이라 원문·이유·경위를 블록으로 남김
  - **§4-3 알림함 첫 공지 교체** — 제목·내용 전면 교체 + 이동 화면 `/talk` → **`/meet`**. 길이 실측 병기
- **§0 사전 조건 4행 손봄 + 1행 신설**(관리자 지시 + 내 편집이 낡게 만든 것)
  - 「예고 스트립 게시(켜기 1~2일 전)」 → **10/1부터 10일, 2단 교체·10/11 비움** (지시받은 행)
  - 「카톡 채널 예고 1회」 **신설**
  - 「예고 문구 3종 확정 — 🙋 미확정」 → **4종 ✅ 확정(2026-09-11)** — 이 행은 *"§4는 전부 초안이고 N월 N일이 비어 있다"*고 말하는데, 내 §4 편집이 그 문장을 **거짓으로 만든다**. 두고 오면 바로 그다음 사람이 "문구부터 정해야 한다"로 읽는다
  - 「10월 정기모임 등록」 **신설** — §4-3이 `/meet`로 보내므로 한 묶음. 🔴 **관리자 지시는 "§0의 「10월 모임 등록」과 연결하라"였으나 그 행이 §0에 없었다** (아래 참조)
  - 「켜는 날 = 조용한 날 · 🙋 결정」 → **✅ 2026-10-11(일) 확정**
  - 표 머리글 `상태 (2026-08-18 기준)` → `상태 (행마다 잰 날짜를 함께 적는다)` — 방금 2026-09-11 사실을 넣은 행 위에 "8-18 기준"이 얹혀 있으면 그 자체가 모순

**왜 그렇게 했는가**

- **문구는 한 글자도 안 바꿨다.** 디자인 확정본이고, 지시도 그랬다. 대신 **왜 그렇게 정했는지**를 전부 옮겨 적었다 — 근거가 안 붙은 문구는 당일에 "조금 더 자연스럽게" 고쳐진다
- **§4-1 「렌더 실측」에 🟨를 달았다.** 2단은 320px 실측(`strip-2dan-320.png`)이 있는데 **1단은 390px 한 장뿐**이다. "둘 다 320까지 통과"라고 적으면 재본 적 없는 것이 사실로 굳는다
  - **같은 날 관리자가 이 🟨를 실측으로 닫았다** — 1단도 320px에서 한 줄(텍스트 영역 201px, 2단과 동일). 🟨 표기를 지우고 두 출처(디자인·관리자)를 함께 적었다
  - 🔴 **그 실측이 첫 시도에서 「둘 다 두 줄」로 나왔고 하마터면 "디자인 검증이 틀렸다"가 될 뻔했다.** 원인은 문구가 아니라 재현 하네스(`width:320px`만 주고 flex 부모에 넣어 실제 폭이 262px로 찌그러짐). **교훈 한 줄을 §4-1 아래 인용 블록으로 남겼다** — 켜는 날 이후에도 이 문서로 문구를 재는 사람이 같은 함정을 밟는다
- **기각한 초안도 길이는 통과했다는 사실을 일부러 남겼다.** 이게 없으면 다음 사람이 "짧으니 「이번 주 일요일」이 더 친절하다"로 되살린다. 뒤집힌 것을 지우지 않는 이유와 같다
- **§4-2 변경을 각주가 아니라 본문 블록으로 적었다.** 확정 사안을 바꿨기 때문이다. 원문·이유·**누가 발견했고 누가 결정했는지**가 같이 있어야 다음에 "왜 약속이 약해졌지"로 되돌려지지 않는다

**안 한 것과 이유**

- 🔴 **`DECISIONS.md`에 줄을 안 늘렸다 — 지시가 명시적으로 금지했다**("관리자가 따로 본다"). 이 부서 기준으로는 **결정이 났는데 `DECISIONS.md`에 없는 상태**이므로, 남아야 할 4줄을 관리자에게 초안으로 넘겼다. **관리자가 적을 때까지 이 건은 닫힌 것이 아니다**
- §4-4 문의 3종 · §1 · §2 · §3 — 범위 밖. 단 §1 표 3행(「예고 스트립 문구 비우기」)은 §4-1 표와 서로 가리키게만 해 두고 내용은 안 건드렸다
- 커밋 안 함 (지시)
- 신청 시점이 D-2~D-9에 퍼져 있다는 실측은 **내가 재현하지 않았다** — 관리자 실측으로 출처를 밝혀 적었다

**주고받은 메시지**

- ← 관리자: §4 확정본 반영 지시(문구 4종 + §4-2 변경 결정 + 10월 모임 연결)
- → 관리자: 반영 보고 + 지시 오류 2건(아래) + `DECISIONS.md` 4줄 초안

**🔴 관리자 지시에서 확인해보니 틀린 것 2건**

1. **`10/8(수)` → 실제는 `10/8(목)`.** 10/11이 일요일이므로 10/9 금 · 10/8 목이다(둠스데이: 2026년 둠스데이 = 토, 10/10이 둠스데이 날짜 → 토). 문구가 아니라 **날짜 라벨**이라 그대로 고쳤다. 그냥 뒀으면 게시 담당이 수요일(10/7)에 하루 일찍 2단으로 갈아 끼운다
2. **`§0 사전 조건의 「10월 모임 등록」`이라는 행은 §0에 없었다.** 그 사실은 `DECISIONS.md` 2026-09-10 켜는 날 행 **본문 안에만** 있었다 — 지시는 "서로 연결해 적어라"였으므로 **행을 새로 만들어** §4-3과 양방향으로 걸었다. 있는 줄 알고 링크만 걸었으면 **아무 데도 안 적힌 채 지나갔을 사전 조건**이다

**다음**

- 관리자가 `DECISIONS.md` 4줄을 적으면 이 건 종료. 안 적히면 다음 세션에 이 로그만 남는다

**관리자 판정**

- (관리자가 채움)

---

## 2026-09-10 — 켜는 날 날짜 확정 + 게이트 실행 반영 + 돈 점검 방향 신설 (3건)

**한 일**

- `DECISIONS.md` **신규 2행 · 갱신 1행 · 「아직 정하지 않은 것」 1줄**
  - 제품·정책 맨 위 — **켜는 날 = 2026-10-11(일)** (신규, `🔨 미적용`)
  - 기술·운영 맨 위 — **돈 점검은 결제사 → DB 방향** (신규, `✅ 적용`)
  - 기술·운영 2026-08-22 「머지 순서」 행 — `🔨 미적용 (머지 전)` → **`✅ 적용 (retarget·#66 draft — 2026-09-10) / 🔨 미적용 (머지 자체는 켜는 날)`**
  - 「아직 정하지 않은 것」 — **대사를 정기 루틴에 넣을지** 1줄

**왜 그렇게 했는가**

- **관리자가 준 사실을 전부 직접 재현했다.** 2026-08-21에 관리자가 "명백하다"며 직접 쓴 여섯 줄이 다 틀렸던 전력이 지시서에 붙어 있었다. `gh pr view 53/66`(base·draft·files 11개), 런북 §1-3 존재, `reconcile-payments.ts` 존재·읽기 전용, 조사 문서 §4-1의 실행 출력, 10/9=금·10/11=일·10/14=수 요일 계산 — **전부 일치했다**
- **켜는 날 행에 「10월 모임이 비어 있다」를 넣었다.** 날짜만 적으면 다음 사람이 절차만 보고 켠다. 개편 홈은 "할 일"만 보여주는 구조라(2026-08-15 행) **모임이 없으면 신규 회원이 빈 화면을 맞는다** — 절차가 아니라 **사전 조건**이라고 명시했다
- **머지 순서 행에 「결정된 날 ≠ 리모트가 바뀐 날」을 남겼다.** 18일 벌어져 있었고, 그동안 착각이 없었던 이유가 게이트 표의 한 문장이었다. 그 문장이 왜 효과가 있었는지가 다음 판단의 근거다
- **돈 점검 행에 「기지 고아는 매번 뜬다」 경고를 넣었다.** 대사 도구가 계좌이체를 못 보므로 **선의로 두 번 환불할 위험**이 결정문 자체에 붙어 있어야 한다. 스크립트 주석에도 있지만, 도구를 안 열고 결정만 읽는 사람이 있다

**안 한 것과 이유**

- **「정기 루틴 편입」을 상태 칸에 `🔨 미적용`으로 넣지 않았다.** 관리자 지시는 *"그 부분만 따로 표시하라"*였는데, `🔨 미적용`은 **"정해졌지만 코드에 없다"**는 뜻이라 **아직 안 정해진 것**에 쓰면 파일이 스스로 정한 뜻이 깨진다. 대신 상태 칸에 `⚠️ 미결` 포인터를 달고 **「아직 정하지 않은 것」에 실물 줄**을 만들었다 — 표시 목적(다음 사람이 본다)은 그대로 달성되고, 상태 칸 규칙(다섯 개가 전부)도 안 깨진다
- **`10월 모임 0건`을 직접 재현 못 했다.** prod 조회 도구가 이 세션에 없고, `npx tsx scripts/*`는 공통규약 §6 금지다. **관리자 실측으로 출처를 밝혀 적었다**
- **`CLAUDE.md` 켜는 날 게이트 표 미수정** — 관리자 소유(§3). 게이트 1번은 이제 실제로 닫혔으므로 **관리자가 그 표를 손봐야 한다**(아래 「다음」)
- 런북·`logs/관리자.md`·루트 `CLAUDE.md` **미수정** — 이미 갱신돼 있다는 지시대로 열어서 확인만 했다
- **커밋 안 함** — 지시대로 관리자가 묶는다

**주고받은 메시지**

- ← 관리자: 3건 반영 지시 + *"그대로 베끼지 말고 근거를 직접 확인하라"*
- → 관리자: 반영 위치 + 재현 결과 보고

**다음**

- 🙋 **관리자** — 루트 `CLAUDE.md` 켜는 날 게이트 표에서 **1번(retarget)을 닫고, "리모트 변경 대기 중"이라는 문장을 걷어낼 것.** 오늘 실행됐다. 그 표는 관리자 소유라 여기서 못 고친다. **표가 하루 낡는 사고가 2026-08-22에 이미 한 번 났다**
- 🙋 **관리자·대표님** — 켜는 날 행에 적은 **10월 모임 등록**. 켜는 날 절차가 아니라 그 전에 끝나야 하는 것이라 담당이 정해져야 한다

**관리자 판정** — 🟢

- **지시를 고쳐서 따른 것이 이번 소환의 값이다.** 「정기 루틴 편입」을 `🔨 미적용`으로 넣으라는 내 지시는 **틀렸다.** `🔨`는 *"정해졌지만 코드에 없다"*는 뜻이라 **정해진 것**에만 쓴다. 안 정해진 것에 붙였으면 이 파일이 스스로 세운 규칙을 관리자 지시로 깨는 꼴이었다. **부서가 파일 규칙을 나보다 정확히 읽었다.**
- 재현하라는 요구를 실제로 수행했고, **재현 못 한 것 하나(`10월 모임 0건`)를 감추지 않고 출처를 관리자로 밝혔다.** 도구가 없으면 없다고 하는 것이 맞다.
- 켜는 날 행에 **「10월 모임이 비어 있다」를 사전 조건으로 넣은 판단**이 좋다. 날짜만 적힌 결정문은 다음 사람이 절차만 보고 켜게 만든다.

🔴 **다만 「다음」의 관리자 지시 1건은 오탐이다 — 관리자가 실측으로 확인했다.**

> *"루트 `CLAUDE.md` 게이트 표 1번을 닫고 「리모트 변경 대기 중」 문장을 걷어낼 것"*

**이미 오늘 오전에 고쳐져 있다.** 실측: `CLAUDE.md:121` = **`### ✅ 켜는 날 게이트 — 절차는 전부 닫혔다`**, `:144` = 게이트 3번 `✅ 닫혔다`, `:156` = *"~~남은 2건은…대기 중~~ → ✅ 2026-09-10 실행 완료"*.

**왜 반대로 읽혔는지가 중요하다.** 닫힌 항목을 지우지 않고 **`<details>`로 접어 취소선으로 남기는** 것이 이 저장소 방식이고(결정 기록 규칙과 같은 이유다), **접힌 안쪽의 옛 문장을 현행으로 읽으면 정확히 이 오탐이 난다.** 취소선과 `~~` 안쪽은 **과거 기록**이다.

> 이 오탐 자체는 해롭지 않았다 — 관리자가 걸렀다. **부서 보고를 그대로 받지 않는 것이 관리자 몫**이고, 같은 날 개발·운영의 주장 셋도 같은 방식으로 재현해서 받았다. 다만 다음부터 **접힌 절·취소선 안쪽을 근거로 삼지 말 것.**

- 남은 지시 1건(**10월 모임 등록 담당**)은 유효하다. 대표님께 이미 올렸다.

---

## 2026-08-26 (2차) — `VOICE.md` 범위 확대 기각을 기록

**한 일**

- `DECISIONS.md` 제품·정책에 **행 1개** (🚫 안 함) — `VOICE.md` 적용 범위를 인앱까지 넓히지 않음

**왜 그렇게 했는가**

- **기각 근거를 결정문 본문에 넣었다.** *"문서가 무엇을 위해 쓰였는지가 있는데, 거기 없는 대상이 나왔다고 범위를 늘리는 것은 순서가 거꾸로"* — 이게 없으면 다음 세션이 같은 제안을 다시 올린다. `🚫 안 함`의 목적이 **다시 제안되는 걸 막는 것**이라고 그 파일이 스스로 적어뒀다
- **「인앱 어미 기준이 없다」를 사실로만 적고 제안으로 쓰지 말라는 경고를 붙였다.** 관리자 지시 그대로다. 사실과 제안이 한 줄에 섞이면, 사실이 근거가 되어 제안이 저절로 되살아난다 — 이 파일이 막으려는 바로 그 고장이다
- **파일명 변경은 "별도 판단"으로 남겼다.** 참조 문서 영향을 안 봤으므로 여기서 닫지 않는다

**안 한 것과 이유**

- **`VOICE.md` 미수정** — 디자인 소유(공통규약 §3 예외 2건 중 하나). 제목·범위 명시는 디자인이 작업 중
- 어미 제안 8건 재검토·인앱 기준 초안 **안 함** — 지시 밖이고, 대표님이 요청하지 않았다

**주고받은 메시지**

- ← 관리자: 대표님 결정 통보 + 근거·재발 방지·남는 사실을 함께 적으라는 지시, "이 한 줄만"
- → 관리자: 기록 완료 보고

**다음**

- 없음 (1차 회차의 「다음」은 그대로 유효 — 쿼리 B·E·F 결과 대기)

**관리자 판정** — 🟢 통과.

- **연 파일** — `git diff 검토문서/DECISIONS.md`로 **행이 실제로 들어갔는지** 확인. 🚫 안 함 행에 경위·기각 근거·함께 정해진 것·남는 사실이 다 들어 있다
- **「사실과 제안을 한 줄에 섞으면 사실이 근거가 되어 제안이 저절로 되살아난다」** — 이 관찰이 정확하다. 그래서 본문에 *"이 줄을 인앱 기준 신설 제안으로 쓰지 말 것"* 경고를 넣은 것이 맞다. `🚫 안 함` 상태의 목적이 **다시 제안되는 걸 막는 것**인데, 근거 문장이 그 안에서 새 제안의 씨앗이 되면 상태가 무력해진다
- **`VOICE.md`를 안 건드린 것도 옳다** — 디자인 소유다. 한 줄이라도 넘어가지 않았다

---

## 2026-08-26 — 온보딩 전환 검토: 병목은 온보딩이 아니라 그 다음 화면이다

> 🔺 **이번 회차는 「전환 설계자」 렌즈를 겸했다.** 성장 부서는 아직 정의 파일이 없어(`.claude/agents/`에 4개뿐) 관리자 지시로 제품기획이 그 관점을 대신 들었다. **부서 정의가 생기면 이 회차 산출물은 성장이 이어받는 것이 맞다.**

**한 일**

- `검토문서/2026-08-26-온보딩-전환검토.md` 작성 — 로그인·환영·프로필 입력 3화면 + **온보딩 직후 도착 화면**
- `DECISIONS.md` **2곳** — 북극성 행 1개 추가(🔨 미적용) + 「아직 정하지 않은 것」에 **신규 회원 도착 화면** 1건 추가
- 개발·운영에 넘길 **prod SELECT 6개**를 문서 안에 그대로 실행 가능한 형태로 남김

**왜 그렇게 했는가**

- **관리자 질문 1(60명은 어디서 멈췄나)에 "온보딩이 아니다"로 답했다.** 근거는 명단 §0~§3의 **60 대 6**이다 — 온보딩 3화면을 완벽히 고쳐도 6명이 최대치고 60명은 그대로다. 요청은 온보딩 리스킨이었지만, **그 요청대로만 답하면 관리자가 미리 경고한 "화면만 예뻐지고 숫자는 그대로"가 정확히 일어난다**
- **가장 큰 발견은 요청 밖이 아니라 요청 한복판에 있었다** — 개편 홈(`(next)/home`)이 신규 회원에게 구조적 빈 화면이다(4블록 전부 "이미 뭔가 한 사람" 전제). **온보딩 직후 도착 화면을 보라는 것이 질문 2였으므로 범위 안이다**
- **그것을 "결함"으로 쓰지 않았다.** 2026-08-15 결정(*"홈에 목록을 두지 않는다"*)이 명확한 의도이고 지금도 유효하다. 공통규약 §1의 3단계대로 **"전제가 바뀌었다"**로 적었다 — 그 결정은 할 일이 있는 기존 회원을 상정했고, *"신청 0건이 최대 집단"*이라는 숫자는 **8/15보다 일주일 늦은 8/22에** 나왔다. 그리고 요구를 **"목록을 되살려라"가 아니라 "행동 하나"**로 좁혀 8/15와 충돌하지 않게 했다
- **그 항목은 `DECISIONS.md` 표에 넣지 않고 「아직 정하지 않은 것」에 넣었다.** 회원 전체 노출 = 되돌리기 어려운 것이라 부서 정의 §7상 **정하지 말고 올리는** 사안이다. 표에 넣으면 승인 안 받은 것이 "정해진 것"으로 굳는다
- **북극성은 정했다.** 이건 되돌릴 수 있고(측정 정의일 뿐) 관리자가 명시적으로 시켰다. **계측을 심지 않아도 도는 지표를 고른 것이 핵심 판단**이다 — 물어보기 응답률 100% 초과 사고가 분자·분모를 다른 곳에서 가져와 났으므로, 두 테이블로 끝나는 정의를 택했다. 덕분에 **개편 전 기준선을 켜기 전에** 확보할 수 있다
- **쿼리 6개에 한계를 같이 적었다.** 쿼리 B는 삭제된 모임 때문에 **과소 집계(상한)**, 쿼리 C의 `last_sign_in_at`은 새 OAuth 로그인에서만 갱신돼 **「확인 불가」를 "안 왔다"로 읽으면 안 된다**. 한계 없이 넘기면 다음 사람이 그 숫자를 사실로 읽는다
- **`payment_mode` 한 줄 쿼리를 따로 세웠다.** `transfer_only`면 첫 신청이 「카드 한 번」이 아니라 「은행 앱 열어 입금자명 규칙대로 송금」이다 — **진단이 통째로 갈리는데 아직 아무도 안 확인했다**

**안 한 것과 이유**

- **시안·문구·화면 배치 전부 안 함** — 금지 확인. 환영 CTA(「이번 달 모임 보기」)가 프로필 폼으로 떨어지는 것도 **흐름 사실로만** 적고 문구는 안 건드렸다(UX 라이터 소관)
- **전화번호 필수를 안 흔들었다** — 근거 있는 결정(2026-08-13, 프로필 완료 순간 환영 알림톡). 지시대로
- **prod 조회를 직접 안 했다** — DB 도구가 이 세션에 없다. 추정으로 메우지 않고 **쿼리로 남겨 개발·운영에 넘겼다**
- **"60명이 왜 안 했는지"를 단정하지 않았다** — 온보딩 구간 `trackEvent` **0건** + GA4 `user_id` 미설정이라 화면 행동으로는 못 가른다. 못 가르는 것을 못 가른다고 적었다
- **코어 문서·로드맵 미수정** — 이번 검토에서 개정 근거가 나오지 않았다

**주고받은 메시지**

- ← 관리자: 온보딩 전환 검토 지시(전환 설계자 렌즈 겸함, 질문 4개, 산출물 경로·형식 지정)
- → 관리자: `검토문서/2026-08-26-온보딩-전환검토.md` 완료 보고 + **관리자 판정이 필요한 1건**(신규 회원 도착 화면) + 개발·운영에 넘길 쿼리 6개 안내

**다음**

- 쿼리 B·F 결과가 오면 §5 질문 3개를 **그 값으로 다시 좁힌다** (특히 `payment_mode`)
- 쿼리 E 결과로 북극성 창(14일)을 확정 → `DECISIONS.md` 해당 행 갱신

**관리자 판정** — 🟢 통과. **"온보딩을 고쳐라"는 지시를 받고 "온보딩은 병목이 아니다"라고 답했다.**

- **연 파일** — `검토문서/2026-08-26-온보딩-전환검토.md` · `DECISIONS.md` 북극성 행(실제 추가 확인) · `components/next/HomeView.tsx:163-168`(빈 상태가 링크가 아님 확인)
- 🔴 **이 회차의 값어치는 지시를 그대로 수행하지 않은 데 있다.** 나는 온보딩 3화면을 새로 입히려고 넷을 불렀는데, 이 부서는 **72명 중 66명이 온보딩을 완주한다**는 사실을 먼저 내놓았다. **고치려던 곳이 안 망가져 있었다.** 화면만 예뻐지고 숫자는 그대로일 뻔했다
- 🔴 **그리고 진짜 위험을 찾았다** — 새 홈이 신규 회원에게 **구조적으로 빈 화면**이고, 지금 홈에는 있는 신청 버튼이 **켜는 순간 사라진다.** 그 대상이 정확히 최대 집단(60명)이다. **켠 뒤 발견하면 되돌리는 게 플래그 flip이 아니라 새 화면**이라는 지적이 이 건의 무게를 정확히 재고 있다
- **2026-08-15 결정을 뒤집자고 하지 않은 것이 옳다.** *"홈에 목록을 두지 않는다"*는 유효하고, 그 결정이 **할 일이 있는 기존 회원**을 상정했으며 *"신청 0건이 최대 집단"*이라는 숫자는 **일주일 뒤에** 나왔다는 것까지 갈랐다. 요구가 **목록이 아니라 행동 하나**라는 것도 정확하다
- **북극성을 계측 없이 도는 것으로 고른 판단에 동의한다.** 물어보기 응답률이 100%를 넘겼던 사고(분자·분모 어긋남)를 피하고, **개편 전 기준선을 켜기 전에 확보**할 수 있다. 분모에 온보딩 미완자를 넣은 이유(*"빼면 이탈이 숫자에서 숨는다"*)도 맞다
- **못 가른 것을 추정하지 않았다** — 홈에 도착해 무엇을 눌렀는지는 `trackEvent` 0건 + GA4 `user_id` 미설정이라 알 수 없다고 적었다
- **관리자 결정** — 🔴 **「신규 회원의 홈이 빈 화면」은 켜기 전에 닫는다.** 온보딩 시안 회차에 **홈 첫 화면까지 포함**시킨다. 온보딩만 고치고 도착지를 그대로 두면 이번 작업이 무의미하다. 쿼리 **B·F**(그 시점에 신청할 모임이 있었나 / `payment_mode`가 무엇인가)는 개발·운영에 넘긴다 — **F 한 줄이 진단을 통째로 가른다**

---

## 2026-08-22 (3차) — 켜는 날 순서를 뒤집었다 (env → 머지) + SHA 스냅샷 표시

**한 일**

- 런북 §1 **표 재배열** — `SQL → env 교체(재배포 금지) → 머지 → 플래그 flip`. 단계 번호도 다시 매겼다
- 런북 **§1-2 신설** — 공식 문서 인용 + **왜 반대로 굳었는지 경위**
- `DECISIONS.md` **행 1개 추가**(켜는 날 순서 확정, ✅ 적용) + 머지 순서 행에 **retarget 결정 합침**
- SHA 스냅샷 표시 2곳 — `DECISIONS.md` 재확인 헤더, 우선순위 문서 헤더

**왜 그렇게 했는가**

- **전제 한 단어가 빠진 것이 사고의 원인이었다.** 런북 경고문은 *"env만 바꾸면 알림톡이 실패한다"*였는데 **"재배포하면"이 빠져 있었다.** 틀린 문장이 아니라 **전제가 빠진 문장**이라 더 위험했다 — 읽는 사람이 "env를 먼저 바꾸면 위험하다"로 정확히 반대로 읽고, 순서가 그렇게 굳었다. 그래서 §1-2에 **그 경위를 남겼다.** 다음 사람이 "왜 뒤집었지"에 답을 찾아야 한다
- **위험한 것은 env 교체가 아니라 그 뒤의 재배포**라는 것을 표 1행에 🔴로 못 박았다. 관리자가 지적한 대로 대시보드 Redeploy 버튼·다른 브랜치 머지가 그 창을 연다 — **하지 말라는 것을 하지 말라고 적어야** 안 한다
- **retarget을 새 행으로 세우지 않고 머지 순서 행에 합쳤다.** 둘 다 "#53을 어떻게 내보내는가" 한 사안이고, 한 결정 = 한 줄이 이 파일의 규칙이다. 근거가 **A-3 격리 논리와 같은 모양**(되돌리는 단위를 쪼갠다)이라는 점만 적어뒀다
- **SHA를 지우지 않고 "스냅샷"으로 표시했다.** 지우면 *"그때 무엇을 보고 판정했나"*가 사라져 재현이 안 된다. 낡는 것은 SHA 자체가 아니라 **그것을 현행으로 읽는 것**이라, `git rev-parse origin/main`으로 현행을 확인하라는 문장을 함께 붙였다. 공통규약 §7의 "검증 프로필 테이블 17개" 예외와 같은 취급 — **일부러 고정한 값이면 왜 고정했는지를 적는다**
- 우선순위 문서 본문의 `85209f2 → 0970fd0`(PR #64 머지 사실)은 **안 건드렸다.** 그건 과거에 일어난 일이라 낡지 않는다

**안 한 것과 이유**

- **문서총괄 D2(스택 PR 충돌)를 어디에도 반영하지 않았다** — 도구가 base를 잘못 잡아 생긴 것이고 실제 머지에서 충돌이 없었다. **틀린 관측을 문서에 옮기면 그때부터 그게 사실이 된다**
- `CLAUDE.md` 게이트 표 — 관리자 소관
- **다른 문서의 SHA는 안 뒤졌다.** 내 소관(`검토문서/**`) 2곳만 고쳤다. `docs/**`·`verification-squad/**` 쪽은 각 부서 것이다

**주고받은 메시지**

- ← 관리자: Vercel 공식 문서로 순서 확정, 런북·`DECISIONS.md` 반영 지시 + 머지 경로 결판·SHA 정리 통보
- → 관리자: 반영 완료 보고

**다음**

- 켜는 날 게이트 4건 중 **1·2·4가 닫혔다**(retarget · `SOLAPI_TEMPLATE_WELCOME` · 순서). 남은 것은 **3번(prod `site_settings` INSERT)** — 켜는 날 승인 후 실행이라 예정대로다
- A-2 착수 블로커는 그대로: **오프라인·구글폼 시절 참여자 예외**(단무지님만 아는 사실)

**관리자 판정**

- (관리자가 채움)

---

## 2026-08-22 (2차) — 관리자 결정 5건 기록 + **내 문서의 전제가 깨진 것을 정정했다**

**한 일**

- `DECISIONS.md`에 **행 4개 추가**(관리자 결정) — 토론모임 `(next)` 전용 노출(🔨) · `meeting-remind` 유형 무관 유지(✅/🔨) · 머지 순서 #50→#49→#53→#66(🔨) · prod INSERT는 켜는 날 승인 후(✅)
- `DECISIONS.md` **내 행 2개 정정** — 자격 행에 "3곳"의 정확한 뜻(살아 있는 경로 전부, 레거시 토스 웹훅은 제외)과 prod NULL 실측 결과를 반영, 플래그 행에 교차 확인 근거 추가
- `2026-08-22-토론자격-판정규칙.md` **§2-2 표 2행 + §2-2-b 신설** — 플래그로 안 덮이는 두 경로
- `2026-08-21-미적용-우선순위.md` A-3에 **정정 행** — 틀린 문장은 지우지 않고 왜 틀렸는지 붙였다
- 런북 §1-1 — `SOLAPI_TEMPLATE_WELCOME` 행을 **필수 6번째로** 승격 (개발·운영이 §4에서 권고하며 "제품기획 소관이라 안 고쳤다"고 넘긴 것)

**깨진 전제 — 내가 틀린 것**

내 판정규칙 초판과 어제 우선순위 문서 둘 다 *"토론모임은 `next_ui` 뒤에 있다"*를 전제했다. **틀렸다.** `HomeContent.tsx:64-71`(회원 홈)과 `policy/meetings/page.tsx:39-44`(비로그인 공개 목록)가 `meeting_type`을 안 거른다 — **`next_ui`가 꺼져 있어도 토론모임이 뜬다.**

**왜 놓쳤나 — 기록해 둘 값어치가 있다.** 나는 `(next)` 쪽 코드를 읽고 "토론은 여기 있다"를 확인한 뒤, **구 화면이 토론을 어떻게 취급하는지는 안 봤다.** 새로 만든 쪽만 보고 기존 쪽을 안 본 것이다. 지금까지 안 터진 이유는 설계가 아니라 **prod 토론모임이 전 기간 0건**이라는 사실 하나뿐이었다.

> 이번에 개발·운영이 잡았지만, 다음에 같은 모양을 피하려면 규칙이 하나 필요하다 — **"플래그 뒤에 넣었다"고 쓰기 전에 그 대상이 등장하는 경로를 전수로 센다.** 새 경로가 아니라 **기존 경로가 새 데이터를 어떻게 다루는지**가 구멍이 나는 자리다.

**왜 그렇게 기록했는가**

- **틀린 문장을 지우지 않았다.** 우선순위 문서 A-3의 *"`next_ui`만 켜져 있으면"*은 남겨두고 정정 행을 위에 붙였다 — 이 파일의 규칙(뒤집힌 것은 취소선으로 남긴다)과 같은 이유다. **지우면 다음 세션이 "왜 두 경로를 따로 막았지?"에 답을 못 찾는다**
- **관리자 결정 1을 별도 행으로 세웠다.** 내 플래그 행에 흡수시킬 수도 있었지만, 이건 **플래그 값과 무관한 규칙**이라 성격이 다르다 — 플래그 행 안에 넣으면 "플래그를 켜면 구 홈에도 뜨나?"로 읽힌다
- **`meeting-remind` 행의 상태를 둘로 갈랐다** — 발송 자체는 코드가 이미 그렇게 동작하므로 `✅ 적용(현행 동작 유지)`, 문안 점검은 아직 없으므로 `🔨`. "바꿀 것이 없다는 결정"도 결정이라 기록해 둔다. **적어두지 않으면 다음에 누가 "토론에 리마인드가 나가는데 의도인가?"로 다시 올린다**
- **교차 확인을 기록에 남겼다**(플래그 OFF 시 상세 유지). 두 부서가 서로 모르는 채 **같은 파일의 같은 선례**(D-7 `MeetingDetailContent.tsx:138-139`)에 도달했다 — 한쪽만 말했으면 "그럴듯한 판단"이지만, 둘이 독립적으로 같은 데 닿으면 **이 저장소에 이미 있던 규칙**이라는 뜻이다

**안 한 것과 이유**

- 🔴 **런북 §1의 env↔머지 순서는 바꾸지 않았다 — 관리자에게 올린다.** 지금 `CLAUDE.md` 켜는 날 게이트 #4는 *"env 먼저 → 머지"*라고 하는데 **런북 §1 1행은 "머지 → env → 재배포"**다. **두 문서가 반대를 말하고 있고, 켜는 날 보는 것은 런북이다.** 개발·운영이 "env 먼저"를 권고했지만 그 근거(*"Vercel은 env만 바꿔도 재배포되지 않는다"*)를 **🟨 추정으로 표시**했다(콘솔·CLI 미확인). 배포 절차는 되돌리기 어려운 축이라, **추정 위에서 순서를 뒤집지 않았다.** 확인은 사람이 Vercel에서 1분이면 한다
- **레거시 토스 웹훅 처리 방침** — 관리자 미결(개발 §6-6). 내 자격 행에는 "죽은 경로라 게이트를 안 넣는다"는 사실만 적고 삭제 여부는 안 정했다
- **오프라인·구글폼 시절 참여자 예외**(개발 §6-1, 착수 블로커) — **단무지님만 아는 사실**이라 내가 정하지 않는다. 다만 미리 적어둘 것: **예외가 필요하면 `profiles`에 수동 자격 칸이 생기고 A-2 범위가 커진다.** `is_free` 선례대로 UI 없이 SQL 지정이 어울린다
- **문안 점검**(`meeting-remind`가 정기 전용 표현을 쓰는지) — 디자인 소관

**주고받은 메시지**

- ← 관리자: 결정 2건 + 추가 3건을 `DECISIONS.md`에 남기라는 지시, §2-2 표·A-3 문장 정정 요청
- → 관리자: 반영 완료 + **런북 ✕ `CLAUDE.md` 순서 모순 1건 올림**

**다음**

- 🔴 **관리자 확인 필요: 런북 §1 순서.** `CLAUDE.md`와 반대로 적혀 있다. Vercel env 동작 확인(사람 1분) 후 한쪽으로 통일
- A-2·A-3 머지되면 `DECISIONS.md` 세 행(자격·플래그·`(next)` 전용 노출)을 `✅ 적용`으로

**관리자 판정**

- (관리자가 채움)

---

## 2026-08-22 — 토론모임 참여 자격의 "세는 법"과 격리 플래그 운영 규칙을 닫았다

**한 일**

- `검토문서/2026-08-22-토론자격-판정규칙.md` 신규 — 어제 내 문서가 *"왜 막혔나"*로 적어둔 미결(자격 판정의 분모)을 **전수 표로 확정**. `registrations.status` 6종 전부 + 경계 5종(선결제·삭제 모임·`meeting_type` null·필터 방향·윈도우) + 역할 우회 3종(admin·editor / `is_staff` / `is_free`)
- `검토문서/DECISIONS.md`에 **행 3개 추가** — 자격 판정 규칙(🔨 미적용) · 플래그 운영 규칙(🔨 미적용) · 북극성 지표 보류(⏸, 되살릴 조건 명시)
- `검토문서/2026-08-18-켜는날-런북.md` **3곳 갱신** — §0 플래그 INSERT 대상 2→3개 · §1-3 "플래그 2개 켜기"→3개 · §3 롤백을 **범위 좁은 것부터**(토론만 끄기 → `next_ui` 끄기)로 재배열 + 돈 취급 규칙
- `검토문서/2026-08-21-미적용-우선순위.md` A-2·A-3에 닫힘 표시

**왜 그렇게 했는가**

- **새 기준을 만들지 않았다.** 기존에 같은 것을 세는 정의가 이미 셋 있었다 — "참여 = 결제완료 + 모임일 경과"(프로필), `PARTICIPATED_STATUSES`(`registration-status.ts:18`), `isAskEligibleMeeting()`(`asks-pure.ts:17`). 특히 세 번째는 **물어보기 분모가 이미 "정기모임 실참여"를 세고 있는 것**이라, 자격 규칙은 거기서 **60일 윈도우만 걷어낸 것**이 됐다. 네 번째 정의를 만들었으면 분모가 또 갈라졌을 것이다
- **`meeting_type` null 폴백을 자격에 넣지 않았다.** 지시에 "환불은 정기로 폴백하는데 자격도 같은 폴백인가"가 있었는데, 확인해보니 **그 데이터 자체가 없다** — `migration-meeting-type.sql:28`이 `NOT NULL DEFAULT 'regular'` + CHECK고 TS 타입도 non-nullable이다. `refund.ts:120`의 `string | null | undefined`는 **함수 시그니처의 방어**지 데이터 상태가 아니다. 없는 상태에 분기를 만들면 다음 사람이 "null인 경우가 있구나"로 읽는다
- **필터를 `= 'regular'` 화이트리스트로** 정했다. `<> 'discussion'`은 지금은 같은 결과지만 **2단계 번개가 세 번째 유형으로 들어오는 순간 번개를 정기로 세어버린다**
- **`meetings.status='active'`를 명시적으로 넣었다.** 모임 삭제 시 registration이 전부 `cancelled`로 바뀌므로 이중이지만(`delete/route.ts:95-144`), **일괄 환불이 부분 실패하면 모임은 `deleting`에 멈추고 일부 registration이 `confirmed`로 남는다**(`:152-158`). status 조건만으로는 그 잔재를 못 거른다
- **`is_free`만 우회에서 뺐다.** admin·editor·`is_staff`는 우회시켰다 — 셋은 `isCurator()`와 정확히 같은 집합이라 규칙이 늘지 않고, **발제를 쓰는 사람이 그 모임에 신청 못 하면 앞뒤가 안 맞는다.** 반면 `is_free`는 실제로 정기모임을 신청하므로(pending_transfer) 일반 규칙으로 자격을 얻는다 — 우회를 넣으면 **정산용 플래그가 권한 플래그로 번진다**
- **RPC에는 자격을 넣지 말라고 적었다.** 스텝 할인 슬롯이 FOR UPDATE 락 안에서 재검증되는 이유는 두 사람이 마지막 슬롯을 동시에 집을 수 있어서인데, **자격은 과거 참여로 결정되는 값이라 그런 경합이 없다.** 넣으면 `pricing.ts ↔ SQL 동기 필수` 같은 부담이 하나 더 생긴다
- **플래그 기본값은 선례(`library_enabled`·`next_ui`)를 그대로 따랐다** — 행 없으면 OFF. 세 번째 플래그만 기본값이 다르면 켜는 날 매번 확인해야 한다. 대신 **행은 값과 무관하게 켜는 날 만들라**고 정했다: 행이 없으면 끌 때 SQL을 새로 써야 하는데 **사고 시간에 SQL을 쓰게 두면 안 된다**
- **"끄면 무슨 일이 나나"의 핵심은 돈이다.** OFF는 새로 들어오는 것만 막고, **이미 결제한 신청의 조회·취소·환불은 플래그와 무관하게 살려뒀다.** 끄면서 취소까지 막으면 환불 경로 자체가 사라지고, 결제한 모임이 화면에서 사라지면 회원은 그걸 돈 사고로 읽는다. 자동 환불도 걸지 않았다 — 화면을 내린 것이지 모임을 취소한 게 아니고, **돈을 자동으로 움직이면 되돌릴 수 없다**
- **번개(최근 3개월) 규칙을 복사하지 않았다.** 번개는 "마지막 참여일을 반드시 적는다"인데 토론은 평생이라 **안 잃는 자격**이다. 날짜를 보여주면 없는 걱정("언제 만료되나?")을 만든다
- **런북을 실제로 고쳤다.** "어디에 들어가야 하는가"만 적고 안 옮기는 것이 이 부서가 이미 한 번 지적당한 실패 모드다(2026-08-21 8일 규칙)

**안 한 것과 이유**

- **미자격 안내 문구·버튼 상태 이름** — *무엇을 말할지*(왜 잠겼나 / 어떻게 풀리나 / 어디로 가나) 셋만 정하고 *어떻게 말할지*는 디자인에 남겼다 (부서정의 §문구는 누구 소관인가)
- **구현·SQL** — 개발·운영 소관. `jidokhae-web/**`은 손대지 않았다
- **prod `meeting_type` NULL 행 실측** — 이 부서에 DB 조회 도구가 없다. 한 줄 쿼리로 끝나므로 개발·운영에 넘겼다(판정규칙 「확인 못 한 것」). 0이 아니면 §1-3 판정을 다시 봐야 한다
- **2026-08-14 원 결정 행의 상태 변경** — 자격·플래그 둘 다 여전히 코드 0건이라 `🔨 미적용` 그대로 뒀다. 진행 단계를 상태로 올리지 않는다

**주고받은 메시지**

- → 관리자(main): 산출물 4개 경로 + 개발이 바로 옮겨 쓸 확정문(§1-7) 위치 알림

**다음**

- A-2·A-3 PR이 머지되면 `DECISIONS.md` 두 행을 `✅ 적용`으로. **켜는 날 직후 상태 칸 전수 재확인**과 같은 타이밍
- 북극성 지표는 **첫 토론모임 1회차 종료 직후** 다시 연다 (담당 데이터)

**관리자 판정** — 🟢 통과

- **연 파일**
  - `검토문서/2026-08-22-토론자격-판정규칙.md` — 전문. §1-2 status 6종 · §1-3 경계 5종 · §1-4 우회 3종 · §1-7 확정문이 실제로 있는지 확인
  - `git diff 검토문서/DECISIONS.md` — 행 3개가 **실제로 들어갔는지** 확인. 상태 칸도 규칙대로(🔨 2건 · ⏸ 1건, 진행 단계를 상태로 올리지 않음)
  - `git diff 검토문서/2026-08-18-켜는날-런북.md` — **"어디에 들어가야 한다"가 아니라 실제로 세 곳에 반영됐는지** 확인. §0 INSERT 행 신설 · §1-3 "2개"→3개 · §3 롤백 표 재배열 + 돈 취급 문단. 12줄 추가 2줄 삭제
  - `jidokhae-web/src/lib/registration-status.ts:18` — `PARTICIPATED_STATUSES = ['confirmed','pending_transfer']` 실재 확인
  - `jidokhae-web/supabase/migration-meeting-type.sql:28,35` — `NOT NULL DEFAULT 'regular'` + CHECK 실재 확인

- **근거**
  - **인용한 코드가 전부 실재한다.** 문서가 든 파일:줄 다섯 개를 직접 열어 대조했고 어긋난 것이 없었다. 이 부서가 전에 지적받은 실패 모드(정해놓고 안 옮김)도 이번엔 없다 — **런북 세 곳이 실제로 바뀌어 있다.**
  - **새 정의를 만들지 않은 것이 이 산출물의 핵심이다.** 같은 것을 세는 정의가 이미 셋 있었고(`PARTICIPATED_STATUSES` · "결제완료+모임일 경과" · `isAskEligibleMeeting`), 네 번째를 만들었으면 분모가 또 갈라졌다. 자격을 **"물어보기 분모에서 60일 윈도우만 걷어낸 것"**으로 정의한 것이 정확하다.
  - **§1-3의 `meetings.status='active'` 근거가 특히 좋다.** 삭제 시 registration이 `cancelled`로 바뀌므로 이중이지만, **일괄 환불 부분 실패로 모임이 `deleting`에 멈추면 `confirmed` 잔재가 남는다**(`delete/route.ts:152-158`)는 것까지 짚었다. 이건 "혹시 몰라서"가 아니라 **실제 코드 경로에 근거한** 방어다.
  - **§2-2가 돈을 정확히 다뤘다.** 「OFF는 새로 들어오는 것만 막는다」 한 줄로 원칙을 세우고, *끈 상태에서 취소가 막히면 환불 경로 자체가 사라진다* 는 결과까지 도달했다. 관리자가 올려 받으려던 축(돈·회원 노출)을 **규칙 안에서 닫아 올릴 것을 없앤 것**이 맞는 판단이다.
  - **`is_free`만 우회에서 뺀 판단에 동의한다.** 셋(admin·editor·`is_staff`)은 `isCurator()`와 같은 집합이라 규칙이 늘지 않고, `is_free`는 일반 규칙으로 자격을 얻으므로 우회가 불필요하다. **정산용 플래그를 권한 플래그로 번지게 하지 않는다**는 경계가 2026-07-05 결정(UI 토글 폐기 사유)과 같은 선이다.

- **조건부·반려 아님. 다만 관리자가 이어받은 것 둘**
  1. **prod `meeting_type is null` 실측** — 문서가 「확인 못 한 것」에 정직하게 올렸다. 도구가 없어 못 한 것을 안 한 척하지 않은 것이 옳다. `engineering-a23`에 SELECT 한 줄로 넘겼다(2026-08-22, 관리자 메시지). 0이 아니면 §1-3 재검토
  2. **§1-1이 짚은 함정** — 자격 판정이 `isAskEligibleMeeting`과 모양이 거의 같은데 윈도우만 없다. *"나란히 두면 다음 사람이 재사용하다 윈도우를 딸려 보낸다."* 구현 시 처리 방법(주석 못 박기 / 재사용 구조 분리)을 개발·운영이 계획에 적도록 함께 넘겼다

- **동시 수정 경고 1건** — 이 부서가 `DECISIONS.md` 경과일까지 갱신하는 동안 `librarian-gate`가 같은 파일을 같은 목적으로 돌고 있었다. 관리자가 뒤늦게 알려 충돌은 없었다. **다음부터 같은 파일을 만질 부서를 동시에 부르지 않는다** — 이건 부서 잘못이 아니라 관리자 소환 설계의 실수다

## 2026-08-21 (2차) — 관리자가 쓴 `DECISIONS.md` 네 줄을 인수 (관리자규칙 §7 첫 적용)

**배경 — 이 소환의 이유**

오늘 오후 관리자가 내 소관 파일(`검토문서/DECISIONS.md`)에 직접 세 줄을 썼다. 단무지님이 "갱신은 부서에서 해야지"라고 지적했고 [관리자규칙 §7](../관리자규칙.md)이 그래서 생겼다. **이 항목은 §7이 실제로 지켜지는지 다음에 확인할 근거로 남긴다.**

관리자가 손댄 것은 `DECISIONS.md` 세 줄이다(8일 규칙 · 호칭 flip · 토론 환불 보류). 나머지 2026-08-21 행 2건(`🚫` 멤버십·모임 5종)은 오늘 아침 내가 쓴 것이다. 셋 다 **커밋 `9e68ff9` 하나에 내 작업과 섞여 있어** 커밋 단위로는 분리되지 않는다 — 그래서 내용으로 갈랐다.

**한 일 — 네 줄을 검토하고 전부 고쳤다. 네 줄 다 사실이 틀렸거나 기준이 어긋났다**

| # | 관리자가 쓴 것 | 내 판정 | 어떻게 고쳤나 |
|---|---|---|---|
| 1 | 8일 규칙 → `✅ 적용 (런북 §0 반영 필요)` | 🔴 **틀림.** 런북에 없다 | 런북 §0에 **실제로 넣고** `✅ 적용 — 런북 §0`으로 |
| 2 | 호칭 flip → `🔨 커밋됨 — 푸시·머지 대기` | 🔴 **표에 없는 말 + 사실도 틀림.** 이미 푸시됨 | 상태 칸 규칙을 세우고 `🔨 미적용`으로 흡수 |
| 3 | 토론 환불 → `⏸ 보류`, "머지되면 이 행을 지운다" | 🔴 **이미 머지돼 있었다 + 지우면 안 된다** | 취소선으로 닫고, **안 지운 이유를 행 안에 적었다** |
| 4 | (그 밖의 손자국) | 🔴 **빠뜨린 것이 있다** — 같은 결정에 행이 둘 | 2026-08-07 호칭 행의 중복 사실 제거 |

**1. 8일 규칙 — `✅ 적용`이 아니다**

`검토문서/2026-08-18-켜는날-런북.md`를 열어 확인했다. §0 사전 조건 표에는 *"다음 토론모임 + 책 연결 + 발제문 선등록"* 한 줄뿐이고 **날짜 규칙이 없다**(`grep "8일" → 0건`). 상태 표의 `✅ 적용` 정의는 *"코드/운영에 반영됐고 **누군가 실제로 겪고 있다**"*인데, 켜는 날 런북을 보는 사람은 이 규칙을 만나지 못한다.

게다가 **`✅ 적용 (반영 필요)`는 그 자체로 모순**이다 — 괄호가 "아직 할 일이 남았다"고 말하는 순간 ✅가 취소된다.

고친 방식: `검토문서/**`는 내 소관이므로 **런북 §0에 행을 직접 넣었다.** 그러고 나니 `✅ 적용`이 참이 됐다. `🔨 미적용`으로 낮춰 적고 넘기는 것보다 이쪽이 낫다 — 한 줄이면 되는 일을 상태로만 표시해두면 그게 다음 주까지 산다.

**2. 호칭 flip — 새 상태를 만들지 않고 `🔨 미적용`으로 흡수했다**

`🌑 다크 배포`를 아침에 신설한 게 나라서, 새 칸을 만드는 기준을 내가 대야 한다. **기준은 "진행이 더 갔는가"가 아니라 "되돌리는 방법이 다른가"다.** `🌑`이 칸을 얻은 건 되돌리기가 flip 하나이기 때문이지 배포 단계라서가 아니다.

`커밋됨`은 그 기준을 통과하지 못한다 — 되돌릴 게 없고(미적용과 같다), 회원이 겪는 것도 없다(미적용과 같다). 통과시키면 `푸시됨`·`리뷰 중`·`머지 대기`가 줄줄이 따라 들어와 결정 기록이 git 대시보드가 된다.

**결정적인 근거는 그 표기가 쓰인 시점에 이미 틀렸다는 것이다.**

```
origin/fix/remind-payment-status@{2026-08-21 19:46:05 +0900}: update by push
origin/feat/book-ask-cron@{2026-08-21 19:46:07 +0900}: update by push
origin/feat/welcome-alimtalk@{2026-08-21 19:46:10 +0900}: update by push
커밋 9e68ff9 ("푸시·머지 대기"라고 적은 커밋)      19:46:30 +0900
```

**25초 먼저 푸시돼 있었다.** GitHub이 정확하게 세는 것을 문서에 손으로 베끼면 지는 싸움이다. 그래서 상태 표 아래에 절을 하나 신설했다 — *「상태 칸은 다섯 개가 전부다 — 진행 단계로 새 칸을 만들지 않는다」*. 진행 정도는 **결정 칸 본문에 PR 번호·SHA 같은 확인 가능한 포인터로** 적는다.

**3. 행을 지우지 않았다 — 이 파일의 원칙과 정면으로 어긋난다**

관리자는 *"그 작업이 머지되면 이 행을 지운다"*고 적었다. 이 파일의 규칙은 **"뒤집힌 결정은 지우지 말고 취소선"**이고, 부서 정의 §4도 같다. 지우면 다음 세션은 배선이 필요했던 적이 있는지도, **세 파일이 왜 한동안 접근 금지였는지도** 알 수 없다.

지우자는 논리("남아 있으면 다음 세션이 아직 안 됐다로 읽는다")는 **진단은 맞고 처방이 틀렸다.** 잘못 읽히는 원인은 행이 존재하는 게 아니라 **상태 칸이 낡는 것**이다. 답은 삭제가 아니라 갱신이다.

**그리고 이 행은 쓰인 시점에 이미 낡아 있었다** — PR #64가 **16:22 KST에 머지**됐고 관리자는 **19:46에** "다른 세션이 작업 중, 접근 금지"라고 적었다. **3시간 24분 늦은 정보다.**

**4. 빠뜨린 손자국 — 한 결정에 행이 둘, 사실이 둘**

관리자는 호칭에 대해 **새 행(2026-08-21)을 추가하면서 원래 행(2026-08-07)을 그대로 뒀다.** 그 결과:

- 2026-08-21 행: *"7곳 커밋 완료"*
- 2026-08-07 행: *"`real_name || nickname`이 **5곳 그대로**"*

**같은 사안에 두 문장이 다른 말을 한다.** 이건 이 파일이 존재하는 이유가 된 바로 그 사고(한 사안에 세 문서가 다른 말)를 **파일 안에서 재현한 것**이다. 원래 행에서 실측 숫자를 빼고 *"진행 상태는 2026-08-21 행 하나에서만 관리한다"*로 바꿨다.

**곁가지로 잡은 것 — `main`이 갈라져 있다**

`git fetch` 후 확인: **`origin/main`이 우리 브랜치보다 6커밋 앞서 있고**(PR #62·#63·#64), 그중 **#63·#64가 `DECISIONS.md`를 각각 한 줄씩 고쳤다.** 즉 이 파일은 지금 두 곳에서 따로 편집되고 있다. 갈라진 두 줄(D-7·토론 환불)은 **`origin/main`을 직접 읽어 사실을 확인한 뒤 우리 사본에 반영**했으므로, 머지할 때 우리 쪽을 채택하면 손실이 없다. 그 사실을 파일 머리에 경고로 적었다. **머지 자체는 손대지 않았다** — 브랜치 작업은 내 소관이 아니다.

**왜 그렇게 했는가**

- **관리자 말을 하나도 안 믿었다.** SHA 3개·7곳·푸시 여부·머지 여부를 `git show`·`git grep`·`git rev-list`·`git reflog`·`gh pr view`로 전부 직접 확인했다. **네 줄 중 네 줄이 틀렸다** — 지시가 "믿지 마라"였던 게 옳았다
- **기준이 두 개가 되지 않게 하는 게 이번 일의 핵심**이라 봤다. 그래서 개별 줄을 고치는 데서 멈추지 않고 **새 상태를 만드는 조건 자체를 문서에 박았다.** 다음에 누가(관리자든 부서든) 진행 단계를 상태로 올리려 하면 그 절이 막는다
- **관리자를 비난하는 방식으로 적지 않았다.** 행 안에는 *무엇이 사실이고 왜 그렇게 정했는지*만 남겼다. 누가 썼는지는 이 로그에만 있다 — `DECISIONS.md`는 결정을 찾아보는 파일이지 잘잘못을 따지는 파일이 아니다

**새로 찾은 것**

- 🔴 **아침에 A 묶음 1순위로 올린 A-1(토론 환불 미연결)이 같은 날 닫혔다.** PR #64가 `calculateRefundByType`(`refund.ts:113`) 단일 진입점을 만들어 실환불·권장액·취소 모달·규칙 문구 4곳에 꽂았다. **아침에 지목한 3곳 전부 + 환불정책 페이지.** 미적용 총계 **19 → 18건**, A 묶음 **3 → 2건**
- **`DECISIONS.md`가 `main`과 갈라져 편집되고 있다**(위)
- 이 결함의 모양이 *"순수 함수 + 테스트는 있고 호출부가 없다"*였다는 점을 우선순위 문서에 교훈으로 남겼다 — **테스트 6종이 통과해서 오히려 됐다고 믿기 쉬웠다**

**안 한 것과 이유**

- **코드 한 줄도 안 고쳤다.** 브랜치 체크아웃·푸시도 안 했다(`git fetch`와 읽기 명령만). `git status`는 시작 때와 같다 — 관리자의 미커밋 `관리자규칙.md` 하나뿐이고 **건드리지 않았다**
- **`jidokhae-web/VOICE.md`·`DESIGN_TOKENS.md` 안 열었다** — 디자인 소관, 동시 소환 중
- **결정 내용 자체를 뒤집지 않았다.** 8일 규칙도, 호칭 flip도, 환불 7/3도 그대로다. 바꾼 것은 **상태 표기와 기록 방식**뿐
- **`CLAUDE.md` 안 고쳤다** — 관리자 소관. 어긋난 것은 아래 「넘김」에
- **`main` 머지·충돌 해소 안 했다** — 관리자·개발운영 소관. 사실만 적어뒀다

**주고받은 메시지**

- **없음.** 공통규약 §2의 직접 소통 쌍에 제품기획↔개발운영이 없어 이번에도 관리자를 거친다

**관리자에게 넘김 (`CLAUDE.md` — 내가 못 고치는 것)**

🔴 **`CLAUDE.md:122` — 「플래그 켜기 게이트」 5건 중 4번이 이미 해소됐다.**

```
122: 4. 토론모임 환불이 정기 규칙으로 돌아간다(`calculateDiscussionRefund` 호출부 0)
        — **다른 세션 담당, 접근 금지**
```

PR #64가 `main`에 머지되면서(16:22 KST) **호출부 0이 아니고, 접근 금지도 풀렸다.** 게이트는 **5건 → 4건**이다. 이 문장은 매 세션 전 부서에 자동 주입되므로, 두면 **다음 부서가 세 파일을 계속 못 건드리고** 켜기 전 할 일을 하나 더 있는 것으로 센다. `CLAUDE.md`는 관리자 소관이라 **고치지 않고 넘긴다.**

> **내가 틀렸던 것 하나 — 정정.** 처음엔 *"`CLAUDE.md`에 BOOK_ASK가 아직 재심사 중이라 적혀 있다"*고 적었다. **틀렸다.** 내 세션에 주입된 사본이 세션 시작 시점 것이라 낡았을 뿐, 디스크의 `CLAUDE.md:117`은 이미 *"BOOK_ASK도 ✅ APPROVED(8/19)"*로 고쳐져 있다(관리자가 `9e68ff9`에서 처리). **주입된 컨텍스트를 파일 상태로 믿으면 안 된다** — 관리자 말을 안 믿으려다 주입된 문서를 믿을 뻔했다. 파일을 열어 확인하고 지웠다.

**다음**

- **켜는 날 직후 상태 칸 3차 재확인** — 플래그를 켜면 `🌑` 행이 한꺼번에 `✅`가 되고, PR #50·#49·#53이 머지되면 호칭 행 두 개가 함께 `✅`가 된다
- **A-2(참여 자격) · A-3(격리 플래그)** — 권고는 **A-3 먼저**(끄는 수단이 없으면 A-2가 틀렸을 때 `next_ui` 전체를 꺼야 한다). 둘 다 못 넣으면 A-3만이라도
- `main` 갈라짐은 **머지 전에 관리자가 처리**해야 한다

**관리자 판정** — 🟢 통과 (2026-08-21). **네 건 전부 제품기획이 맞고 관리자가 틀렸다**

- **연 파일 / 돌린 명령**
  - `gh pr view 64` → **MERGED 2026-08-21T07:22:55Z = 16:22 KST.** 관리자가 `⏸ 접근 금지`를 쓴 19:46보다 **3시간 24분 앞선다**
  - `git grep calculateRefundByType origin/main -- jidokhae-web/src` → `cancel.ts` · `MeetingActionButton.tsx` · `mark-refunded/route.ts` · `AdminMeetingSection.tsx` **전부 배선됨.** 호출부 0이 아니다
  - `git reflog show origin/fix/remind-payment-status --date=iso` → **`19:46:05 update by push`.** 커밋 `9e68ff9`는 19:46:30. **25초 먼저 푸시돼 있었다**
  - `git rev-list --left-right --count origin/main...HEAD` → **`8 11`.** main이 8커밋 앞서 있다

- **네 줄 다 틀린 이유가 서로 다르다 — 그래서 더 값어치가 있다**

  | # | 관리자가 쓴 것 | 무엇이 틀렸나 | 성질 |
  |---|---|---|---|
  | 1 | `✅ 적용 (런북 §0 반영 필요)` | 런북에 없었다. 그리고 **괄호가 ✅를 취소한다** | 자기모순 |
  | 2 | `🔨 커밋됨 — 푸시·머지 대기` | **쓰는 순간 이미 틀렸다**(25초 전 푸시됨) + 상태 표에 없는 말 | 사실 오류 + 기준 침범 |
  | 3 | "머지되면 이 행을 지운다" | 이 파일 원칙(뒤집힌 결정도 취소선)과 정면 충돌 + **이미 머지돼 있었다** | 원칙 위반 |
  | 4 | 새 행만 추가하고 원래 행 방치 | 한 사안에 두 문장이 다른 말 | **이 파일을 만들게 한 사고의 재현** |

- **특히 평가할 점 — 개별 줄이 아니라 기준을 고쳤다**
  1. **`🌑`을 만든 자기 기준을 근거로 새 칸을 거절했다.** *"기준은 진행이 더 갔는가가 아니라 되돌리는 방법이 다른가"* — `커밋됨`은 되돌릴 것도 겪는 사람도 미적용과 같으므로 칸을 안 준다. 통과시키면 `푸시됨`·`리뷰 중`이 줄줄이 들어와 결정 기록이 git 대시보드가 된다는 지적이 정확하다. **아침에 자기가 만든 체계를 오후에 관리자로부터 지켰다**
  2. **3번에서 "진단은 맞고 처방이 틀렸다"로 갈랐다.** 잘못 읽히는 원인은 행의 존재가 아니라 상태 칸이 낡는 것 — 답은 삭제가 아니라 갱신. 내 진단을 버리지 않고 처방만 교체했다
  3. **상태로 표시하고 넘기는 대신 런북을 직접 고쳤다.** *"한 줄이면 되는 일을 상태로만 표시해두면 그게 다음 주까지 산다."* 이 프로젝트가 정확히 그것 때문에 두 번 물렸다
  4. **자기 오류를 스스로 잡고 정정했다.** "`CLAUDE.md`에 BOOK_ASK가 재심사 중"이라 썼다가 **주입된 사본이 낡은 것**임을 확인하고 지웠다. *"관리자 말을 안 믿으려다 주입된 문서를 믿을 뻔했다"* — 이 자각이 이 로그에서 가장 값어치 있는 한 줄이다
  5. **`DECISIONS.md`에 누가 틀렸는지 안 적었다.** 행에는 사실만 남기고 경위는 이 로그에만 뒀다. *"결정을 찾아보는 파일이지 잘잘못을 따지는 파일이 아니다."* 옳다

- **범위를 넘지 않았다.** 코드·브랜치·푸시 없음, `VOICE.md`·`CLAUDE.md` 미접촉, **결정 내용은 하나도 안 뒤집음**(상태 표기와 기록 방식만)

- **관리자가 받아 처리한 것**
  - `CLAUDE.md:122` 게이트 4번 → **닫았다.** 게이트 5건 → 4건. 세 파일 접근 금지 해제도 명시
  - `main` 8커밋 갈라짐 → 관리자가 머지한다(브랜치 작업은 부서 소관 아님)

- **이 판정이 §7의 첫 검증 결과다.** 규칙을 만든 당일에 규칙이 지켜지는지 시험했고, **부서가 관리자를 네 번 정정했다.** 관리자가 그대로 뒀으면 넷 다 그대로 살아남았을 것이다. §7은 형식이 아니라 실제 방어선이다.

---

## 2026-08-21 — `DECISIONS.md` 상태 칸 전수 재확인 + 폐기 기록 2건 신설

**한 일**

- **상태 표기 하나 신설.** `🌑 다크 배포` = *코드가 prod에 나가 있으나 플래그 OFF로 회원 노출 0.* 파일 상단 상태 표에 추가하고, 왜 `배포 대기`가 틀린 말인지(되돌리는 방법이 다르다 — 미적용은 만들면 되고 다크 배포는 flip 하나)를 함께 적었다. 한 결정이 두 상태에 걸치면 둘 다 적는 규칙도 명시
- **`검토문서/DECISIONS.md` 상태 칸 전수 재확인 후 수정.** 문서총괄 1차 §4가 지목한 10행 + 그 외 눈에 띄는 행까지 총 **24행**. 양방향 오류 둘 다 정정:
  - 「배포 대기」 → 실제로는 이미 prod: 토스 스킨 · 탭 5개 · 토론 진입점 · 발제 관리 권한 · 공지 A+C+D · 책 표지 배치 · 발제문 5개 · 발제 참여 부담 · 카운터 명사 · 유형 형태 구분 · 홈 "할 일" 워딩
  - 「미적용」 → 실제로는 적용: 토론 상세 순서(PR #57) · 완독 재촉 금지(금지 규율이라 grep 0건 = 지켜짐) · 운영자 필수 입력(부분) · 토론 참여 자격 문서 개정분
  - 반대로 **「구현됨」이 사실이 아닌 것 1건을 새로 찾았다** → A-1(아래)
- **폐기 기록 2건 신설**(문서총괄 §14):
  - `🚫 안 함` — 멤버십·시즌권 사업 설계(보관소 2026-04-17). 되살릴 조건 2개 명시
  - `🚫 안 함` — 모임 5종 체계. 단 **「팝업 살롱」은 예외로 살렸다**
- **`검토문서/2026-08-21-미적용-우선순위.md` 신설.** 남은 미적용 19건을 A 켜기 전 필수(3) / B 켜는 날 함께(4+1) / C 켠 뒤 첫 주(4) / D 2단계(8)로 갈랐다. 각 항목에 경과일·막힌 이유·근거 파일:라인
- `검토문서/README.md` 인덱스 갱신 — 「켜는 날」 절 신설(런북·판정·우선순위 3종), 문서정비 계획 추가, 상태 어휘에 `🌑` 반영
- `DECISIONS.md` "아직 정하지 않은 것"에서 **코어 문서 전면 정비** 항목을 닫았다 — 문서정비 계획(8-18)이 무엇을·언제를 이미 정했다

**왜 그렇게 했는가**

- **보고서를 그대로 믿지 않았다.** 8-18 이후 main에 22커밋(PR #58~#61)이 더 들어왔고, 그중 PR #61이 D-7을 서버에서 강제하는 등 상태를 바꿨다. 전 행을 `git log`·`git grep`·파일 확인으로 재검증했다
- **prod DB를 읽기 조회했다**(MCP, 읽기만). 문서만으로는 "플래그가 실제로 꺼져 있는가"를 확신할 수 없었다. 결과: `site_settings`에 `next_ui`·`library_enabled`·`notice_text` **행 자체가 없고**(= OFF), **토론모임이 전 기간 0건**이다. 이 두 사실이 우선순위의 뼈대가 됐다
- **우선순위를 경과일로 줄 세우지 않았다.** 부서 정의 §6 — 근거는 돈·회원 노출·경과일·막힌 의존이다. 지금은 플래그 flip이 임박해 **같은 항목의 무게가 켜기 전후로 달라진다.** 그래서 켜는 날을 축으로 잡고, 경과일은 묶음 안에서만 썼다
- **`🌑`을 새로 만든 이유** — 기존 4칸으로는 이 프로젝트의 지배적 상태를 못 적는다. `(main)`/`(next)` 두 벌이 공존하는 동안 14행이 이 상태다. "배포 대기"로 적으면 롤백 범위와 남은 작업량을 둘 다 잘못 읽는다

**새로 찾은 것 (보고서에 없던 것)**

- 🔴 **토론모임 환불 7/3일이 "구현됨"으로 적혀 있으나 실제로는 호출부가 0이다.** `calculateDiscussionRefund`(`lib/refund.ts:78`)를 부르는 곳은 테스트 파일뿐이고, 실제 취소 경로 3곳(`cancel.ts:49` · `MeetingActionButton.tsx:189` · `mark-refunded/route.ts:126`)은 `meeting_type` 분기 없이 정기 규칙(3일/2일)을 쓴다 → **토론모임을 열면 D-3~D-7 구간에서 잘못된 금액이 환불된다.** prod에 토론모임이 0건이라 아직 사고는 없다
- **PR #50이 호칭 결정 대상 줄을 손대면서 지나쳤다.** #50 diff에 `displayName = profile.real_name || profile.nickname`이 그대로 있다. 결정(2026-08-07, 14일째)은 5줄이면 끝나고 #50이 이미 그 두 파일을 열고 있다 — **지금 얹으면 5줄, 켠 뒤면 별도 PR + 별도 배포**
- **토론모임 격리 플래그가 없어 롤백 수단이 `next_ui` 전체 끄기뿐이다.** 토론 하나를 끄려고 개편 전체를 되돌리게 된다

**안 한 것과 이유**

- **코드 한 줄도 안 고쳤다.** 부서 정의 §6 — 구현은 개발·운영 소관. A-1은 돈에 닿으므로 더더욱
- **`core/` 손대지 않았다.** read-only. 개정 계획은 이미 `2026-08-18-문서정비-계획.md`에 있어 새로 쓰지 않고 `DECISIONS.md`에서 그것을 가리키게 했다
- **이미 정해진 것을 다시 열지 않았다.** 특히 `LibraryGrid.tsx:163`의 `읽는 중` 라벨은 완독 재촉 금지 3종에 해당하지 않는다고 판단하고, 문구 판단은 디자인 소관이라 결정을 건드리지 않았다 — 우선순위 문서 "확인 못 한 것"에만 남겼다
- **`jidokhae-web/VOICE.md`·`2026-08-21-켜는날-코드준비-판정.md` 열지 않았다** (인덱스에 한 줄 넣기 위해 후자의 머리 6줄만 읽음). 디자인·개발운영이 동시에 쓰는 중
- **옵시디언 보관소는 읽기만 했다** (`320. Area\지독해\` 2건)
- **`(next)` 화면이 실제로 어떻게 보이는지는 검증 안 함** — 플래그 OFF라 prod에서 볼 수 없다. 품질·디자인 소관

**주고받은 메시지**

- **없음.** 개발·운영에게 A-1(환불 미연결)을 직접 알리고 싶었으나, 공통규약 §2의 직접 소통 쌍(제품기획↔디자인 · 개발운영↔품질 · 데이터↔제품기획 · 고객VOC↔제품기획)에 **제품기획↔개발운영이 없다.** 관리자를 거치도록 최종 보고에 올렸다

**다음**

- 관리자 판단이 필요한 4건 (우선순위 문서 「관리자에게 올리는 것」): ① A-1이 의도적 보류인가 ② 호칭 flip을 PR #50에 얹을지 ③ 켜는 날 첫 토론모임 날짜 ④ A 3건을 켜기 전에 넣을 시간이 있는가
- **켜는 날 직후 상태 칸 재확인** — 플래그를 켜면 `🌑` 14행이 한꺼번에 `✅`가 된다. 그 자리에서 갱신하지 않으면 이번에 고친 것이 그날 다시 낡는다

**관리자 판정** — 🟢 통과 (2026-08-21)

- **연 파일 / 돌린 명령**
  - `검토문서/DECISIONS.md` — 상태 표에 `🌑 다크 배포` 신설 확인, 본문 **15행** 적용. `🚫 안 함` 2건(멤버십·모임 5종) 전문 확인
  - `검토문서/2026-08-21-미적용-우선순위.md` — A(3)/B(4+1)/C(4)/D(8) 분류와 「관리자에게 올리는 것」 확인
  - `grep -rn calculateDiscussionRefund src` → **정의 1 + 테스트 8. 프로덕션 호출부 0**
  - `sed -n 45,55p src/lib/cancel.ts` → `calculateRefund(meetingDate, ...)` 무조건 호출. `grep meeting_type cancel.ts refund.ts MeetingActionButton.tsx` → **0건**

- **근거 — 🔴 A-1(토론 환불 미연결)을 직접 재현했고 사실이다.** `calculateDiscussionRefund`는 만들어졌고 테스트도 6종 통과하는데 **아무도 부르지 않는다.** 취소 경로는 `meeting_type` 분기 없이 정기 규칙(3일/2일)으로 간다. 지금 prod에 토론모임이 0건이라 사고가 안 났을 뿐이고, **런북 §0은 켜는 날 첫 토론모임을 만들라고 한다.** 테스트가 통과한다는 사실이 오히려 "됐다"고 믿게 만드는 모양이라 위험이 크다.

- **특히 평가할 점**
  1. **"구현됨"이라는 상태 칸을 액면가로 받지 않고 호출부까지 봤다.** 이 파일의 존재 이유가 정한 것과 적용한 것의 간극인데, 그 간극이 **정의는 있고 배선이 없는** 형태로도 난다는 것을 찾아냈다. 상태 칸 감사를 시켰더니 감사 방법 자체를 한 단계 깊게 가져갔다
  2. **`🌑 다크 배포` 표기를 만든 판단이 옳다.** "배포 대기"와 되돌리는 방법이 다르다(미적용은 만들어야 하고, 다크 배포는 flip 하나) — 상태를 나누는 기준으로 정확하다
  3. **폐기하면서 살릴 것을 갈랐다.** 모임 5종을 닫으면서 「팝업 살롱」만 예외로 남겼다. 그 절이 지금 운영 중인 번개모임을 담당자·프로그램까지 넉 달 먼저 적어둔 1차 자료라는 이유다. 통째로 닫았으면 2단계 설계가 같은 것을 처음부터 다시 만들었을 것이다
  4. **되살릴 조건을 함께 적었다.** `🚫 안 함`이 "영원히 금지"가 아니라 "이 조건이면 다시 연다"가 되어, 다음에 같은 안이 올라와도 재논의가 아니라 조건 대조로 끝난다

- **관리자 정정 — 내 검증이 틀렸다.** "PR #50 diff에 `real_name || nickname`이 있다"를 확인하려고 `grep -c "real_name || nickname"`을 돌려 0이 나왔으나, 실제 코드는 `profile.real_name || profile.nickname`이라 **내 패턴이 틀렸다.** 다시 보니 PR #50은 그 두 파일(`notification.ts`·`meeting-remind/route.ts`)을 실제로 열고 있고 해당 줄이 hunk 안에 있다. **제품기획 주장이 맞다.**

- **범위를 넘지 않았다.** 코드 미수정, `core/` 미개정, 동시 작업 부서 파일(`VOICE.md`·개발운영 판정문) 미접촉

- **정의 개정 필요 없음.** `product.md` 첫 사용에서 §4(상태 칸이 이 파일의 주된 고장 방식)가 그대로 작동했고, §7(돈·회원 노출은 정하지 말고 올린다)대로 4건을 올렸다

- **후속** — 올라온 4건 중 ①③은 단무지님께 여쭌다(의도·일정은 관리자가 지어낼 수 없다). ②는 **얹는 쪽으로 추천**한다: 지금 5줄, 안 얹으면 문서총괄 N1대로 7곳이 되고 별도 PR·별도 배포가 된다. ④는 ①의 답에 달려 있다.
