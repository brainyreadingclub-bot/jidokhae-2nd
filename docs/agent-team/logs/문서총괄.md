# 문서총괄(사서) 작업 로그

> 최신이 맨 위. 양식은 [공통규약](../공통규약.md) §4.

---

## 2026-08-21 — 2차 점호 (1차 이후 23커밋이 만든 새 어긋남 + 1차 19건 닫힘 확인)

**한 일**

- 산출물: [`docs/agent-team/문서지도/2026-08-21-중복모순-2차.md`](../문서지도/2026-08-21-중복모순-2차.md) — **신규 14건 (🔴 3 · 🟡 9 · 🟢 2)**
- 범위: `7ac3211..origin/main`(1fd0418까지) 23커밋. PR #58~#61, 켜는날 런북 8-19/8-20 개정, 문서정비-계획 신설, 로드맵 3종 상태 공지, 카카오 채널 패널, 번개 설계 초안
- **1차 19건 전건 재확인표** 작성 — ✅ 닫힘 1(12번 로드맵) · 🟠 부분 닫힘 1(1번 런북) · ❌ 악화 2(15·16번) · 그대로 11 · 판정 보류 2 · 재조사 안 함 2
- prod 읽기 조회 6건(Supabase MCP, `SELECT`만) — 테이블 수 17개, `notifications`/`app_notifications` CHECK 실물, `site_settings` 키 목록, `meetings`/`books` 신규 컬럼
- `gh pr diff 49/50/53`으로 켜는 날 머지 예정 코드 직접 대조

**왜 그렇게 했는가**

- **1차의 🔴 1번(런북 결함)이 어떻게 닫혔는지를 먼저 봤다.** 이 체계가 실제로 작동했는지의 유일한 증거라서다. 결과: 절반만 닫혔고, **고치면서 새로 들어온 단계에서 같은 모양의 결함이 다시 났다**(N2 — `SOLAPI_TEMPLATE_WELCOME` env 누락). "닫혔다"로 끝냈으면 못 봤을 것이다
- **문서만 보지 않고 켜는 날 머지 예정 PR의 diff를 열었다.** 런북 사전조건이 그 3건을 "리뷰 완료"로 확정해 뒀으니, 그 코드는 이미 켜는 날 계획의 일부다. 열어보니 결정(호칭=닉네임)과 **반대 방향으로 2곳이 늘어나는 중**이었다(N1). 문서끼리만 대조했으면 놓쳤다
- **prod에 직접 물었다.** 1차에서 "마이그레이션 파일이 없다"까지만 확인하고 실행 여부는 미확인으로 남겼는데, 그 미확인이 이번에 오탐을 만들 뻔했다 — `migration-announcement-type.sql`이 문서 어디에도 없어 "런북 6번이 실패한다"로 쓸 뻔했으나, prod CHECK에 `announcement`가 이미 있었다. 조회 한 번이 잘못된 🔴 한 건을 막았다
- **`site_settings` 플래그 행이 아예 없는 것도 확인했다.** 언뜻 결함처럼 보이지만 코드가 `=== 'on'`으로 읽어 OFF와 동일하고 켜기는 UPSERT다. 공통규약 §1(의도 확인)대로 지적하지 않고 "어긋남 아님"에 남겼다
- 지시받은 대로 프로필 갱신·경과일 갱신·옵시디언 재조사는 하지 않았다. 대신 **전제 정정**을 §0에 뒀다 — "트리가 곧 최신"은 `main`에서는 아직 참이 아니다(프로필 08-17판·검토문서 README 갱신판 등 4개가 브랜치에만 있다). 이걸 안 적으면 `main`에서 갈라진 다른 세션이 4개월 묵은 프로필을 읽는다

**안 한 것과 이유**

- **아무것도 고치지 않았다** — 부서 정의 §6. N1·N2·N4·N10·N11은 개발·운영, N3·N7·N9는 제품기획 소관
- **`DECISIONS.md` `🔨 미적용` 경과일 갱신 생략** — 제품기획이 같은 파일 상태 칸을 고치는 중이라 관리자 지시로 생략
- **오늘 작업 중인 5개 파일은 어긋남으로 세지 않았다**(`DECISIONS.md`·`2026-08-21-미적용-우선순위.md`·`2026-08-21-켜는날-코드준비-판정.md`·`VOICE.md`·`DESIGN_TOKENS.md`). 1차 2·4·11번은 "판정 보류"로 표시만 했다
- **옵시디언 보관소 재조사 안 함** — 지시대로
- **Vercel·Solapi 콘솔 미확인** — N10(`KAKAO_REST_API_KEY` prod 설정 여부)·N3(승인 상태 실물)은 **확인 필요**로 남겼다. 사람만 볼 수 있다
- **prod 쓰기 없음** — 전부 `SELECT`. 대상 ref는 `.env.local`의 `NEXT_PUBLIC_SUPABASE_URL`과 대조해 `ycqqzzvyixvtdorjxkrn` 하나임을 먼저 확인

**주고받은 메시지**

- 없음. 다른 세 부서(제품기획·디자인·개발·운영)와 파일이 겹치지 않도록 소관 밖 파일은 읽기만 했다

**다음**

- 관리자 판단 필요 — **N1·N2는 켜는 날 이전에 개발·운영으로 넘어가야 한다.** 지금 두 줄이면 끝나고, 머지 후엔 7곳이 된다
- **N3(`CLAUDE.md`의 BOOK_ASK "재심사 중")은 한 줄 수정이지만 매 세션 주입되는 문장**이라 우선순위가 높다
- N9(1차 보고서 ↔ 문서정비-계획 이중화)는 **어느 쪽을 체크리스트로 삼을지 관리자가 정해야** 다음 정비가 두 번 일어나지 않는다
- 3차 점호 시: `origin/main`이 `40b82e0` 이후로 더 나갔을 것이므로 시작점은 `1fd0418`

**관리자 판정** — 🟢 통과 (2026-08-21)

- **연 파일 / 돌린 명령**
  - `docs/agent-team/문서지도/2026-08-21-중복모순-2차.md` — 신규 14건 + 1차 19건 재확인표 확인
  - `gh pr diff 49 | grep "^+" | grep "real_name || "` → **1건 추가.** `gh pr diff 53` → **1건 추가.** N1 확인
  - `CLAUDE.md:101-102` — "BOOK_ASK 재심사 중 / 켜기 게이트 = BOOK_ASK 승인" 확인. 런북 `:15`는 "✅ APPROVED (8/19)". N3 확인

- **근거 — 🔴 3건 중 2건을 직접 재현했고 둘 다 사실이다.** N2(`SOLAPI_TEMPLATE_WELCOME` 누락)는 개발·운영이 같은 것을 독립적으로 찾아냈다 — **서로 모르는 두 부서가 같은 구멍을 짚었다.** 오탐일 확률이 크게 내려간다.

- **가장 값어치 있는 발견은 N1이다.** 1차에서 "결정이 14일째 코드에 없다"를 잡았는데, 2차는 한 걸음 더 갔다 — **켜는 날 머지할 PR들이 그 결정을 되돌리는 방향으로 코드를 늘린다.** 방치가 아니라 역행이다. 지금 두 줄이고 머지 후엔 일곱 곳이다.

- **특히 평가할 점**
  1. **관리자가 준 전제를 정정했다.** 내가 소환문에 "트리가 곧 최신"이라고 썼는데, `main`에서는 여전히 아니라고 §0에서 바로잡았다 — 검증 프로필 08-17판·`검토문서/README` 갱신판이 `feat/agent-team`에만 있어 **main에서 갈라진 세션은 4개월 묵은 프로필을 읽는다.** 공통규약 §8(기준 문서가 낡았으면 멈추고 보고)을 자기를 부른 사람에게 적용했다. 1차에서도 같은 성질이 나왔다(15~19번이 관리자 산출물의 결함)
  2. **1차 19건을 전건 재확인표로 닫았다.** "그대로 11 · 악화 2 · 부분 닫힘 1"까지 구분해, 이 체계가 실제로 굴러가는지를 숫자로 남겼다. 특히 **1차 15번(README 인덱스 누락)이 "악화"**로 뒤집힌 것 — 규칙을 만든 문서가 그 규칙을 또 어겼다
  3. **오탐 하나를 prod 조회로 막았다.** `migration-announcement-type.sql`이 문서 어디에도 없어 미실행처럼 보였는데, prod CHECK에 이미 반영돼 있었다. 문서만 봤으면 🔴로 올렸을 자리다
  4. **부작용을 짚었다(N7).** 1차 12번을 고치려고 로드맵에 상태 공지를 넣은 것이 `CLAUDE.md`와 새 모순을 만들었다. **조용한 낡음을 시끄러운 모순으로 바꾼 것**이라, 고치는 행위 자체가 어긋남을 만들 수 있다는 지적이다

- **범위를 넘지 않았다.** 아무것도 고치지 않았고, 동시 작업 4파일을 어긋남으로 올리지 않았으며, 보관소 재조사도 지시대로 건너뛰었다. prod는 `SELECT`만(6건), ref는 `.env.local`과 대조

- **정의 개정 필요 없음.** 두 번째 소환에서 §3(새로 발견한 것만)이 지켜졌다 — 1차 항목 반복 없이 신규 14건만 냈다

- **관리자가 답해야 할 것 (N9)** — 1차 보고서와 `문서정비-계획`이 같은 사안의 두 문서가 됐다. **체크리스트는 `문서정비-계획` 쪽으로 정한다.** 이유: 그쪽이 "언제 무엇을 고칠지"를 일정으로 들고 있고, 1차 보고서는 시점 스냅샷이라 갱신 대상이 아니다. 다만 계획서에 **1차·2차 보고서를 근거로 링크**하고, 계획서가 틀리게 적은 한 줄("CLAUDE.md 현행 상태 줄 갱신됨(8-17·18)" — `git log`상 8-18 갱신 없음)을 바로잡는다. 이 작업은 **제품기획 소관**(`검토문서/**`)이라 다음 소환에 넘긴다.

---

## 2026-08-18 — 1차 중복·모순 조사 (저장소 × 옵시디언 보관소)

**한 일**

- 산출물: [`docs/agent-team/문서지도/2026-08-18-중복모순-1차.md`](../문서지도/2026-08-18-중복모순-1차.md) — **19건 (🔴 5 · 🟡 11 · 🟢 3)**
- 범위: 저장소 `C:\jidokhae-2nd` 전체(`.claude/worktrees/` 제외) + 옵시디언 개인 보관소 `Documents\Sync`(읽기만). `Documents\jidokhae-doce`는 링크 모음이라 세지 않음
- 마크다운 링크 전수 검사 스크립트 실행 → **깨진 링크 0건**. 백틱 경로 참조 검사 → 실사용 문서에서 4건
- `DECISIONS.md` `🔨 미적용` 경과일 갱신 — **19건, 최장 33일**(2026-07-16 물어보기 진입점)
- 관리자 짐작 후보 2건: **둘 다 맞음.** ②는 짐작 범위보다 큼(멤버십 제도 전체가 저장소에 없고 폐기 기록도 없음)

**가장 심각한 것 3개**

1. **켜는 날 런북 ↔ 알림톡 변수 대조표 충돌** — 런북(08-18)은 "env만 교체, 코드 수정 없음", 대조표(08-13)는 "env와 PR #50/#49 머지는 같이 나가야 한다". `git grep 결제상태 main -- src` **0건**, PR #49·#50 아직 draft. 런북대로 하면 알림톡 3종이 켜는 날 죽는다
2. **`VOICE.md:17,331` 호칭 "미정" ↔ `DECISIONS.md` 2026-08-07 "닉네임 확정"** — `DECISIONS.md`를 만들게 한 바로 그 사고의 재발. 11일째. 결정 이후 VOICE.md가 두 번 개정됐는데도 그 줄만 남음
3. **`project_next_session.md`가 두 벌** — 저장소(08-13, 118행)는 "열린 PR 5건 + 머지 순서"를, `.claude`(08-18, 59행)는 "전부 머지·플래그 OFF"를 말한다. 자동 주입되는 건 후자인데 `CLAUDE.md`는 전자를 문서화한다. `MEMORY.md`도 같은 구조(저장소 11파일 vs `.claude` 65파일)

**왜 그렇게 했는가**

- **`main` 기준으로 판정했다.** 현재 체크아웃된 `feat/agent-team`이 PR #56·#57 이전에서 갈라져 `DECISIONS.md`가 4행 뒤처지고 런북이 아예 없었다. 브랜치 트리만 보고 "런북이 없다"고 썼으면 오탐이었다. 이 교차 상태 자체도 항목으로 올렸다(부서들이 이 트리를 읽는다)
- **"이미 알려진 것"을 무조건 빼지 않았다.** 프로필 §알려진 미해결에 있는 것(알림 union 4중 정의, 재발송 불가, 호칭 불일치)은 새 발견으로 보고하지 않았다. 다만 품질 부서 2026-08-14 리포트가 지적한 것 중 **4일 지나도 안 고쳐진 것**(C2 마이그레이션 미커밋, W12-b 메뉴 7개, W12-c lib 누락)은 "경과"를 붙여 남겼다 — 그 리포트는 프로필의 면책 목록이 아니고, 방치 기간이 판단 근거가 된다
- **의도 확인(규약 §1)을 거쳤다.** `pending_transfer` 동일 표시, `attended` 미사용, TossPayments 잔존, 토큰 3명 마스킹은 프로필 §의도된 설계라 지적하지 않았다. 다만 `attended`는 보관소 멤버십 노트의 "체크인 차감"과 충돌하므로 **보관소 대조 맥락에서만** 언급했다
- **§8(기준 문서 낡음)대로 보고했다.** 프로필이 스스로 건 트립와이어 3개 중 2개가 이미 울려 있다(결정 최신일 08-17≠08-18, 갱신일 이후 코드 변경 있음)
- **숫자는 세어서 적었다.** lib 38 / API 35 / 테이블 17 / adminMenu 9는 전부 `git ls-tree`·`grep` 실측

**안 한 것과 이유**

- **아무 문서도 고치지 않았다** — 범위 밖(부서정의 §2). 1·2·4번은 각각 개발·운영 / 디자인 / 제품기획 소관
- **옵시디언 `Documents\Sync`는 읽기만** — 규약상 쓰기 금지
- **Solapi 콘솔 미확인** — V2 템플릿이 실제로 `#{결제상태}`를 요구하는지는 외부 SaaS라 볼 수 없다. 1번은 "대조표가 명시 + 코드에 토큰 없음"까지만 검증됨. **켜는 날 전 콘솔 육안 확인 필요**로 남김
- **prod DB 미질의** — 테이블 17개는 마이그레이션 파일 대조로 계산. `notifications.type` CHECK에 `book_ask`가 실제로 있는지는 미확인(품질 2026-08-14 C2 숙제 그대로)
- **`Sync`의 지독해 무관 노트 미열람** — Inbox는 `지독해|brainy-club|jidokhae` 필터로 10개만 확인

**주고받은 메시지**

- 없음. 이번 조사는 다른 부서 산출물을 읽기만 했고 의존 쌍(규약 §2)에 해당하는 왕복이 없었다

**다음**

- 🔴 1번은 **켜는 날 전에 반드시** 개발·운영이 판정해야 한다 — 런북에 PR #50/#49 머지 단계를 넣든, 코드가 이미 충분하다고 확인하든
- 🔴 2·4번은 문서 한 줄 수정이면 끝난다(VOICE.md 호칭 / DECISIONS 상태 칸 8행). 제품기획·디자인에 넘길 것
- 🔴 3번(`project_next_session.md`·`MEMORY.md` 이중화)은 **어느 쪽이 정본인지 결정**이 먼저다. 결정 없이 한쪽을 지우면 기록이 날아간다
- 🟡 14번(보관소 멤버십 설계)은 `DECISIONS.md`의 `🚫 안 함` 한 줄로 닫는 것이 가장 싸다 — 지금은 폐기됐다는 기록이 없어 언제든 되살아날 수 있다
- 다음 점호부터는 **이 보고서 이후 새로 생긴 것만** 추리면 된다

**관리자 판정** — 🟢 통과 (2026-08-18)

- **연 파일**
  - `docs/agent-team/문서지도/2026-08-18-중복모순-1차.md` (379행, 목차·심각도 분류 확인)
  - `git show main:검토문서/2026-08-18-켜는날-런북.md` — 102행 전문에 `#49`·`#50`·`결제상태` **0회** 확인
  - `git grep 결제상태 main -- jidokhae-web/src` — **0건** 확인
  - `git show main:jidokhae-web/VOICE.md` — 17행 `호칭 | **미정**` 확인
  - `git grep "real_name || " main -- jidokhae-web/src` — 4파일 확인

- **근거** — 1·2번을 관리자가 직접 재현했고 둘 다 사실이다. 짐작 후보 2건도 맞았고, ②는 짐작보다 컸다(폐기 기록 없는 사업 설계가 보관소에만 존재). **오탐 0건** — 프로필의 "의도된 설계"를 새 문제로 올린 것이 하나도 없다.

- **특히 평가할 점** — 15~19번이 **관리자가 오늘 만든 것들의 결함**이다(`검토문서/README` 규칙을 만든 날 위반, 프로필 트립와이어가 이미 울림, 공통규약 §7 위반, 안내판이 "초안"인데 필독). 자기를 부른 사람의 산출물을 봐준 것이라 정의가 제대로 작동했다.

- **정의 개정 필요 없음.** 첫 소환에서 목적("두 문서가 다른 말을 하지 않게")대로 동작했고 범위 밖(내용 판단·이름 변경·보관소 수정)을 넘지 않았다.

- **후속** — 🔴 1번은 **켜는 날 전 필수 확인 사항**이라 `memory/project_next_session.md`로 올린다. 2·4번은 한 줄 수정이라 다음 세션에서 즉시 처리.
