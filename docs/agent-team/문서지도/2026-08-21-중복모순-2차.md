# 문서 중복·모순 2차 점호 (2026-08-21)

> 부서: 문서총괄(사서) · 두 번째 소환
> 범위: 1차 보고서 시점(`7ac3211`) 이후 `origin/main`에 들어온 23커밋(`1fd0418`까지)이 문서·코드에 만든 **새 어긋남만**
> 제외: 옵시디언 개인 보관소(관리자 지시로 재조사 안 함) · `.claude/worktrees/` · 오늘 세 부서가 작업 중인 5개 파일(`DECISIONS.md`, `2026-08-21-미적용-우선순위.md`, `2026-08-21-켜는날-코드준비-판정.md`, `VOICE.md`, `DESIGN_TOKENS.md`)

**요약: 신규 14건 — 🔴 3 · 🟡 9 · 🟢 2**
**1차 19건 중: ✅ 닫힘 1 · 🟠 부분 닫힘 1 · ❌ 악화 2 · 그대로 11 · 판정 보류 2 · 재조사 안 함 2**

---

## 0. 전제 정정 — "트리가 곧 최신"은 절반만 참이다

소환 지시는 1차의 교차 상태가 해소됐다고 했다. **작업 트리에서는 맞고, `origin/main`에서는 아직 아니다.**

`git diff origin/main HEAD --name-only`로 확인한, **`main`에 아직 없는 문서 4개**:

| 파일 | main | feat/agent-team |
|---|---|---|
| `verification-squad/profiles/jidokhae-profile.md` | **2026-08-14 구판** (전면개편 이전) | 2026-08-17 갱신판 |
| `검토문서/README.md` | 구판(6개만) | 2026-08-18 갱신판 |
| `roadmap/README.md` · `docs/expert-panel/README.md` | **없음** | 있음 (표지 신설분) |

머지는 `main → 브랜치` 한 방향으로만 일어났다. 즉 **`main`에서 갈라진 워크트리·다른 세션은 여전히 4개월 묵은 검증 프로필을 읽는다.** 공통규약 §8이 정확히 이 경우를 "멈추고 보고"로 정했다.

> 또한 점호 중 `origin/main`이 `40b82e0`(PR #62, `(next)/me` 지역 라벨)까지 전진했다. 코드 1파일뿐이라 문서 영향은 없다고 판단했으나, **이 보고서의 근거는 `1fd0418` 기준**이다.

---

## 🔴 신규 — 켜는 날에 터진다

### N1. 켜는 날 머지 예정 PR 3건이 "호칭=닉네임" 결정을 **되돌리는 방향으로** 코드를 2곳 늘린다

| 어디 | 무엇 |
|---|---|
| `검토문서/DECISIONS.md` 2026-08-07행 | **"호칭은 전 채널 닉네임.** `real_name \|\| nickname`을 `nickname \|\| real_name`으로 뒤집는다" |
| `gh pr diff 49` (+205행) | `+ const displayName = profile.real_name \|\| profile.nickname` — **신규 추가** |
| `gh pr diff 53` (`notification.ts` `sendNewMemberWelcomeNotification`) | `+ const displayName = profile.real_name \|\| profile.nickname` — **신규 추가** |
| `검토문서/2026-08-18-켜는날-런북.md:16` | 그 3건을 "✅ 리뷰 완료 (8/20) — 당일 머지만 남음"으로 사전조건에 올림 |

**실측:** 현재 `HEAD`에 `real_name || nickname` **5곳**(`lib/notification.ts:164,209,254,284` · `api/cron/meeting-remind/route.ts:96`). PR #49·#53 머지 시 **7곳**이 된다.

**어느 쪽이 최신인가:** 결정(08-07)이 코드(08-13 작성 PR)보다 6일 먼저다. 즉 **결정을 모른 채 쓴 코드가 아니라, 결정 이후에 쓰였는데 결정을 따르지 않은 코드**다.

**심각도 🔴** — 1차 2번은 "결정이 문서에 안 옮겨졌다"였다. 이건 한 단계 나쁘다: **결정과 반대 방향으로 부채가 늘어나는 중**이고, 늘어나는 시점이 하필 켜는 날이다. 지금 머지 전에 두 줄만 고치면 되고, 머지 후엔 7곳을 고쳐야 한다.

---

### N2. 런북이 PR #53에 필요한 **신규 env 키 이름을 적지 않았다**

| 어디 몇 행 | 무엇 |
|---|---|
| `검토문서/2026-08-18-켜는날-런북.md:18` | `SOLAPI_TEMPLATE_BOOK_ASK` **env 신설** — 키 이름·템플릿 ID 명시 ✅ |
| 〃 `:45` | `\| (가입 환영 — **코드 확인 필요 시**) \| KA01TP260811081356296zFnA72ABSa5 \|` — **키 이름 없음** |
| 〃 `:16` | 그런데 사전조건은 **#53 머지를 확정**해 두었다 |
| `gh pr diff 53` (`.env.example` +1) | `+SOLAPI_TEMPLATE_WELCOME=` — **신설 키다** |
| 〃 (`notification.ts`) | `templateCode: process.env.SOLAPI_TEMPLATE_WELCOME!` |

**따라가면 어떻게 되나:** 런북대로 하면 #53은 머지되고 `SOLAPI_TEMPLATE_WELCOME`은 설정되지 않는다. `!` non-null assertion이라 런타임에 `undefined`가 그대로 Solapi로 나가고, 알림은 fire-and-forget이라 **가입 흐름은 멀쩡한 채 환영 알림톡만 조용히 실패**한다. 프로필 설정 시점 1회 발송이라 실패를 알아챌 계기도 없다.

**어느 쪽이 최신인가:** 런북(8-20 개정)이 최신이지만 **최신 쪽이 빠뜨렸다.** 같은 문서가 BOOK_ASK는 "신설"로 정확히 처리했으므로 규칙을 몰라서가 아니라 한 건을 놓친 것이다.

**심각도 🔴** — 1차 1번과 **같은 실패 모양의 재발**(env와 코드가 짝이 안 맞아 알림톡이 조용히 죽는다). 1번을 고치면서 새로 들어온 단계에서 다시 났다.

---

### N3. BOOK_ASK 심사 상태가 문서 세 곳에서 갈린다 — 매 세션 주입되는 쪽이 낡았다

| 어디 몇 행 | 무엇이라고 적혀 있나 |
|---|---|
| `검토문서/2026-08-18-켜는날-런북.md:15` | BOOK_ASK 재심사 — **✅ APPROVED (8/19) — 7종 전원 승인** |
| `CLAUDE.md:101` | "알림톡 V2 6종 승인, **BOOK_ASK 재심사 중**" |
| `CLAUDE.md:102` | "**플래그 켜기 게이트: BOOK_ASK 재심사 승인** + env 6개 교체 + 플래그 flip" |
| `verification-squad/profiles/jidokhae-profile.md:65` | "BOOK_ASK는 문구 수정 **재심사 중(INSPECTING)**" |
| `docs/expert-panel/2026-08-19-kakao-channel-add-rate.md:38` | "BOOK_ASK **재심사 계류**"를 논거로 씀 |

**어느 쪽이 최신인가:** 런북이다. 개정 커밋 `33554b6`(8-20)이 가장 늦고, 근거도 구체적이다(승인된 템플릿 ID `KA01TP260811074235614jtohwC3zIb1`을 `:18`에 적어뒀다). `CLAUDE.md`의 마지막 수정은 `699e70b`(**8-17**)로 사흘 이르다.

**심각도 🔴** — `CLAUDE.md`는 **매 세션 자동 주입**된다. 지금 이 저장소에서 일을 시작하는 누구든 "켜는 날은 아직 외부 심사에 막혀 있다"고 읽는다. 실제로는 마지막 외부 관문이 이틀 전에 풀렸고 남은 건 전부 우리 손 안의 일(PR 머지·env·플래그)이다. **막힌 줄 알고 안 하는 것**이 이 프로젝트에서 가장 비싼 실수다.

---

## 🟡 신규

### N4. 런북과 대조표가 아직 두 곳에서 어긋난다 — 1차 1번은 **부분적으로만** 닫혔다

개정으로 닫힌 것: "코드 수정 없음" 문장 삭제 · `#50 → #49 → #53` 머지 명시 · BOOK_ASK env 신설 추가 · "6개"의 세는 법 정합(§1-1 표 6행 + BOOK_ASK 별도). **여기까지는 제대로 닫혔다.**

남은 두 가지:

| # | `검토문서/2026-08-13-알림톡-변수-대조표-및-배포-순서.md` | `검토문서/2026-08-18-켜는날-런북.md` |
|---|---|---|
| (a) | `:65` 5번 — "배포 후 **실제 발송 1건 확인** — `notifications` 테이블 status가 `sent`인지" | §2 점검 목록(`:54-60`) 7개 중 **알림톡 발송을 확인하는 항목이 없다** |
| (b) | `:66` 6번 — "`library_enabled='on'`은 **맨 마지막.** 이걸 3(env)보다 먼저 켜면 물어보기 알림톡이 env 없이 실패한다" | `:31` 3번 — 발송 확인 없이 **플래그 2개를 동시에 켠다** |

**어느 쪽이 최신인가:** 런북(8-20)이다. 그러나 (a)는 런북에 **대응 항목 자체가 없어** "최신 판단이 옛 판단을 덮었다"고 볼 수 없다 — 누락과 개정은 다르다. (b)는 런북 순서상 env 교체(1번)가 플래그(3번)보다 먼저라 대조표의 걱정은 실질적으로 해소되지만, 그 사이에 **대조표가 요구한 발송 검증이 빠져 있어** 실패를 켠 뒤에야 알게 된다.

### N5. "켜는 날 따를 문서"가 둘이라고 적혀 있다

| 어디 몇 행 | 무엇 |
|---|---|
| `검토문서/README.md` 알림톡 절 마지막 줄 | "⭐ `2026-08-13-알림톡-변수-대조표-및-배포-순서.md` — 템플릿별 변수 토큰 + 배포 6단계. **켜는 날 이 문서를 따른다**" |
| `검토문서/2026-08-18-켜는날-런북.md:4` | "**이 문서 하나로** 결정→실행→점검→롤백까지 끝낸다" |

두 문서가 각각 자기가 켜는 날의 단일 진입점이라고 선언한다. 그리고 **README에는 런북이 실려 있지 않아**(아래 N6) README만 읽은 사람은 런북의 존재를 모른다. N4가 보여주듯 두 문서의 순서·점검 항목은 실제로 다르다.

### N6. `검토문서/README.md`가 신규 2건을 또 빠뜨렸다 — 1차 15번 **악화**

`검토문서/README.md`는 스스로 *"파일을 추가하면 위 목록에 **그 자리에서** 한 줄 넣는다. 나중에 하겠다고 미루면 안 한다 — 실제로 8건이 그렇게 빠졌다"*(추가/이동 시 원칙 0)라고 적어둔 문서다.

빠진 것: `2026-08-18-켜는날-런북.md`(1차에서 이미 지적) · `2026-08-18-문서정비-계획.md`(**이번에 새로**). 1건 → 2건.

### N7. 로드맵 3종의 새 상태 공지가 `CLAUDE.md`와 **정면으로 부딪힌다**

8-18에 로드맵 3종 상단에 상태 공지가 들어갔다. 그 자체는 1차 12번을 닫은 좋은 조치인데, **한쪽만 고쳐서 조용한 낡음이 시끄러운 모순으로 승격**됐다.

| 어디 몇 행 | 무엇 |
|---|---|
| `roadmap/milestones-phase3.md:3` (·`work-packages-phase3.md:3`·`scenarios-phase3.md:3` 동일) | "🔴 **M8·M9는 전면개편으로 대체되었다** … **M10(정산)은 완료**" |
| `CLAUDE.md:124-125` | "- M8 관리자 CMS — 배너 + 한 줄 … - M9 회원 홈 콘텐츠 전면 오픈" — **앞으로 할 일 목록** |
| `CLAUDE.md:143-144` | Milestone Overview 표에 M8·M9가 미래 산출물로 |
| `CLAUDE.md:145` · `:306` | M10을 "관리자 심화(Phase 3)" · `admin/settlements`를 "**M10 placeholder**" |

M10은 2026-07-05에 이미 완료돼 prod에서 돌고 있다(`/admin/settlements` 3탭). **최신은 로드맵 쪽**(8-18 vs CLAUDE.md 8-17)이고, 근거도 로드맵이 구체적이다.

### N8. 같은 6줄이 로드맵 3개 파일에 그대로 복붙돼 있다

`roadmap/milestones-phase3.md:3-7` · `work-packages-phase3.md:3-7` · `scenarios-phase3.md:3-7` — **글자까지 동일한 5줄 블록**이 세 벌.

공통규약이 머리말에서 *"복붙하면 한쪽만 갱신되어 어긋난다 — 이 프로젝트가 이미 겪은 사고다"*라고 못박은 형태 그대로다. 특히 "M11은 토스 스킨 결정과 **정합 재검토 필요**"는 앞으로 결론이 바뀔 문장이라 세 곳이 갈릴 자리다. (한 곳에 두고 나머지가 가리키면 끝날 일이다.)

### N9. `문서정비-계획`이 1차 보고서와 **같은 사안을 다루는 두 번째 문서**가 됐다

같은 날(2026-08-18) 만들어진 두 문서가 서로를 참조하지 않는다.

| `검토문서/2026-08-18-문서정비-계획.md` | 1차 보고서 대응 항목 |
|---|---|
| §2-1 서비스 개요 Out of scope·§7-1·§7-2·번개 부재 | **7번** |
| §2-3 라우트/DB 스키마 미반영 | **9번** |
| §2-4 `CLAUDE.md` 2종 상세 목록 | **6·8·9·10번** |
| §1 로드맵 3종 상태 공지 | **12번** (실제로 닫음) |

**세부에서 어긋난다** — 계획 §2-4는 *"현행 상태 줄은 갱신됨(**8-17·18**)"*이라고 넘어가지만:
- `git log -- CLAUDE.md` → 마지막 수정은 `699e70b` **8-17 하나뿐**. 8-18 갱신은 없다
- `CLAUDE.md:94` 첫 Current status 블록은 여전히 "M7 Step 3 (회원 홈) **예정**"
- `CLAUDE.md:96` 둘째 블록 머리글은 "**(2026-07-28 갱신)**"
- 그 안의 BOOK_ASK 상태는 위 N3대로 낡았다

즉 계획이 "괜찮다"고 판정하고 넘어간 자리가 실제로는 1차 6번이 지적한 이중 블록 그대로다. 또 계획에는 **1차 8번(알림톡 5종)·10번(배너 권한 두 CLAUDE.md 불일치)에 해당하는 줄이 없다.**

**어느 쪽이 최신인가:** 같은 날짜다. 근거의 정밀도로 보면 1차 보고서가 파일:행을 달고 있고 계획은 "위치/방침"만 적는다. **범위는 계획이 넓다**(PRD §UX·마이페이지는 1차에 없다). 둘 중 하나가 틀린 게 아니라 **두 벌로 갈라진 것 자체가 문제**다 — 다음에 고치는 사람은 어느 쪽을 체크리스트로 삼을지 알 수 없다.

### N10. `KAKAO_REST_API_KEY`가 어느 현행 문서에도 없는데, 켜는 날 사전조건이 그 위에 얹혔다

| 어디 | 무엇 |
|---|---|
| `jidokhae-web/src/lib/kakao-books.ts:42` | `const key = process.env.KAKAO_REST_API_KEY` |
| `jidokhae-web/src/components/admin/BookPicker.tsx:46` | `fetch('/api/books/search?q=…')` → 위 함수 호출 (PR #57 신규) |
| `jidokhae-web/.env.example` | **없음** (전수 확인) |
| `CLAUDE.md:411-421` Environment Variables | **없음** |
| `검토문서/2026-08-18-켜는날-런북.md` | **없음.** 그런데 `:19` 사전조건이 "다음 토론모임 + **책 연결** + 발제문 선등록" |
| 유일한 기재 | `docs/superpowers/plans/2026-07-12-…-1조각A-서재기반.md:453` 등 **지나간 계획 문서 4곳** |

키가 없으면 admin 모임 폼의 책 검색이 500으로 떨어지고, 런북 사전조건 "책 연결"이 막힌다. 종전에는 회원 서재(플래그 OFF)만 이 키를 썼는데, **PR #57로 운영자 필수 경로가 같은 키에 얹혔다.**

**확인 필요** — prod Vercel에 이미 설정돼 있을 가능성이 높다(PR #42 시점에 등록 안내가 있었다). Vercel 콘솔은 볼 수 없어 **미확인**이다. 최소한 `.env.example`에 한 줄은 있어야 한다.

### N11. 런북이 지시하는 `migration-welcome-notification.sql`이 `main`에 없다

`검토문서/2026-08-18-켜는날-런북.md:17` — *"🔴 **#53 전 `migration-welcome-notification.sql` prod 실행**(CHECK+UNIQUE — 미실행 시 환영 알림이 조용히 무발동)"*

`git ls-tree -r origin/main -- jidokhae-web/supabase/` 전수 확인 → **없다.** PR #53 브랜치(`feat/welcome-alimtalk`)에만 있다. 런북은 그 파일을 **#53 머지 *전*에** 실행하라고 하므로, 실행 시점에는 `main`에서 찾을 수 없다.

1차 5번과 같은 종류이나 대상이 새 파일이고, **런북이 새로 만든 지시**라 따로 적는다. (실무적으로는 PR 브랜치에서 꺼내면 되므로 심각도는 낮춰 잡는다.)

### N12. 만들어진 지 하루 된 규칙을 새 문서가 어겼다 — 공통규약 §7

`docs/expert-panel/2026-08-19-kakao-channel-add-rate.md:5` — *"**250명 규모** 지역 독서모임 서비스(지독해) … ② 기존 회원 **240여 명** 유도"*
`:41` 최종 합의 — *"**250명 규모에서** 채널 친구를 모으는 본선은 …"*

공통규약 §7("변하는 값은 문서에 적지 않는다")은 **2026-08-18**에 정해졌고, 이 문서는 **8-19**에 만들어졌다. 1차 18번이 든 사례들은 전부 규약 이전 문서였다 — 이건 규약 이후 첫 신규 위반이다.

덧붙여, 1차 14-c가 확인한 대로 보관소(`320. Area\지독해\…`)는 **활성 고유 멤버 약 30명**으로 센다. 이 패널의 결론 강도(오프라인 캠페인 우선)는 규모 판단 위에 서 있는데, **그 규모 숫자가 다른 문서와 8배 차이 난다.** 어느 쪽이 맞는지는 내 판단 밖이고, `profiles`를 세면 30초에 끝난다.

---

## 🟢 신규

### N13. 런북이 "당일 머지만 남음"이라 적은 PR 3건은 전부 **draft**다

`검토문서/2026-08-18-켜는날-런북.md:16` — "✅ 리뷰 완료 (8/20, 3건 모두 MERGEABLE·상호 무충돌) — **당일 머지만 남음**"

`gh pr list --json number,isDraft,mergeable` 실측 → #49·#50·#53 **셋 다 `isDraft: true`**(`mergeable: MERGEABLE`은 맞다). draft는 머지 버튼이 잠겨 있어 "Ready for review" 전환 한 단계가 더 있다. 30분짜리 실행 계획에서 예상 밖 단계가 나오면 그날 판단이 흔들린다. 한 줄이면 닫힌다.

### N14. 1차 17번 죽은 참조 4건 그대로

`roadmap/work-packages-phase3.md:136` — `src/components/admin/Sidebar.tsx` → 실제 이름은 **`AdminSidebar.tsx`**(디렉토리 확인). 나머지 3건도 변동 없음.

---

## 1차 19건 — 닫혔는지 한 줄씩 확인

이 체계가 실제로 작동하는지의 증거다.

| # | 1차 항목 | 지금 | 근거 |
|:--:|---|---|---|
| 1 | 런북 ↔ 대조표 충돌 | 🟠 **부분 닫힘** | `b9a59a7`(8-19)·`33554b6`(8-20)로 "코드 수정 없음" 삭제 + `#50→#49→#53` 명시 + BOOK_ASK env 추가 + "6개" 정합. **남은 것 = N4(발송 검증 누락·플래그 순서)·N2(WELCOME env 누락)** |
| 2 | `VOICE.md` 호칭 미정 | ⏸ **판정 보류** | 디자인이 오늘 작업 중. 코드 5곳(`notification.ts:164,209,254,284`·`meeting-remind:96`)은 그대로이고, **N1로 2곳 더 늘어날 예정** |
| 3 | `project_next_session.md` 두 벌 | 그대로 | 저장소판 여전히 "한 줄 요약 (**2026-08-13** 기준)" |
| 4 | `DECISIONS.md` 상태 칸 낡음 | ⏸ **판정 보류** | 제품기획이 오늘 고치는 중. 실제로 갱신 흔적 확인(`🌑 다크 배포` 등 새 상태 표기 등장) |
| 5 | 마이그레이션 3건 저장소에 없음 | 그대로 | `supabase/` 33개 SQL 전수 재확인 — `migration-book-ask.sql`·`-rollback`·`-welcome-notification` 여전히 없음. **다만 prod 조회로 book-ask는 실행된 것이 확인됐다**(아래 검증 절) |
| 6 | `CLAUDE.md` Current status 두 블록 | 그대로 | `:94` "M7 Step 3 예정" · `:96` "(2026-07-28 갱신)" |
| 7 | Out of scope ↔ 배지·book tracking | 그대로 | `CLAUDE.md:130` 원문 유지. 문서정비-계획 §2-1이 "오픈 직후 개정"으로 **일정만** 잡음 |
| 8 | 알림톡 "5종" | 그대로 | `CLAUDE.md:112,179,378,418` + `.env.example` 5개 |
| 9 | 코드 목록이 실제의 절반 | **격차 확대** | admin 메뉴가 9 → **10개**(`adminMenu.ts`에 `공지 보내기` 추가, PR #56). 문서는 여전히 7개 |
| 10 | 두 `CLAUDE.md` 배너 권한 불일치 | 그대로(악화) | `jidokhae-web/CLAUDE.md:85` "7개 메뉴 … adminOnly는 정산/배너/설정" ↔ 코드의 `adminOnly`는 정산·공지 보내기·알림톡 이력·설정 **4개, 배너 아님** |
| 11 | `DESIGN_TOKENS.md`에 토스 토큰 없음 | ⏸ **판정 보류** | 디자인이 오늘 작업 중 |
| 12 | 로드맵 M9 낡음 · `/home` 이중 의미 | ✅ **닫힘** | 3종 상단 상태 공지(`d1cb4a7`·`19ed506`·`69e99d0`). **단 N7·N8이 새로 생김** |
| 13 | 옵시디언 칸반 3개월 방치 | — | 지시로 재조사 안 함 |
| 14 | 보관소 멤버십·모임 구분 | — | 지시로 재조사 안 함 |
| 15 | `검토문서/README.md` 인덱스 누락 | ❌ **악화** | 1건 → **2건**(N6) |
| 16 | 프로필 트립와이어가 울려 있음 | ❌ **악화** | 여전히 3개 중 2개 울림(아래). 게다가 **갱신판이 `main`에 없다**(§0) |
| 17 | 죽은 참조 4건 | 그대로 | N14 |
| 18 | 변하는 값 숫자 박기 | 그대로 + **신규 1건** | N12 |
| 19 | 안내판이 자기를 "초안"이라 함 | 그대로 | `:3` 원문 유지 |

### 프로필 낡음 감지 트립와이어 — 3개 중 2개가 여전히 울린다

| 확인 | 프로필이 적은 값 | 실측 | 판정 |
|---|---|---|---|
| prod 테이블 수 | 17개 | **17개** (`information_schema` 조회) | ✅ |
| 결정 기록 최신 날짜 | 2026-08-17 | `DECISIONS.md`에 **2026-08-18행 2건** 존재 | ❌ **울림** |
| 갱신일 이후 코드 변경 | "있으면 의심" | 8-17 이후 커밋 다수(`41ef82f`·`2df15da`·`0fdd685` …) | ❌ **울림** |

1차와 같은 2개가 울린 채다. **울린 뒤 사흘째 아무도 안 껐다.** 게다가 그 사이 프로필이 반영 못 한 것이 늘었다 — BOOK_ASK 승인(N3), D-7 서버 강제(`payment.ts`·`transfer` 라우트), 온보딩 게이트(`lib/onboarding.ts`), `/api/admin/announcements`·`/api/admin/books`.

---

## 확인했으나 어긋남이 아니었던 것 — 재조사 방지용

같은 곳을 다시 파지 않도록 남긴다. prod는 **읽기 조회만** 했다(공통규약 §6).

| 확인한 것 | 결과 |
|---|---|
| `app_notifications` CHECK에 `announcement`가 있나 | **있다.** `migration-announcement-type.sql` prod 실행 완료 → 런북 6번(알림함 공지 발송)은 안전. *단 실행 사실이 어느 문서에도 안 적혀 있다* |
| `notifications` CHECK 실제 목록 | 6종(`…, book_ask`) — **`new_member_welcome` 없음.** 대조표 "book-ask SQL 실행 완료" 참 · 런북 "welcome SQL 당일 실행" 참 |
| `site_settings`에 `next_ui`·`library_enabled`·`notice_text` 행이 있나 | **행 자체가 없다.** 코드가 `=== 'on'`으로 읽어 OFF와 동일. 문서의 "현재 꺼짐" 서술과 어긋나지 않고, 켜기는 UPSERT라 문제없다 |
| `meetings.book_id`·`selection_reason`·`books.description` | 모두 존재(표지 배치 결정 적용됨). `host_id`는 **없음** → "진행자 노출 미적용" 서술과 일치 |
| `/api/books/search`가 `library_enabled` 뒤에 잠겨 있나 | **아니다.** 로그인 게이트만. 플래그 OFF 상태에서도 책 연결 가능 → 런북 사전조건에 순환 없음 |
| 번개 초안 `:14` "`flash_opened`·`flash_cancelled` 이미 예약됨" | **참.** `types/app-notification.ts:5-6` + prod CHECK 양쪽 확인 |
| 저장소 전체 마크다운 링크 | 1차와 동일 — 깨진 `[..](..)` 링크 0건 |

---

## 내가 하지 않은 것과 이유

- **아무것도 고치지 않았다.** 부서 정의 §6 — 어긋남만 보고한다. N1·N2·N4·N10·N11은 개발·운영, N3·N7·N9는 제품기획, N12는 성장/제품기획 소관이다
- **`DECISIONS.md`의 `🔨 미적용` 경과일을 갱신하지 않았다** — 제품기획이 같은 파일의 상태 칸을 고치는 중이라 관리자 지시로 생략
- **오늘 작업 중인 5개 파일을 어긋남으로 보고하지 않았다** — `DECISIONS.md`·`2026-08-21-미적용-우선순위.md`·`2026-08-21-켜는날-코드준비-판정.md`·`VOICE.md`·`DESIGN_TOKENS.md`. 관측된 사실만 참고로 남긴다: `DECISIONS.md`에 상태 표(`✅/🔨/⏸/🚫`)에 없는 **`🌑 다크 배포`** 표기가 등장했다 — 범례 갱신이 함께 필요한지는 제품기획 판단
- **옵시디언 보관소를 다시 훑지 않았다** — 지시대로
- **`docs/expert-panel/2026-08-19-…` 패널의 합의 ④("거래정보성 알림톡에 홍보 문구를 섞지 않는다")가 `DECISIONS.md`에 없다는 점은 항목으로 세지 않았다** — 그 파일이 지금 갱신 중이라 지금 상태로 판정하면 오탐이 된다. 제품기획이 오늘 작업을 마친 뒤 확인 대상
- **Vercel·Solapi 콘솔은 못 봤다.** N10(`KAKAO_REST_API_KEY` prod 설정 여부)·N3(승인 상태의 콘솔 실물)은 **미확인**이다. N3은 런북에 적힌 템플릿 ID까지 근거로 있어 신뢰도가 높지만, 콘솔 확인은 여전히 사람만 할 수 있다
- **prod에 쓰지 않았다.** 조회 6건 전부 `SELECT`. 대상 ref는 `jidokhae-web/.env.local`의 `NEXT_PUBLIC_SUPABASE_URL`과 대조해 `ycqqzzvyixvtdorjxkrn` 하나임을 먼저 확인했다
