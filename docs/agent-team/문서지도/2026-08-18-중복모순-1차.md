# 문서 중복·모순 1차 조사 (2026-08-18)

> 부서: 문서총괄(사서) · 첫 소환
> 대상: 저장소 `C:\jidokhae-2nd` (`.claude/worktrees/` 제외) + 옵시디언 개인 보관소 `Documents\Sync` (읽기 전용)
> ⚠️ `Documents\jidokhae-doce`는 저장소를 가리키는 링크 모음이라 중복으로 세지 않았다.

**요약: 19건 — 🔴 5 · 🟡 11 · 🟢 3**

---

## ⚠️ 먼저 알아야 할 것 — 작업 트리가 `main`보다 뒤처져 있다

조사 중 발견한 전제 조건이다. 이걸 모르면 아래 항목들의 근거를 재현할 수 없다.

현재 체크아웃된 브랜치는 `feat/agent-team`이고, 이 브랜치는 PR #56·#57이 머지되기 **전**에서 갈라졌다. 그 결과:

| 파일 | 작업 트리 | `main` |
|---|---|---|
| `검토문서/DECISIONS.md` | 최신 행 = **2026-08-17** | 최신 행 = **2026-08-18** (4행 더 있음) |
| `검토문서/2026-08-18-켜는날-런북.md` | **없음** | 있음 (102행) |
| `검토문서/README.md` | 2026-08-18 갱신판(완전) | 구판(6개만) |
| `verification-squad/profiles/jidokhae-profile.md` | 2026-08-17 갱신판 | 2026-08-14 구판 |

**양쪽이 서로 다른 파일에서 더 새롭다(교차 상태).** 지금 이 트리에서 `DECISIONS.md`를 읽는 부서는 **2026-08-18 결정 4건(공지 3종 A+C+D, 알림함 전체 페이지 유지, 책 표지 배치 확정, "할 일" 워딩)을 못 본다.** `DECISIONS.md`가 존재하는 이유가 정확히 그것을 막는 것이므로 심각도를 🟡로 둔다.

아래 항목의 근거는 명시가 없으면 **`main` 기준**이다.

---

## 🔴 심각 — 돈·발송·회원 노출에 직접 닿는다

### 1. 켜는 날 런북이 "env만 바꾸면 된다"고 하는데, 대조표는 "코드와 같이 나가야 한다"고 한다

| 어느 파일 몇 행 | 무엇이라고 적혀 있나 |
|---|---|
| `검토문서/2026-08-18-켜는날-런북.md:38` (main) | 당일 실행 1번 = "알림톡 템플릿 env 6개를 V2로 교체 + 재배포" · **"코드 수정 없음"** |
| `검토문서/2026-08-13-알림톡-변수-대조표-및-배포-순서.md:64` | 4번 = "PR #50 → PR #49 순서로 머지" · **"3과 4는 같이 나가야 한다. env만 바꾸면 구 코드가 `#{결제상태}`를 안 보내고, 코드만 배포하면 구 템플릿에 변수가 없다"** |

**실측:**
- `git grep "결제상태" main -- jidokhae-web/src` → **0건.** `#{결제상태}`를 보내는 코드는 main에 없다
- `jidokhae-web/src/lib/notification.ts:293-297` (main) — 미승격 환불이 보내는 변수는 `#{회원명}` `#{모임명}` `#{결제금액}` **3개뿐.** 대조표 39행이 명시한 `#{모임일시}`가 없다
- `gh pr list` → **PR #49·#50 둘 다 아직 DRAFT** (2026-08-13 생성, 5일째)
- 런북 전문에 `#49` `#50` `머지` `결제상태` 어느 것도 **한 번도 나오지 않는다**

**어느 쪽이 최신인가:** 런북이 5일 더 최신(08-18 vs 08-13)이다. 그러나 **최신 쪽이 틀렸다.** 대조표의 주장은 코드로 검증되고(위 grep), 런북의 "코드 수정 없음"은 검증되지 않는다. 같은 실패 모양이 이미 한 번 있었다 — 3~6월 알림톡 123건 실패(치환 오류 → 발송 통째 실패).

**심각도 🔴** — 런북은 "이 문서 하나로 결정→실행→점검→롤백까지 끝낸다"고 스스로 선언한 문서다. 그대로 따르면 리마인드·대기 승격·미승격 환불 3종이 켜는 날 동시에 죽는다.

> 부수: 런북 §1-1 ID 표에 `SOLAPI_TEMPLATE_BOOK_ASK` 행이 없다. 대조표 3번은 그 키를 함께 교체하라고 한다. 또 "env 6개"라고 쓰고 표에는 `SOLAPI_TEMPLATE_*` 5개 + 가입 환영 1개가 실려 있어 세는 방법이 안 맞는다.

---

### 2. `VOICE.md`가 아직 "호칭 미정"이라고 적혀 있다 — 결정된 지 11일째

| 어느 파일 몇 행 | 무엇 |
|---|---|
| `jidokhae-web/VOICE.md:17` | 상태표 — `호칭` \| **미정** — 6장 참조 |
| `jidokhae-web/VOICE.md:331-333` | §6 호칭 — 후보 `OO님` → **미정** |
| `검토문서/DECISIONS.md` 2026-08-07행 | **"호칭은 전 채널 닉네임.** `real_name \|\| nickname`을 `nickname \|\| real_name`으로 뒤집는다" · 🔨 **미적용** |

**어느 쪽이 최신인가:** DECISIONS가 최신이다. 결정일(08-07) 이후에 `VOICE.md`가 **두 번 개정**됐는데(v0.4 08-11, v0.5 08-12 — 변경 이력 표) 그때도 이 줄은 그대로 남았다. 즉 "아직 못 옮겼다"가 아니라 **손대면서도 지나쳤다.**

코드도 미적용 확인 — `real_name || nickname`이 main에 5곳:
`lib/notification.ts:164, 209, 254, 284` · `api/cron/meeting-remind/route.ts:96`

**심각도 🔴** — 이건 새 문제가 아니라 **DECISIONS.md를 만들게 한 바로 그 사고의 재발이다.** `DECISIONS.md:5`가 기록한 원 사고 문장이 그대로 다시 성립한다: *"같은 시기 VOICE.md는 여전히 '미정'이라고 적혀 있었다."* 11일 지났고 아직 그대로다.

---

### 3. `project_next_session.md`가 두 벌 있고, 서로 정반대를 말한다

| 경로 | 날짜 | 줄 수 | 내용 |
|---|---|---|---|
| `C:\jidokhae-2nd\memory\project_next_session.md` | **2026-08-13** | 118 | 열린 PR 5건(#49~#53) + "머지 순서: #51·#52 → #49 → #53 → #50" |
| `C:\Users\ytyt8\.claude\projects\C--jidokhae-2nd\memory\project_next_session.md` | **2026-08-18** | 59 | "전면개편 1단계 전부 main 머지, 플래그 OFF" + PR #54~#57 완료 |

`.claude` 쪽이 최신이다(frontmatter `modified: 2026-08-18T11:25:50Z`, PR #56·#57 언급 — git으로 확인됨: `38dff5f`, `b362713`).

저장소 쪽(08-13)이 지금 말하고 있는 것 중 **이미 틀린 것**:
- `:22` PR #52 "머지 가능" → 이미 머지됨(`612b33a`)
- `:21` PR #51 "머지 가능" → 이미 머지됨(`8c0d5ce`)
- `:27` "머지 순서: #51·#52 → #49 → #53 → #50" → 앞 둘은 끝났고 뒤 셋은 5일째 draft
- `:15` "알림톡 템플릿 7건 — 검수 요청 대기(전부 PENDING)" → 실제로는 **V2 6종 APPROVED, BOOK_ASK만 INSPECTING**(08-17 Solapi API 실측)
- `:58` "회원 250명이 보는 문구라" → 공통규약 §7 위반(아래 18번)

**심각도 🔴** — 같은 이름·같은 목적의 파일이 두 곳에 살아 있고, `CLAUDE.md`는 `/memory`를 저장소 디렉토리로 문서화하는데(문서 구조 도표) **실제로 세션에 자동 주입되는 것은 `.claude` 쪽**이다. 어느 쪽이 진짜인지 문서 어디에도 안 적혀 있다.

> 같은 구조로 `MEMORY.md`도 두 벌이다. 저장소 쪽은 16행짜리 인덱스(feedback 10개 + project 1개), `.claude` 쪽은 137행짜리 전체 상태 기록. 저장소 `memory/`에는 파일이 11개, `.claude` 쪽에는 **65개**다. 저장소 쪽 인덱스가 가리키지 않는 파일이 54개 있다.

---

### 4. `DECISIONS.md`의 상태 칸이 낡았다 — "배포 대기"라고 적힌 것들이 이미 prod에 들어가 있다

`DECISIONS.md`는 스스로 *"`상태`를 반드시 채운다. '정했다'와 '적용했다'는 다르다. **대부분의 사고는 그 사이에서 난다**"*(`:10`)고 선언한 파일이다. 그 칸이 낡았다.

| 행 (main 기준) | 상태 칸 | 실제 |
|---|---|---|
| `:27` 개편 공지 A+C+D | `🔨 구현됨 — feat/launch-notices` | PR #56 머지 완료 `38dff5f`, **prod 배포 확인됨** |
| `:29` 책 표지 배치 확정 | `🔨 구현됨 — feat/book-covers **PR 대기**` | PR #57 머지 완료 `b362713` |
| `:32` 발제 관리 권한 | `🔨 구현됨 — feat/next-phase1a **배포 대기**` | PR #54 머지 `b57aee0`, prod 반입(플래그 OFF) |
| `:33` 토론 D-7 통일 | 〃 **배포 대기** | 〃 |
| `:35` 토스 스킨 | 〃 | 〃 |
| `:36` 탭 5개 | 〃 | 〃 |
| `:37` 토론 진입점 = 토론 탭 | 〃 | 〃 |
| `:57` 토론 환불 7/3일 | 〃 **배포 대기** | 〃 |

같은 낡음이 `verification-squad/profiles/jidokhae-profile.md:55`(환불 토론 "배포 대기")와 `:132`(알려진 미해결 표)에도 복사돼 있다 — **한쪽만 갱신될 운명의 복붙.**

반대 방향 오류도 있다:

| 행 | 상태 | 실제 |
|---|---|---|
| `DECISIONS.md:58` (2026-08-14) 토론 상세 순서 = 표지 → 선정 이유 → 책 소개 → 정보 → 환불 → 신청 | `🔨 **미적용**` | **구현·머지 완료.** `components/meetings/BookSection.tsx`, `BookIntro.tsx`, `MeetingDetailContent.tsx`가 `selection_reason`·`books.description`을 렌더 (PR #57) |
| `DECISIONS.md:60` (2026-08-14) 토론 참여 자격을 `서비스 개요` §7-2에서 개정 | `🔨 미적용` | **문서 쪽은 이미 개정됨** — `core/JIDOKHAE-2nd - 서비스 개요.md` v1.7(2026-08-14) 변경 이력에 "7-2 참여 자격을 '6개월 이내' → '1회 이상(평생)'으로 개정" 기재. 코드 게이팅만 미적용 |

**심각도 🔴** — 미적용을 적용으로 잘못 읽는 것보다, **적용을 미적용으로 잘못 읽어 두 번 만드는 쪽**이 이 팀 규모에서는 더 자주 일어난다. 두 방향 다 발생 중이다.

---

### 5. prod에 실행했다고 기록된 마이그레이션 파일이 저장소에 없다

| 참조 위치 | 참조하는 파일 | 저장소 존재 |
|---|---|---|
| `memory/project_next_session.md:53` — "✅ 2026-08-13 완료 (Claude가 MCP로 prod 실행)" | `migration-book-ask.sql` | **없음** (작업 트리·`main` 양쪽 모두) |
| `검토문서/2026-08-13-알림톡-변수-대조표-및-배포-순서.md:62` — "✅ 실행 완료" | 〃 | **없음** |
| 〃 `:87` 롤백 절차 | `migration-book-ask-rollback.sql` | **없음** |
| `memory/project_next_session.md:56` — "[단무지님] 실행" | `migration-welcome-notification.sql` | **없음** |

`git ls-tree -r main`으로 `jidokhae-web/supabase/` 전수 확인 — 33개 SQL 중 위 3개는 없다. 두 파일은 미머지 브랜치(`feat/book-ask-cron`, `feat/welcome-alimtalk`)에만 있다.

이 프로젝트의 자기 교훈 `feedback_prod_sql_must_commit`("prod에 적용한 SQL은 반드시 git에 commit해 차분·추적 가능하게")의 정면 위반이다. 품질 부서가 2026-08-14 리포트 C2로 이미 지적했고(`verification-squad/reports/2026-08-14-benchmark/07-consistency-report.md:43-56`), **4일 지나 그대로다.**

**심각도 🔴** — prod 스키마의 단일 소스(`supabase/migration.sql`)가 prod를 반영하지 않는다. `migration.sql:503-509`의 `notifications.type` CHECK는 5종인데, main의 `lib/notification-log.ts:20-28`은 7종 라벨을 들고 있고, `migration-announcement-type.sql`이 8번째(`announcement`)를 추가한다.

---

## 🟡 보통

### 6. `CLAUDE.md`에 "Current status" 블록이 두 개고, 앞쪽이 낡았다

- `CLAUDE.md:94` — "**Phase 3 (진행 중)** … M7 Step 3 (회원 홈) **예정**"
- `CLAUDE.md:96-102` — "**Current status (2026-07-28 갱신):** M7 Step 3 이후 로드맵보다 토론모임 개편 + 2535 리디자인이 먼저 진행됨 … 전면개편 1단계 ✅ 머지"

같은 제목의 두 블록이 붙어 있고, 뒤쪽이 앞쪽을 부정한다. **앞쪽이 최신이라고 오해할 여지가 크다** — 굵게 표시된 "Phase 3 (진행 중)"이 위에 있기 때문이다. 최신은 뒤쪽(`:101`이 PR #54/#55를 언급 → 2026-08-17 이후 갱신).

### 7. `CLAUDE.md`의 Out of scope가 결정·구현과 어긋난다

`CLAUDE.md:130` — "**Out of scope (future):** Badges/praise, bean (콩) points, landing page, admin analytics dashboard, AI chatbot, **book tracking**"

- **Badges** — `DECISIONS.md`(2026-08-14) "**배지 · 연속 참석 스트릭 도입.** `서비스 개요` Out of scope("Badges/praise")를 뒤집는다" (🔨 미적용, 4일)
- **book tracking** — 이미 만들어져 있다. `books` `library_entries` `book_asks` 3테이블 + `/admin/library` + `lib/library.ts` + 완독 표시(`api/library/complete`)

같은 문장이 `core/JIDOKHAE-2nd - 서비스 개요.md:55, 78, 231, 261` 네 곳에 흩어져 있어, 고치려면 다섯 군데를 동시에 손대야 한다. `DECISIONS.md` "아직 정하지 않은 것"의 "코어 문서 전면 정비" 항목이 이걸 가리킨다 — 인지는 돼 있으나 **어느 문서 몇 행인지는 아무 데도 안 적혀 있다.**

### 8. `CLAUDE.md`가 알림톡을 아직 "5종"으로 센다

| 위치 | 기재 | 실제 |
|---|---|---|
| `CLAUDE.md:179` | "Solapi SDK → KakaoTalk 알림톡. **5종**" | 7종 (+ `book_ask`, `new_member_welcome`) |
| `CLAUDE.md:378` | "**알림톡 5종:**" | 〃 |
| `CLAUDE.md:418` | env 템플릿 ID **5종** 나열 | 대조표는 7종 체계, 런북 §1-1은 6개 + BOOK_ASK |
| `jidokhae-web/.env.example` | 5종 | 〃 |

`verification-squad/profiles/jidokhae-profile.md:65`는 "**알림톡: 7종**"으로 이미 맞다. 프로필이 최신, `CLAUDE.md`가 낡음.

### 9. `CLAUDE.md`의 코드 목록이 실제의 절반 수준이다

| 항목 | `CLAUDE.md` 기재 | `main` 실제 | 근거 |
|---|---|---|---|
| `src/lib/*.ts` | `:348`에 **17개** | **38개** | `git ls-tree -r main` |
| API 라우트 | `:346`에 **22개** | **35개** | 〃 |
| DB 테이블 | `:320`에 **12개** | **17개** (+`books` `library_entries` `book_asks` `discussion_topics` `topic_answers` `answer_replies` `answer_reactions` `app_notifications`, 표기상 12→17) | 프로필 `:59`와 마이그레이션 대조 |
| admin 라우트 | `:306`에 10개 | **14개** (+`notifications` `library` `meetings/[id]/topics` `notices`) | 〃 |
| 회원 라우트 | `(main)` 6개 | + `(next)` **8개** 라우트 그룹 전체 누락 | 〃 |

`CLAUDE.md:103`이 "아래 문서의 라우트·컴포넌트·스키마 목록에는 서재/물어보기가 아직 미반영"이라고 자백해 두었으나, **그 경고가 커버하지 못하는 것이 더 많다** — `(next)` 5탭, 발제 스레드 5테이블, `app_notifications`, `/admin/notices`, 책 표지 컬럼은 경고 문장에 포함돼 있지 않다.

`registration-status.ts`·`notification-log.ts` 누락은 품질 부서가 W12-c로 이미 지적(2026-08-14), 4일째 그대로.

### 10. 두 `CLAUDE.md`가 배너 권한에 대해 서로 다른 말을 한다

| 위치 | 기재 |
|---|---|
| `CLAUDE.md:208` (루트) | "adminOnly 메뉴: 정산, 회원 개인정보, 사이트 설정 (**배너·한줄은 editor도 가능**)" |
| `jidokhae-web/CLAUDE.md:85` | "`adminOnly: true`는 editor 역할에서 숨김(**정산/배너/설정**)" |

**코드가 루트 쪽 손을 들어준다.** `src/components/admin/adminMenu.ts`에서 `adminOnly: true`인 것은 `:25` 정산 · `:42` 알림톡 이력 · `:43` 설정 **3개뿐이고 배너는 아니다.**

같은 줄(`jidokhae-web/CLAUDE.md:85`)에 "**7개 메뉴**"라고도 적혀 있는데 실제는 **9개**다(운영 5 + 콘텐츠 2 + 시스템 2). 품질 부서 W12-b로 이미 지적됨(2026-08-14), 4일째 그대로.

### 11. `DESIGN_TOKENS.md`에 토스 스킨 토큰이 한 줄도 없다

`jidokhae-web/DESIGN_TOKENS.md:3` — *"「잉크 그린 × 에디토리얼」 — 2535 리디자인(2026-07-07)"*, `:5` — *"**All tokens** are defined in `src/app/globals.css`"*, `:64` — Noto Serif KR = 브랜드명·페이지 제목

그런데 `globals.css`에는 문서화되지 않은 토큰군이 이미 들어 있다:
- `:112` `--color-tg-100: #F2F4F6` (토스 그레이 스케일)
- `:121` `--color-brand: #0FA873`
- `:126` `--shadow-toss-card`

그리고 `DECISIONS.md`(2026-08-16, 🔴 표시)는 *"회원 화면 스킨 = 토스 문법 × 지독해 그린 … ⚠️ **세리프(Noto Serif KR) 회원 화면에서 퇴장**"*으로 확정했다.

**심각한 이유:** `docs/agent-team/안내판-라우팅표.md:14`가 시안 작업의 시작 전 필독으로 `DESIGN_TOKENS.md`를 지정한다. 그 문서만 읽은 디자인 부서는 **퇴장이 확정된 세리프를 회원 화면에 다시 넣는다.**

### 12. 로드맵 M9가 이미 지나간 계획을 현재형으로 적고 있고, `/home`이 두 가지 뜻으로 쓰인다

- `roadmap/milestones-phase3.md:32` — "M9 회원 홈 콘텐츠 전면 오픈 … 홈 **4섹션**(배너/D-Day/한 줄/소셜 프루프)"
- `roadmap/milestones-phase3.md:80` — "홈 `/home`은 **placeholder 상태** (섹션은 M9에서 채움)"
- `DECISIONS.md`(2026-08-15, 🔴) — "**탭 5개 — 홈·모임·토론·서재·나.** 홈의 규칙: **목록을 두지 않는다.** 홈에는 '지금 내가 할 수 있는 일'만" → 구현·머지 완료

`/home`은 지금 `src/app/(next)/home/page.tsx`로 **완성된 신규 홈**이다. 로드맵을 믿는 사람은 같은 경로를 빈 껍데기로 안다.

`.claude` 쪽 `project_next_session.md:46`이 "로드맵 문서 갱신(M8·M9 대체)"을 후속 대기열에 올려두었다 — **인지돼 있으나 로드맵 문서 자체에는 아무 표시가 없다.**

### 13. 옵시디언 칸반의 "진행 중"이 3개월 방치돼 있다 — *관리자 짐작 후보 ①, 맞음*

`C:\Users\ytyt8\Documents\Sync\310. Project\jidokhae-2nd.md` (**최종 수정 2026-05-07**)

```
## 진행 중
- [ ] 알림톡 승인 대기
- [ ] 모임 신청자 확인 기능
```

`memory/project_next_session.md`(.claude, 08-18)의 다음 할 일:
1. BOOK_ASK 심사 결과 확인 → 2. 켜는 날 실행 목록(env 6개 V2 교체 / 플래그 2개 flip / 직후 점검 / GA4) → 3. 후속 대기열 → 4. 2단계 번개

**대조 결과:**
- "알림톡 승인 대기" — 글자만 보면 지금도 맞다(BOOK_ASK INSPECTING). 다만 2026-05-07에 적힌 "승인 대기"는 그때의 다른 건이고, **우연히 같은 말이 계속 참인 상태**다. 칸반이 살아 있다는 증거가 아니다
- "모임 신청자 확인 기능" — **저장소 어디에도 대응하는 진행 중 작업이 없다.** 신청자 확인은 `/admin/meetings/[id]` 신청자 목록(M3)과 회원 명단 RPC `get_meeting_participant_nicknames`(피드백 반영분)로 이미 오래전에 나갔다
- **반대로 칸반에 없는 것:** 전면개편 5탭·토스 스킨·발제 스레드·책 표지·켜는 날 게이트 — 지난 두 달의 작업 전부

**어느 쪽이 최신인가:** 저장소가 압도적으로 최신(칸반 mtime 2026-05-07 vs 저장소 커밋 2026-08-18). 칸반은 **버려진 보드**로 보이며, 살릴지 폐기할지 결정이 필요하다. 지금 상태로는 "진행 중"을 믿는 사람을 3개월 전으로 보낸다.

### 14. 보관소의 모임 구조·수익 모델이 저장소와 완전히 다른 세계다 — *관리자 짐작 후보 ②, 맞음 (그리고 짐작보다 크다)*

**14-a. `320. Area\지독해\지독해 모임 구분.md` (2026-04-15) ↔ `meetings.meeting_type`**

| | 보관소 | 저장소 |
|---|---|---|
| 모임 유형 | **5종** — 하프살롱 / 딥살롱 / 북살롱 / 시즌 파티 / 팝업 살롱 | **2종** — `regular` / `discussion` |
| 근거 | 노트 §각 절 | `migration-meeting-type.sql:35` `CHECK (meeting_type IN ('regular','discussion'))` |
| 참여 자격 | 하프살롱만 누구나, 나머지 **멤버십 전용** | 멤버십 개념 없음 |

검증: `grep -rn "하프살롱\|딥살롱\|북살롱\|팝업 살롱\|시즌 파티" --include=*.md --include=*.ts --include=*.tsx` → **저장소 전체 0건**. `멤버십`도 **0건**.

노트의 취지 자체가 저장소 방향과 충돌한다 — *"기존 정기모임과 같은 워딩은 사업적 단어에서 위험군 … 새로운 워딩이 필요"*. 저장소는 지금도 "정기모임"을 회원 화면 전면에 쓴다.

> **가장 아픈 지점:** `DECISIONS.md`(2026-08-14, 🔴)는 *"**번개모임이 실제 운영 중이다**(단무지님 확인) … **우리 스펙 어디에도 없었다. 정보 구조를 다시 짜야 한다**"*고 적었다. 그런데 이 보관소 노트는 **넉 달 전인 2026-04-15에 이미** 「팝업 살롱 — 회원들의 교류를 위한 교류 활동 모임 … **기존 번개모임과 같음** … 누구나 운영 … 우제님의 배드민턴·러닝 벙, 레고님의 책빵클럽, 강물님의 강독회」까지 담당자 이름과 함께 적어두었다. **정보가 없었던 게 아니라 저장소와 보관소가 연결돼 있지 않았다.**

**14-b. `320. Area\지독해\지독해 멤버십 구조 재설계 — MECE 정리.md` (2026-04-17) — 저장소에 그림자조차 없다**

이 노트는 완성된 사업 설계다: 시즌패스 90,000원/3개월 · 4회권 50,000원 · 단건 15,000원 · 파운더 패스 70,000원 · 시즌제(1기 7~9월) · 체크인 차감 · **"환불 없음"** · brainy-club.com 통합 · MVP 기능 5개(상품 페이지·결제·멤버십 관리·체크인·알림톡) · 일정표(5월 4주~6월 2주 시스템 개발, 7월 1주 선판매).

저장소와 정면으로 부딪히는 것:

| 사안 | 보관소 (2026-04-17) | 저장소 |
|---|---|---|
| 판매 단위 | 시즌권/회수권 | 모임 1건당 결제 (`meetings.fee`) |
| 환불 | **없음** | 정기 3일 100%/2일 50% · 토론 7일 100%/3일 50% (`DECISIONS.md`, `lib/refund.ts`) |
| 단건 가격 | 15,000 | 토론 25,000 확정(`DECISIONS.md` 2026-08-14), 정기는 모임별 |
| 체크인 | 필수 기능(QR 추천) | `registrations.attended` **의도적 미사용** — "참여 = 결제완료 + 모임일 경과"(프로필 §의도된 설계) |
| 로드맵 | 6월 개발 → 7월 선판매 | 로드맵 M7~M12 어디에도 없음 |

**어느 쪽이 최신인가:** 저장소다(보관소 mtime 2026-04-17, 이후 저장소는 완전히 다른 방향으로 넉 달 진행). 다만 **"이 계획은 폐기됐다"고 적힌 곳이 어디에도 없다.** `DECISIONS.md`의 `🚫 안 함`이 딱 이런 것을 막으려고 만든 칸인데 이 건이 들어 있지 않다.

**14-c. 회원 규모를 보관소와 저장소가 다르게 센다**

보관소 `:60-63` — *"전체 등록 250명 / **활성 고유 멤버 약 30명**(경주 ~15, 포항 ~15) / 월 연인원 경주 40 포항 40"*
저장소 — `CLAUDE.md:243` *"회원 변화 — 회원 화면 변경(**250명 노출**, 가장 신중)"*

**배포 신중도의 근거 숫자가 실제의 8배다.** 이건 단순 오탈자가 아니라 판단 기준이라 따로 적는다. (숫자를 문서에 박은 것 자체는 아래 18번)

### 15. `검토문서/README.md`가 자기 규칙을 만든 날 이미 어겼다

`검토문서/README.md:5` (작업 트리, 2026-08-18 갱신판) — *"이전에는 14개 중 6개만 실려 있었다 … **불완전한 인덱스는 없는 것보다 나쁘다.** 파일을 추가하면 이 목록도 **그 자리에서** 갱신할 것"*
`:51` — *"파일을 추가하면 위 목록에 그 자리에서 한 줄 넣는다. 나중에 하겠다고 미루면 안 한다 — **실제로 8건이 그렇게 빠졌다**"*

같은 날 `main`에 `검토문서/2026-08-18-켜는날-런북.md`가 추가됐는데 **인덱스에 없다.** (두 브랜치 어느 쪽에서 봐도 없다 — 갱신판 README는 이 브랜치에, 런북은 main에 있어 서로 못 만난다.)

`main` 기준으로는 더 나쁘다 — main의 README는 구판이라 `DECISIONS.md`·알림톡 6건·런북까지 **8개가 빠진 상태**다.

### 16. 검증 프로필이 자기 낡음 감지 장치를 이미 울렸다

`verification-squad/profiles/jidokhae-profile.md:7-16`은 *"이 프로필이 낡았는지 30초 안에 확인하는 법 … **하나라도 어긋나면 작업을 멈추고 관리자에게 보고한다**"*로 3개 트립와이어를 걸어뒀다.

| 확인 | 프로필이 적은 값 | 실제 | 판정 |
|---|---|---|---|
| prod 테이블 수 | 17개 | 17개 (마이그레이션 대조) | ✅ |
| 결정 기록 최신 날짜 | **2026-08-17** | **2026-08-18** (main) | ❌ **울림** |
| 갱신일 이후 코드 변경 | "있으면 의심" | `41ef82f` `2df15da` 등 **있음** | ❌ **울림** |

공통규약 §8이 "기준 문서가 낡았으면 멈추고 보고한다"고 정했으므로 **규약대로 보고한다.** 프로필이 반영하지 못한 것: 공지 3종(예고 스트립·왓츠뉴 시트·알림함 공지), `/admin/notices`, `announcement` 알림 타입, 책 표지 연결(`meetings.book_id`·`selection_reason`·`books.description`), `NoticeStrip` 컴포넌트.

> 장치 자체는 잘 작동했다 — 4일 만에 낡음을 잡아냈다. 문제는 울린 뒤 아무도 안 껐다는 것이다.

---

## 🟢 낮음

### 17. 죽은 참조 — 마크다운 링크는 0건, 백틱 경로만 4건

저장소 전체 `.md`의 마크다운 링크 `[..](..)` **전수 검사 → 깨진 링크 0건.** (스크립트 검사, `.claude/worktrees/`·`node_modules` 제외)

백틱으로 적힌 파일 경로 중 실제로 없는 것:

| 어느 파일 몇 행 | 가리키는 것 | 실제 |
|---|---|---|
| `roadmap/work-packages-phase3.md:130` | `src/components/admin/Sidebar.tsx` | 실제 이름은 **`AdminSidebar.tsx`** |
| `docs/expert-panel/2026-04-27-mobile-homescreen-icon-quality.md:9` | `manifest.json` | Next.js 방식이라 **`src/app/manifest.ts`** |
| `docs/superpowers/plans/2026-04-28-pwa-service-worker.md:41` | `jidokhae-web/src/components/ServiceWorkerRegister.tsx` | 없음 (계획 문서 — 미구현 또는 제거) |
| `docs/superpowers/plans/2026-07-12-…-1단계A-….md:44` | `src/components/skeletons/LibrarySkeleton.tsx` | 없음 |

앞 둘은 현행 문서라 고칠 값어치가 있고, 뒤 둘은 지나간 계획 문서라 그대로 둬도 무해하다.

(5번의 마이그레이션 3건도 죽은 참조지만 성격이 달라 🔴로 따로 뒀다.)

### 18. 변하는 값이 문서에 숫자로 박혀 있다 — 공통규약 §7 위반

규약이 예시로 든 문장(`docs/agent-team/공통규약.md:153` *"문서에 `회원 250명`이라고 적으면…"*)이 **실제 위반 사례를 그대로 인용한 것**인데, 원본이 아직 살아 있다.

| 어느 파일 몇 행 |
|---|
| `CLAUDE.md:9` — "a reading club in Gyeongju/Pohang, Korea (**250 members**)" |
| `CLAUDE.md:205` — "현 규모(**250명**, 한국 국내)에서 법적 의무 아님" |
| `CLAUDE.md:243` — "회원 화면 변경 (**250명** 노출, 가장 신중)" |
| `memory/project_next_session.md:58` — "회원 **250명**이 보는 문구라" |
| `core/JIDOKHAE-2nd - PRD.md:13, 301` — "**250명** 규모" |
| `검토문서/Phase-2-3-백오피스-지시서.md:265-267, 446, 499, 653, 658` — 7곳 |

프로필(`:68`)은 이미 규약대로 고쳐져 있다 — *"회원 수는 계속 변하므로 숫자를 여기 적지 않는다 — 필요하면 `profiles` 테이블을 직접 센다"*. **한 팀 안에서 같은 사안을 두 방식으로 적고 있다.**

지시서·PRD처럼 지나간 문서는 역사 기록이라 그대로 둬도 된다고 본다. 다만 `CLAUDE.md`는 매 세션 주입되는 현행 문서라 다르다. (14-c에서 봤듯 그 숫자는 활성 규모와 8배 차이 난다.)

### 19. `docs/agent-team/안내판-라우팅표.md`가 자기를 "초안"이라고 적어두고 필독으로 지정돼 있다

`:3` — *"**초안입니다.** 단무지님이 실제로 자주 하시는 요청을 기준으로 고쳐야 완성됩니다"*
`:6` — *"이 표를 건너뛰고 작업에 들어가지 않는다 … ⭐ 가장 중요"*

한 문서가 "믿지 말라"와 "반드시 따르라"를 동시에 말한다. 작동에 문제는 없으나, 초안 딱지를 언제 뗄지가 정해져 있지 않으면 영구히 초안으로 남는다. 심각도는 낮게 둔다.

---

## `DECISIONS.md` 🔨 미적용 경과일 (2026-08-18 기준, `main`)

**미적용 19건.** 가장 오래된 것이 33일째다.

| 결정일 | 건수 | 경과 | 내용 |
|---|:--:|:--:|---|
| 2026-07-16 | 1 | **33일** | 물어보기 주 진입점 = 모임 다음날 알림톡 1발 (PR #49 **draft**) |
| 2026-08-07 | 1 | **11일** | 🔴 호칭 전 채널 닉네임 (위 2번 — 코드 5곳 그대로) |
| 2026-08-13 | 2 | **5일** | 가입 환영 알림톡 발송 시점 / 알림 종류 union을 `types/notification.ts`로 통합 (둘 다 PR #53 **draft**) |
| 2026-08-14 | 14 | **4일** | 배지·스트릭 · 누적 카운터 3종 · 번개 3건(운영 중·자격 3개월·정보 구조) · 토론 6건(정원 10명·책 주문 마감·10% 할인·서재 담김 체감·플래그+지표·운영자 입력 최소화) · 완독 재촉 금지(개정) · 진행자 `host_id` 노출 |

**검증한 것:**
- 호칭 → `real_name || nickname`이 main에 **5곳 그대로** (`notification.ts:164,209,254,284` · `meeting-remind/route.ts:96`)
- 진행자 노출 → `git grep host_id main -- jidokhae-web` **0건.** 진짜 미적용 ✅
- 토론 상세 순서(`:58`) → **오분류.** 이미 구현·머지됨 (위 4번)
- PR #49·#50·#53 → `gh pr list`로 **셋 다 아직 draft** 확인

> 취소선 처리된 구버전 행(`~~2026-08-14~~` 완독 재촉 초판)도 상태 칸이 `🔨 미적용`으로 남아 있다. 뒤집힌 결정은 상태를 비우거나 `—`로 두는 편이 세기 편하다. (위 표의 19건에는 넣지 않았다.)

---

## 관리자가 짐작한 후보 2건 — 채점

| # | 짐작 | 판정 | 근거 |
|---|---|---|---|
| ① | 칸반 "진행 중" ↔ `project_next_session.md` 다음 할 일 | ✅ **맞음** | 칸반 최종 수정 **2026-05-07**, 3개월 방치. "모임 신청자 확인 기능"은 저장소에 대응 작업 없음(이미 출시된 기능). 지난 두 달 작업 전부가 칸반에 없음. **덧붙임:** `project_next_session.md`가 두 벌이라(위 3번) 대조 상대부터 정해야 한다 |
| ② | `지독해 모임 구분.md` ↔ `meetings.meeting_type` | ✅ **맞음, 짐작보다 큼** | 5종 vs 2종에 그치지 않고 **멤버십 제도 전체**가 저장소에 없다(`멤버십` grep 0건). 옆 노트 `멤버십 구조 재설계 MECE`(2026-04-17)는 가격·시즌·체크인까지 확정된 사업 설계인데 저장소에 흔적이 없고, **폐기 기록도 없다.** 게다가 그 노트가 "번개모임 = 팝업 살롱"을 4개월 먼저 적어뒀는데, 저장소는 2026-08-14에 "우리 스펙 어디에도 없었다"로 뒤늦게 발견했다 |

---

## 내가 하지 않은 것과 이유

- **아무것도 고치지 않았다.** 부서 정의 §6 — 문서총괄은 어긋남만 보고하고 무엇이 맞는지는 해당 부서가 정한다. 특히 1·2·4번은 각각 개발·운영 / 디자인 / 제품기획 소관이다
- **옵시디언 `Documents\Sync`는 읽기만 했다.** 개인 지식 체계이고 규약상 쓰기 금지
- **Solapi 콘솔의 V2 템플릿이 실제로 `#{결제상태}`를 요구하는지는 확인 못 했다** — 외부 SaaS 콘솔은 볼 수 없다. 1번은 "대조표가 그렇게 명시한다 + 코드에 그 토큰이 없다"까지만 검증된 상태다. **켜는 날 전에 콘솔에서 눈으로 확인 필요**
- **prod DB에 직접 질의하지 않았다.** 테이블 17개는 마이그레이션 파일 대조로 계산했다. `notifications.type` CHECK에 `book_ask`가 실제로 있는지는 여전히 미확인(품질 부서 2026-08-14 리포트 C2가 남긴 숙제 그대로)
- **`Sync` 보관소의 지독해 무관 노트는 열지 않았다** — Inbox에서 `지독해|brainy-club|jidokhae`로 걸러 10개만 확인
