# 「이제 없어도 되는 것」 전수 점호 (2026-09-19)

> 부서: 문서총괄(사서) · 네 번째 소환
> 범위: 저장소 문서 전부. 새 세션이 실제로 읽는 순서대로 — 루트 `CLAUDE.md` → `검토문서/` → `roadmap/` → `docs/superpowers/` → `verification-squad/` → 나머지
> 기준: 작업 트리 `feat/agent-team` `d4b8717` · 실측 시각 2026-09-19
> 제외: `.claude/worktrees/` 안의 저장소 사본(§H에서 폴더 자체만 다룸) · 옵시디언 개인 보관소 · 저장소 `memory/`(처분은 이미 정해져 있다 — 루트 `CLAUDE.md:67-74`)
>
> 🔴 **아무것도 지우지 않았다. 이 문서는 목록이다.** 실행은 대표님 확인 후 관리자가 한다.

---

## 요약

- **총 31건.** 처방별 — **삭제 7 / 정본으로 합치기 1 / 줄이기 7 / 과거 표시 8 / 보류 8**
- **파일 통째로 지워도 되는 것은 5개뿐이다** — `prompts` 1개 + 빈 폴더 4개. **마크다운 문서는 한 장도 통째 삭제 대상이 아니다.** 지울 값어치가 있는 건 문서가 아니라 **문서 안의 낡은 문장**이었다
- **가장 비싼 자리는 루트 `CLAUDE.md`다.** 65,315자가 매 세션 주입되는데 그중 **8,222자(12.6%)가 이미 끝난 켜는 날 절차 이야기**이고, 또 **4,886자가 "실제의 절반"이라고 스스로 적어둔 목록**이다. 둘을 줄이면 주입량의 **5분의 1**이 준다
- **가장 위험한 것은 학습서와 코어다** — 둘 다 결제사를 **토스페이먼츠**로 설명한다(실제는 PortOne V2, 2026-03 이관). 학습서는 **대표님이 직접 읽는 자리**이고, 코어는 `CLAUDE.md:78`이 *"authoritative"*라고 못박은 자리다
- **삭제로 판정한 7건은 전부 「당시 상태 서술」이고 「왜」를 하나도 담고 있지 않다.** 뒤집힌 결정·사고 조사·부서 로그는 한 건도 삭제로 올리지 않았다

---

## 목록

### A. 루트 `CLAUDE.md` — 매 세션 주입되는 가장 비싼 자리 (537줄 / 65,315자)

| # | 무엇 | 어디 (파일:줄) | 갈래 | 처방 | 근거 |
|:--:|---|---|---|---|---|
| 1 | **끝난 켜는 날 절차 이야기가 매 세션 8천 자를 차지한다.** 접혀 있지만 접어도 주입 비용은 그대로 든다 | `CLAUDE.md:121-172` (52줄 / **8,222자 = 파일의 12.6%**) | 낡음 + 중복 | **줄이기** — **남길 것 3문장**: *"명령이 실행되기 전까지 닫힌 것이 아니다"* · *"`merge-tree`로 스택 PR의 머지 가능성을 재지 않는다"* · *"위험한 것은 env 교체가 아니라 그 뒤의 재배포다"*. **뺄 것**: 게이트 표 4행의 경위, PR·SHA·tree 해시(`b85e2f9` 등), *"같은 날 두 건이 더 닫혔다"* 류 확인 기록, *"이 표가 하루 낡아 있었다"* 자책 문단, 197 테스트 통과 기록 | 세 「왜」가 `검토문서/DECISIONS.md:119-120`에 **이미 결론 형태로** 있다 → 정본은 `DECISIONS.md`. 접기(`<details>`)는 옳은 처방이었고 유지한다 — 2026-09-10 오탐이 정확히 이 구분 없음에서 났다 |
| 2 | **"이 목록은 실제의 절반"이라고 스스로 적어놓은 목록 4덩이가 그대로 있다** | `CLAUDE.md:396`(1,305자) · `:410`(968) · `:436`(1,236) · `:438`(1,377) — 합 **4,886자** | 낡음 + 숫자 박기 | **줄이기** — 목록 본문을 빼고 바로 위 `:174-192`의 **세는 명령 표만** 남긴다 | `CLAUDE.md:174`가 *"목록을 신뢰하지 말고 실제 파일을 세라"*고 적었다. 일괄 갱신 시점은 이미 **「켜는 날 직후」로 예약**돼 있다(`검토문서/2026-08-18-문서정비-계획.md:39`) — 그때 같이 하면 된다 |
| 3 | **DB 스키마 설명이 `banners`·`book_quotes`를 "M8 예정"이라 부른다 — M8은 취소된 계획이다** | `CLAUDE.md:410` | 낡음 | **줄이기**(2번과 같은 손질에 포함) | 같은 파일 `:230`이 *"~~M8 관리자 CMS~~ 🚫 전면개편으로 대체"* |
| 4 | **정산 화면을 "M10 placeholder"라고 부르는데 정산은 2026-07-05에 완성됐다** | `CLAUDE.md:396` (세 글자) ↔ `CLAUDE.md:233` *"M10 ✅ 완료"* | 모순 | **삭제** (`(M10 placeholder, admin 전용)` → `(admin 전용)`) | 한 파일 안에서 두 문장이 반대를 말한다. 코드 실재 확인: `jidokhae-web/src/app/(admin)/admin/settlements/` |
| 5 | **관리자 화면에 눌러도 "M8에서 만들 예정"만 뜨는 메뉴가 둘 있다** | `CLAUDE.md:396`이 서술 · 실체는 `jidokhae-web/src/app/(admin)/admin/banners/page.tsx:7` · `.../quotes/page.tsx:7` (둘 다 `milestone="M8"`) | 낡음 | **보류** — *문서 문장은 사실을 정확히 적고 있다.* 지울 대상은 코드 쪽 메뉴이고 **개발·운영 소관**이다. 문서총괄이 정할 일이 아니다 | M8은 `CLAUDE.md:230`에서 🚫 대체 판정. 즉 두 메뉴는 **취소된 마일스톤을 기다리는 빈 화면**이다 |
| 6 | **결제 백업 설명이 "LIKE로 주문번호에서 회원을 되찾는다"고 적는데, 같은 파일 다른 줄은 "LIKE를 쓰면 안 된다"고 적는다** | `CLAUDE.md:454` ↔ `CLAUDE.md:260`(결제 안전망) | 모순 | **줄이기** — `:454`를 *"이 prefix 복원은 `uuid LIKE text`라 prod에서 항상 실패했다(42883). 고친 방식은 「결제 안전망」 항목"* 한 줄로. 🔴 **문장을 없애지 말 것** — 고친 코드는 아직 미머지(`fix/payment-safety-net`)라 지우면 **지금 `main`의 실제 동작 설명이 사라진다** | `:259`가 *"이 백업은 M4 최초 구현부터 2026-09-11까지 한 번도 작동한 적이 없었다"*. `:454`는 **한 번도 안 돌아간 방식을 설명서처럼** 적어둔 자리다 |
| 7 | **"로드맵 문서를 전부 읽고 그 단계를 구현하라"는 프롬프트 파일이 남아 있다 — 로드맵은 더 이상 현행이 아니다** | `prompts` (9줄, 파일 전체) + 참조 2곳 `CLAUDE.md:62` · `CLAUDE.md:310` | 낡음 + 역할 끝남 | **삭제** (파일 + 참조 2줄 함께) | `roadmap/README.md:34` *"로드맵을 현재 상태의 근거로 쓰지 말 것"*. **삭제 기준 충족** — 프롬프트 템플릿이라 「왜 이 서비스가 이렇게 생겼는지」를 한 글자도 담고 있지 않다. M1~M6 실행 방식은 `roadmap/`과 git history에 남는다 |

### B. 새 세션이 「시작 전 필독」으로 읽는 문서

| # | 무엇 | 어디 (파일:줄) | 갈래 | 처방 | 근거 |
|:--:|---|---|---|---|---|
| 8 | **검증 프로필이 자기가 박아둔 낡음 경보를 지금 스스로 울리고 있다** | `verification-squad/profiles/jidokhae-profile.md:14` *"결정 기록 최신 날짜 = **2026-08-17**"* ↔ `검토문서/DECISIONS.md:47` 첫 행 = **2026-09-19** | 낡음 | **보류** — 이 숫자는 **일부러 고정한 트립와이어**다(공통규약 §7 예외). 필요한 건 폐기가 아니라 **갱신**이고, 소관은 관리자·품질이다 | 같은 파일 `:9`가 *"하나라도 어긋나면 작업을 멈추고 관리자에게 보고한다"*고 정해뒀다. 규약대로 **보고만** 한다 |
| 9 | **같은 프로필의 세 줄이 이미 사실이 아니다** — "토론 환불은 배포 대기", "BOOK_ASK는 심사 중" | `jidokhae-profile.md:55` · `:132` (*"`feat/next-phase1a` 배포 대기"*) · `:65` (*"BOOK_ASK … 재심사 중(INSPECTING)"*) | 낡음 | **삭제** (세 줄의 "대기 / 심사 중" 부분만) | 환불 7/3은 PR #64로 2026-08-21 머지됨(`DECISIONS.md:91` ✅ 적용). BOOK_ASK는 8/19 승인(`검토문서/2026-08-18-켜는날-런북.md:14`). **결론이 이미 다른 곳에 확정으로 있어 지워도 맥락이 안 끊긴다** |
| 10 | **"트래픽 일 50~100명, 동시 10~20명"이 프로필에 숫자로 박혀 있다** | `jidokhae-profile.md:88` | 숫자 박기 | **삭제**(숫자만) | 공통규약 §7 위반. 같은 파일 `:68`은 회원 수에 대해 *"숫자를 여기 적지 않는다"*고 **스스로 규율을 지키고 있다** — 한 파일 안에서 기준이 갈린다 |
| 11 | **부서 체계 표지가 "아직 안 만들었다"고 적은 것이 이미 만들어져 있다** | `docs/agent-team/00 읽는 순서.md:43` (*"부서 정의 파일(`.claude/agents/`) — 아직 안 만들었다"*) · `:44` (*"검증 보고서 — 용량이 커서 git에서 제외돼 있다"*) · `:34-38` (「지금 확인이 필요한 것」 3문항) — 45줄 중 **약 14줄** | 낡음 | **삭제** (세 덩이) | `.claude/agents/`에 4개 실재(design·engineering·librarian·product). `verification-squad/reports/2026-04-06/` 8개가 **git에 추적되고 있다**(`git ls-files` 확인). 세 덩이 전부 **당시 상태 서술**이고 「왜」가 없다 |
| 12 | **안내판이 한 달 넘게 "초안입니다"로 서 있다** | `docs/agent-team/안내판-라우팅표.md:3` | 낡음 | **보류** — 초안 딱지를 뗄지는 관리자 판단이다. **표 내용이 틀렸다는 근거는 찾지 못했다** | 판단 근거가 없어 폐기로 올리지 않는다 |

### C. 켜는 날 3종 세트 — `검토문서/README.md:15`가 "셋을 함께 본다"고 지목한 자리

| # | 무엇 | 어디 (파일:줄) | 갈래 | 처방 | 근거 |
|:--:|---|---|---|---|---|
| 13 | **"🟡 — 아래 5건을 끝내면 켤 수 있다"는 판정이 그대로 서 있는데 5건은 전부 닫혔다** | `검토문서/2026-08-21-켜는날-코드준비-판정.md:12` (문서 278줄) | 낡음 | **과거 표시** — 맨 위에 *"2026-08-21 시점 판정. 5건 전부 닫힘(2026-08-22 · 2026-09-10). 현행 게이트는 루트 `CLAUDE.md` §켜는 날 게이트"* 배너 | 닫힘 근거 `CLAUDE.md:121` *"절차는 전부 닫혔다"*. **삭제 금지** — 그 5건이 **왜 런북에 없었는지**가 이 문서에만 있다. 게다가 `검토문서/README.md:18`이 **켜는 날 필독 3종**으로 올려둬 당일 읽힐 확률이 높다 |
| 14 | **미적용 우선순위가 "19건/18건"과 3주 전 SHA로 순서를 매긴다** | `검토문서/2026-08-21-미적용-우선순위.md:3`(`0970fd0`) · `:9` · `:15`(*"남은 미적용 19건"*) — 문서 **171줄** | 낡음 + 숫자 박기 | **줄이기** — **남길 것**: 「왜 경과일이 아니라 켜는 날로 가르는가」 문단(이 문서의 유일한 「왜」다). **뺄 것**: 건수 총계 3곳, SHA 기준선 3곳, 「2차 갱신에서 바뀐 것」 경위 블록(약 5줄) | `DECISIONS.md:39`가 이 문서를 **우선순위 정본**으로 링크한다. 그 사이 최소 2건(토론 환불 · retarget·draft 되돌림)이 닫혔다. 자기 문서 `:3`이 *"이 SHA는 스냅샷이지 현행 `main`이 아니다"*라고 이미 적어뒀다 |
| 15 | **알림톡 문서 6건 중 4건은 최종 문안에 이르는 과정이다** | `검토문서/2026-08-02-…6번째-템플릿-문안.md`(226줄) · `2026-08-06-…5종-개선안.md`(236) · `2026-08-07-…결함-3건-수정안.md`(213) · `2026-08-07-템플릿-정리-계획.md`(151) | 역할 끝남 | **보류** — **지금 지워서 얻는 것이 없다.** 넷 중 `2026-08-07-템플릿-정리-계획.md`는 **살아 있는 금지 조항**(`POST_MEETING` 삭제 금지)을 들고 있고 `DECISIONS.md:99`가 링크한다. 나머지 셋도 승인된 심사 문구가 왜 그 모양인지의 근거다 | `검토문서/README.md:42`가 이미 *"혼자 볼 문서는 맨 아래 두 개고, 위 넷은 거기에 이르는 과정이다"*로 **갈라놨다** — 표시가 이미 제 역할을 하고 있다 |

### D. 로드맵 (7파일 4,018줄)

| # | 무엇 | 어디 (파일:줄) | 갈래 | 처방 | 근거 |
|:--:|---|---|---|---|---|
| 16 | **MVP 로드맵 3종이 결제사를 토스페이먼츠로 적고, 완료됐다는 표시가 아예 없다** | `roadmap/milestones.md:153`(*"토스페이먼츠 연동"*) · `:230`(*"토스페이먼츠 라이브 전환 심사 대기 중"*) — MVP 3종 합 **1,969줄** | 낡음 | **과거 표시** — 세 파일 상단에 *"M1~M6 완료 기록(2026-03). 결제는 그 뒤 PortOne V2로 이관 — 본문의 토스페이먼츠는 당시 기준"* 한 줄 | Phase 3 3종에는 🚫 공지가 붙어 있는데 **MVP 3종에는 아무 표시가 없다.** 결제 이관은 `CLAUDE.md:246`(PR #28) |
| 17 | **똑같은 6줄 공지가 세 파일 머리에 복사돼 있다 — 한쪽만 갱신될 구조** | `roadmap/milestones-phase3.md:3-7` · `work-packages-phase3.md:3-7` · `scenarios-phase3.md:3-7` | 중복 | **정본으로 합치기(`roadmap/README.md`)** — 세 곳은 *"상태 공지는 `roadmap/README.md`를 본다"* 한 줄로 | `roadmap/README.md:32-34`에 **이미 같은 경고**가 있다. 정본은 README 쪽 — 거기가 로드맵 전체(MVP+Phase3)를 다루고 이 공지는 Phase 3에만 붙어 있다 |
| 18 | **`phase3-requirements.md`는 M8·M9 요구사항 원본인데 M8·M9는 취소됐다** | `roadmap/phase3-requirements.md` (209줄, 상단 표시 없음) | 낡음 | **과거 표시** (16·17번과 같은 방식) | **삭제 금지** — 5탭이 흡수한 요구가 **원래 무엇이었는지**가 여기 있다(`CLAUDE.md:230` *"배너·한 줄 계획은 5탭 구조가 흡수했다"*) |

### E. `docs/superpowers/` — plans 11 / specs 12 / mockups 13MB

| # | 무엇 | 어디 (파일:줄) | 갈래 | 처방 | 근거 |
|:--:|---|---|---|---|---|
| 19 | **끝난 구현 계획 9개가 "이 계획을 태스크 단위로 지금 구현하라"는 지시문을 달고 있다** | `docs/superpowers/plans/` 11개 중 Status 헤더 있는 건 2개뿐(`2026-04-26-…:3` · `2026-04-28-…:3`). 나머지 9개는 첫 문단이 *"REQUIRED SUB-SKILL … implement this plan task-by-task"* (예: `2026-06-28-mypage-profile.md:3` · `2026-08-17-전면개편-1단계A-백엔드.md:3`) — 9개 합 **약 7,558줄** | 낡음 + 중복 | **과거 표시** — 9개 상단에 `> **Status:** ✅ Merged in PR #NN` 한 줄씩 | 전부 머지 완료(PR #37·#39·#42·#43·#54·#55 등, `CLAUDE.md` Current status). 🔴 **`CLAUDE.md:56`은 *"plans/는 머지된 plan에 Status 헤더"*라고 적는데 11개 중 2개만 그렇다** — 문서가 규칙을 적어두고 지키지 않았다. **삭제 금지**: 각 plan의 Architecture 문단이 「왜 그렇게 설계했나」이고 디버깅 때 실제로 쓰인다 |
| 20 | **채택되지 않은 스킨 탐색 시안 약 20개(5.9MB)에 "이건 안 뽑혔다" 표시가 없다** | `docs/superpowers/mockups/2026-08-14-*` · `2026-08-15-*` (예: `2026-08-14-토론모임-표지색-적용.html` 992KB · `2026-08-15-전체화면-18종.html` 400KB). 확정안은 `2026-08-16-토스-스킨.html` | 역할 끝남 | **과거 표시** — `docs/superpowers/mockups/README.md`를 한 장 만들어 **확정안 / 탐색 잔재 / 기각안**을 가른다 | `DECISIONS.md:69`가 여정을 *"21벌 시안 → 은유 3종 → 토스 확정"*으로 기록해뒀다 — **삭제하면 이 서비스 화면이 왜 지금 모습인지의 근거가 사라진다.** `docs/expert-panel/README.md`가 이미 같은 방식으로 작동 중이라 검증된 처방이다 |
| 21 | **토론모임 흐름 시안 v1이 v2로 대체됐는데 파일명만으로는 알 수 없다** | `docs/superpowers/mockups/2026-08-13-토론모임-흐름.html`(33KB) ← `-v2.html`(431KB)이 대체 | 역할 끝남 | **과거 표시** (20번의 README에 «v1 — v2로 대체» 한 줄) | `DECISIONS.md:134`가 **v2만** 링크한다. 삭제도 가능하나 v1→v2에서 무엇이 바뀌었는지가 사라진다 |
| 22 | **취소된 M8·M9의 Before/After 목업이 저장소 루트에 있다** | `phase3-preview.html` (894줄) + 참조 `CLAUDE.md:63` · `CLAUDE.md:313` | 역할 끝남 | **보류** — 패널 기록 `docs/expert-panel/2026-04-17-phase3-preview-html-review.md`가 **이 파일을 대상으로 리뷰**한다. 지우면 그 기록이 허공을 가리킨다. 옮기는 것도 범위 밖이다(`DECISIONS.md`가 파일명으로 링크하는 자산 군) | 부서 정의 §6 — 파일명·위치 변경은 문서총괄이 하지 않는다 |

### F. 파트너 학습서 — 대표님이 직접 읽는 자리

| # | 무엇 | 어디 (파일:줄) | 갈래 | 처방 | 근거 |
|:--:|---|---|---|---|---|
| 23 | 🔴 **학습서가 "우리는 토스페이먼츠를 쓴다, 왜 골랐냐면"을 챕터 하나로 가르친다** | `docs/learning/part1-기초/03-우리-기술스택.md:83`(*"## 3. TossPayments"*) · `:88`(*"왜 TossPayments인가"*) · `:235` · `:256` / `01-용어사전.md:166`·`:167`·`:173`·`:186`·`:191`·`:192` / `02-웹서비스-큰그림.md:67`·`:90`·`:94` | 낡음 | **과거 표시**(즉시) — 세 파일 상단에 *"2026-04 작성. 결제는 그 뒤 PortOne V2로 바뀌었다 — 결제 관련 서술은 현행이 아니다"*. 본문 개정은 그다음 | 실제는 PortOne V2(`CLAUDE.md:246`, PR #28). **대상 독자가 비전공자 대표님이라 잘못된 개념이 그대로 굳는다.** 🔴 **이 세 파일은 `검토문서/2026-08-18-문서정비-계획.md`의 개정 대상에 들어 있지 않다** — 그 계획은 코어 3종과 `CLAUDE.md` 2종만 들고 있다(§2-1~2-4) |
| 24 | **학습서가 규모를 "250명"으로 세 번 박아뒀다** | `03-우리-기술스택.md:12` · `:149` · `:246` | 숫자 박기 | **삭제**(숫자만 — *"소규모"*로) | 공통규약 §7. 루트 `CLAUDE.md:7`이 *"회원 수는 계속 변하므로 문서에 숫자를 박지 않는다"*고 이미 정했다 |
| 25 | **없는 챕터 21개의 목차가 있는 것처럼 실려 있다** | `docs/learning/README.md:34-83`(Part 2~6 목차 21항목, 전부 *"작성 예정"*) + `:85-92`(추천 읽기 경로가 **없는 19·21장을 가리킨다**) — 약 **50줄** | 낡음 | **줄이기** — 21줄 목차 → *"Part 2~6은 계획만 있고 쓰지 않았다(2026-04 이후 진척 없음)"* 2줄. 추천 읽기 경로는 Part 1만 남긴다 | 5개월째 0챕터. 같은 README `:104`는 *"이 책은 살아있는 문서다"*라고 적는다 — **없는 목차는 읽는 사람에게 있는 줄 착각시킨다** |

### G. 검증 리포트

| # | 무엇 | 어디 (파일:줄) | 갈래 | 처방 | 근거 |
|:--:|---|---|---|---|---|
| 26 | **2026-04-06 풀스캔 8건이 "🟡 조건부 승인"이라는 판정을 현행처럼 들고 있다 — 토스페이먼츠 시절 것이다** | `verification-squad/reports/2026-04-06/08-final-report.md:11-16` 외 7건, 합 **1,355줄**. 상단에 시점 표시 없음. 같은 `:16`이 규모를 *"250명"*으로 박음 | 낡음 + 역할 끝남 | **과거 표시** — `verification-squad/reports/2026-04-06/README.md` 한 장: *"2026-04-06 · TossPayments 시절 · 결론은 `검토문서/2026-04-23-풀스캔-후속-의사결정.md`로 흡수됨 · 현행 판정 아님 · 본문의 회원 수는 2026-04 기준"* | **삭제 금지** — `jidokhae-profile.md:106-123` 「의도된 설계 — 문제로 오인하지 말 것」 목록의 상당수가 **이 리포트의 오탐에서 나왔다.** 그 목록이 왜 생겼는지의 원본이다 |

### H. 저장소 잡짐 · 규율

| # | 무엇 | 어디 (파일:줄) | 갈래 | 처방 | 근거 |
|:--:|---|---|---|---|---|
| 27 | **비어 있는 작업 사본 폴더 4개** | `.claude/worktrees/adoring-ishizaka`(2026-03-26) · `charming-heyrovsky`(05-09) · `distracted-allen`(03-26) · `modest-lichterman`(06-25) | 역할 끝남 | **삭제** | `git worktree list`에 **등록돼 있지 않고**, 내용물이 **0개**다(`ls` 확인). 지울 「왜」가 애초에 없다 |
| 28 | **살아 있는 작업 사본 3개는 저장소를 통째로 한 벌씩 더 들고 있다** | `.claude/worktrees/agent-a9eff…`(`fix/payment-safety-net`) · `agent-ae0c…`(`feat/discussion-gate`) · `marketing-tracking`(PR #66) — 각각 옛 `DECISIONS.md` 사본(16행 대 현행 20행)을 포함 | 중복 | **보류** — 셋 다 **미머지 브랜치**라 지우면 작업이 사라진다. 다만 **문서 검색 때는 반드시 제외**한다(옛 판이 중복으로 잡힌다) | `git worktree list` 실측. `fix/payment-safety-net`은 `CLAUDE.md:260`이 결제 안전망 근거로 지목하는 브랜치다 |
| 29 | **버릴 것을 판단하는 기준 목록인 인덱스가 최근 파일 3개를 빠뜨렸다** | `검토문서/README.md` — `2026-08-22-A2A3-코드지형.md` · `2026-08-22-토론자격-판정규칙.md` · `2026-08-26-온보딩-전환검토.md` 없음 | 낡음 | **보류** — 폐기가 아니라 **추가** 사안이다. 다만 **이 인덱스가 「어느 파일이 있는가」의 정본**이라 폐기 작업 전에 먼저 메워져야 한다 | 같은 파일 `:5` *"불완전한 인덱스는 없는 것보다 나쁘다"* · `:60` *"파일을 추가하면 그 자리에서 한 줄 넣는다. 나중에 하겠다고 미루면 안 한다 — 실제로 8건이 그렇게 빠졌다"*. **같은 일이 세 번 더 일어났다** |
| 30 | **옛 호칭 「단무지님」이 문서 70개 파일에 남아 있다** | `검토문서/DECISIONS.md` 20회 · `docs/agent-team/logs/개발운영.md` 11회 · `logs/관리자.md` 10회 등 (`grep -rc` 실측, worktree 사본 제외) | 낡음 | **보류** — 두 가지 이유. (1) 과거 로그·결정의 **화자 표기**까지 바꾸면 "그때 누가 그렇게 말했나"의 기록을 사후에 손대는 일이 된다. (2) 일괄 치환은 **다른 부서 파일 다수**를 건드려 범위 밖이다. **앞으로 쓰는 문서만 「대표님」**으로 가는 것이 안전하다 | 현행 호칭은 **「대표님」**(2026-08-25 본인 지시). 최근 문서는 이미 그렇게 쓰고 있다 — `DECISIONS.md:47`·`:48`, `검토문서/2026-08-18-켜는날-런북.md:23` |
| 31 | **`DECISIONS.md`의 "며칠째" 두 줄이 4주 멈춰 있다** | `검토문서/DECISIONS.md:100`(*"15일째(2026-08-22 기준)"* → 오늘 **43일째**) · `:105`(*"37일째"* → **65일째**) | 낡음 + 숫자 박기 | **줄이기** — 경과일을 빼고 **시작일만** 적는다(*"2026-08-07 결정, `main`에 아직 0곳"*). 읽는 사람이 오늘과 빼면 된다 | 같은 파일 `:32`가 **정확히 같은 이유로** 진행 단계 칸을 거부했다 — *"손으로 적는 순간부터 틀린다. GitHub이 정확하게 세는 것을 문서에 베껴 적으면 지는 싸움이다"*. 경과일도 같은 종류다 |

---

## 삭제로 올리지 않은 것 — 왜 남기는가

폐기 후보로 검토했으나 **「왜 이 서비스가 지금 모습인지」를 설명하는 자리**라 처방을 바꾼 것들이다.

| 검토한 것 | 왜 안 지우나 |
|---|---|
| `DECISIONS.md`의 취소선 행 4개(2026-08-13 토론 환불 14/7 · 2026-08-14 완독 재촉 구판 · 2026-08-21 접근 금지 해제 등) | **지금도 제 역할을 하고 있다.** `:58`이 그 이유를 직접 적었다 — *"지우면 다음 세션이 세 파일이 왜 한동안 잠겨 있었는지조차 알 수 없다"* |
| `docs/agent-team/조사/` 8건 (사고 조사 포함) | 2026-09-10 결제 사고의 규명 기록. `CLAUDE.md:260` 결제 안전망 규칙 전체가 여기서 나왔다 |
| 부서 로그 5종 (합 3,244줄) | 세션이 끝나면 사라지는 판단의 유일한 기록(공통규약 §4). **길이를 이유로 줄이지 않는다** |
| `docs/expert-panel/` 15건 | `README.md`가 이미 «결정으로 이어졌나» 칸으로 현행/비현행을 갈라놨다. 표시가 작동 중이다 |
| 저장소 `memory/` | 처분이 이미 정해져 있다 — *"지우지 않고 남겨두되 갱신하지 않는다"*(`CLAUDE.md:74`). 재론하지 않는다 |
| 코어 3종의 낡은 문장 (배지 범위 밖 · 6개월 자격 등) | `검토문서/2026-08-18-문서정비-계획.md` §2가 **이미 전수로 들고 있고 개정 시점도 잡혀 있다**(켜는 날 직후). 중복 보고하지 않는다. ⚠️ **단, 그 계획에 결제사(토스→PortOne) 항목이 빠져 있다** — 23번 참조 |

---

## 관리자가 먼저 볼 3건

1. **#1 + #2 — 루트 `CLAUDE.md`의 13,108자(파일의 20%)가 「끝난 절차」와 「스스로 절반이라 적은 목록」이다.** 매 세션 주입되는 자리라 비용이 가장 크고, 두 건 다 **정본이 다른 곳에 이미 있다**(`DECISIONS.md:119-120` / `:174` 세는 명령). 관리자 소유 파일이라 관리자만 손댈 수 있다.
2. **#23 — 대표님이 읽는 학습서가 결제사를 토스페이먼츠로 가르친다.** 그리고 **문서 정비 계획에 이 항목이 없다** — 코어만 잡아뒀고 학습서는 빠져 있다. 오늘 배너 한 줄이면 막힌다.
3. **#13 — 켜는 날 필독 3종 중 하나가 "🟡 5건 남았다"로 서 있다.** 켜는 날(2026-10-11)이 3주 앞이고, `검토문서/README.md:18`이 그 문서를 당일 읽으라고 지목한다. 배너 한 줄로 끝나지만 **안 붙이면 당일에 읽힌다.**
