# 에이전트 부서 체계 — 읽는 순서

> 이 폴더는 **복사본이 아니라 링크**다. 저장소 `C:\jidokhae-2nd`의 실제 파일을 그대로 보고 있다.
> 그래서 항상 최신이고, 따로 갱신할 필요가 없다.

## ⚠️ 먼저 알아둘 것

**여기서 파일을 고치면 저장소가 바뀐다.** 링크라서 같은 파일이기 때문이다.

- 읽고 검토하는 것 — 마음껏
- 메모를 남기고 싶으면 — **이 폴더 밖에**(예: `320. Area/지독해/`) 새 노트를 만들어 `[[공통규약]]`처럼 링크를 걸 것
- 여기서 직접 고치면 git에 변경으로 잡힌다. 나쁜 건 아니지만, 고쳤다는 사실을 알고 있어야 한다

## 읽는 순서

| # | 문서 | 무엇인가 | 분량 |
|---|---|---|---|
| 1 | [[공통규약]] | **여기부터.** 부서가 일 시작 전 읽는 규칙 전부 | 중간 |
| 2 | [[안내판-라우팅표]] | 어떤 요청이 오면 어느 부서로 가는가 (**초안 — 확인 필요**) | 짧음 |
| 3 | `검증프로필/jidokhae-profile` | 에이전트에게 주는 "우리 서비스 소개서" | 중간 |
| 4 | [[벤치마크-결과]] | 기존 검증 도구를 실제로 재본 결과 | 김 |
| 5 | `logs/관리자` | 작업 기록 + 다음에 할 일 | 짧음 |

**시간이 없으면 1번과 2번만** 봐도 된다. 3~5번은 근거 자료다.

## 지금 확인이 필요한 것

안내판(2번)이 초안이다. 저장소 기록을 보고 추측으로 채운 것이라 실제와 다를 수 있다.

1. 자주 하는 요청 말투가 다 들어 있는가
2. "문구 다듬기"를 디자인 부서에 넣었는데 맞는가
3. 데이터·고객VOC·성장은 실제로 어떻게 부르는가 (지금 거의 안 쓰는 자리라 표현을 모른다)

## 옆 폴더

| 폴더 | 무엇 |
|---|---|
| `검증프로필/` | 에이전트에게 주입되는 프로젝트 소개서 |
| `결정기록/` | `DECISIONS.md`가 있는 곳. **이미 정해진 것**을 다시 안건으로 올리지 않기 위한 표 |

## 여기 없는 것

- 부서 정의 파일(`.claude/agents/`) — 아직 안 만들었다. 관리자 규칙 다음 차례
- 검증 보고서(`verification-squad/reports/`) — 용량이 커서 git에서 제외돼 있다
- 검증 에이전트 8명의 정의 — 저장소가 아니라 `C:\Users\ytyt8\verification-squad\agents\`에 있다
