# 알림톡 결함 3건 수정안

> 2026-08-07 | 작성: 총괄 | **배포 금지 — 문구 확정만**
> 원문은 Solapi API(`getKakaoAlimtalkTemplates`)로 조회한 실제 승인 템플릿이다.

## 이 문서의 성격

라이팅 가이드보다 **먼저** 처리한다. 세 건 중 하나는 **회원이 지금 잘못된 정보를 받고 있는 문제**다.

**어미는 손대지 않는다.** 현행 템플릿이 합쇼체이므로 수정안도 합쇼체를 유지한다. 어미 결정(가이드 §6)이 난 뒤에 문안 전면 재작성을 별도로 한다. 지금은 **결함만** 고친다.

---

## 결함 1 — 🔴 리마인드가 미입금자에게 "결제완료"라고 단언한다

**대상**: `MEETING_REMIND` (`SOLAPI_TEMPLATE_REMIND`)

### 현재

```
#{회원명}님, 내일 모임에 참석해주세요!

■ 모임: #{모임명}
■ 일시: #{모임일시}
■ 장소: #{장소}
■ 참가비: #{참가비}원 (결제완료)
```

### 무엇이 문제인가

마지막 줄의 `(결제완료)`가 **변수가 아니라 고정 문구**다.

PR #44로 리마인드 대상에 `pending_transfer`(입금 확인 전)를 포함시킨 뒤부터, **아직 입금하지 않은 회원도 "결제완료"라고 쓰인 메시지를 받고 있다.**

- 입금을 잊은 회원 → "냈다고 하네" 하고 넘어간다
- 이미 낸 회원 → 문제없지만, 두 경우를 문면으로 구분할 수 없다

### 수정

```
#{회원명}님, 내일 모임에 참석해주세요!

■ 모임: #{모임명}
■ 일시: #{모임일시}
■ 장소: #{장소}
■ 참가비: #{참가비}원 (#{결제상태})
```

| 주입값 | 조건 |
|---|---|
| `결제완료` | `confirmed` |
| `입금 확인 중` | `pending_transfer` |

### 필요한 작업

| # | |
|---|---|
| 1 | 템플릿에 `#{결제상태}` 변수 추가 → **카카오 재심사** |
| 2 | `notification.ts`의 리마인드 래퍼에 `'#{결제상태}'` 전달 추가 |
| 3 | **템플릿 승인과 코드 배포를 같이 내보낸다.** 템플릿이 먼저 교체되면 코드가 변수를 안 보내 발송이 실패한다 |

> `참석해주세요!`(강요·느낌표)는 이 결함과 별개다. 어차피 재심사에 올라가므로 **문안 전면 재작성 때 함께** 처리하는 편이 재심사 횟수를 줄인다. 다만 어미가 미정이라 지금 확정하지 않는다.

---

## 결함 2 — `승격`이라는 내부 용어가 회원에게 나간다

**대상**: `WAITLIST_REFUNDED` (`SOLAPI_TEMPLATE_WAITLIST_REFUNDED`)

### 현재

```
#{회원명}님, #{모임명} 모임의 대기 중 승격되지 않아 결제 금액이 환불됩니다.

■ 환불 금액: #{결제금액}원 (전액)
■ 환불 예정: 영업일 기준 3~5일

다음에 더 좋은 기회로 뵙겠습니다.
```

### 무엇이 문제인가

- **`승격`은 개발 용어다.** 회원은 자기가 어디서 어디로 올라갔는지 모른다 (가이드 §3-7 위반)
- **잡초** — `#{모임명} 모임의`에서 `모임의`가 중복이다 (§3-1 위반)
- **`영업일 기준 3~5일`을 무조건 단언한다.** 카드는 카드사가 처리하니 맞지만, **계좌이체는 운영자가 직접 이체**하므로 그 기한을 보장할 수 없다
- **`다음에 더 좋은 기회로`** — 이번이 나빴다는 뉘앙스다

### 수정

```
#{회원명}님, #{모임명} 자리가 나지 않아 결제 금액을 환불해 드립니다.

■ 환불 금액: #{결제금액}원 (전액)

▪ 카드 결제는 카드사 처리에 영업일 3~5일이 걸립니다.

다음 모임에서 뵙겠습니다.
```

### 필요한 작업
카카오 재심사만. 코드 변경 없음(변수 동일).

---

## 결함 3 — 🚫 진단 철회 (2026-08-11)

**제 진단이 틀렸다.** 본문만 보고 판단했는데, 이 템플릿은 **강조표기형**이라 본문 위에 별도 필드가 있다.

```
emphasizeTitle    : 참여가 확정되었어요!
emphasizeSubtitle : 대기 중이던 모임에 자리가 났어요
content 첫 줄     : #{회원명}님, 대기 중이던 모임의 자리가 나서 참여가 확정되었습니다!
```

**좋은 소식은 이미 맨 위에 크게 붙어 있다.** 본문 첫 줄이 잘리는 것은 사실이나, 회원이 먼저 보는 것은 강조 타이틀이므로 애초에 진단한 만큼의 손실이 없다.

**수정하지 않는다.** 실제 노출 형태를 눈으로 확인하기 전에는 건드릴 이유가 없다. 재심사 비용만 든다.

> **여기서 얻은 교훈** — 템플릿을 판단할 때 `content`만 보면 안 된다. `emphasizeTitle`·`emphasizeSubtitle`·`extra`·`ad`가 별도 필드로 있고, 회원이 먼저 보는 건 그쪽이다.

<details>
<summary>철회된 원래 진단 (기록 보존)</summary>

### 좋은 소식이 잠금화면에서 잘린다

**대상**: `WAITLIST_PROMOTED` (`SOLAPI_TEMPLATE_WAITLIST_PROMOTED`)

### 현재

```
#{회원명}님, 대기 중이던 모임의 자리가 나서 참여가 확정되었습니다!
```

### 무엇이 문제인가

잠금화면 미리보기는 앞부분만 보인다. 지금 구조로는 이렇게 잘린다.

```
초록고래님, 대기 중이던 모임의 자…
```

**서비스에서 가장 반가운 소식인데, 열어보기 전에는 그게 좋은 일인지 알 수 없다.** 앞의 `대기 중이던 모임의`는 회원이 이미 아는 정보라 잡초이기도 하다(§3-1, §3-3).

### 수정

```
#{회원명}님, 자리가 났습니다.

■ 모임: #{모임명}
■ 일시: #{모임일시}
■ 장소: #{장소}
■ 결제 금액: #{결제금액}원 (결제완료)

참여가 확정되었습니다.
```

잘리는 구간에서 이렇게 보인다.

```
초록고래님, 자리가 났습니다.
```

> ⚠️ 이 템플릿의 `(결제완료)`도 고정 문구다. **계좌이체로 대기 신청한 회원은 미입금 상태로 승격될 수 있으므로** 결함 1과 같은 문제를 안고 있다. 재심사에 올릴 때 `#{결제상태}`로 함께 바꾸는 것을 권한다.

### 필요한 작업
카카오 재심사. `#{결제상태}`까지 함께 바꾸면 코드 변경 1건 추가.

</details>

> 다만 이 템플릿의 `(결제완료)`도 결함 1과 같은 고정 문구다. **계좌이체로 대기 신청한 회원은 미입금 상태로 승격될 수 있다.** 이건 별건으로 남겨두고, 어미 확정 후 문안 전면 재작성 때 함께 처리한다.

---

## 진행 상황 (2026-08-11)

### V2 신규 등록 완료 — 검수 대기

기존 템플릿을 수정하면 재검수 상태가 되어 **검수 중에는 발송이 막힌다.** 그래서 기존은 그대로 두고 V2를 새로 등록했다.

| 템플릿 | templateId | 상태 |
|---|---|---|
| `MEETING_REMIND_V2` | `KA01TP26081107172974875tZ6EzF5wl` | **PENDING** (검수 필요) |
| `WAITLIST_REFUNDED_V2` | `KA01TP260811071750589d25k7TNGiaK` | **PENDING** (검수 필요) |

> ⚠️ **`PENDING`은 "검수 중"이 아니라 "검수 요청 전"이다.** Solapi SDK에 검수 요청 메서드가 없어 **콘솔에서 직접 눌러야 한다.** 승인까지 영업일 1~3일.

**등록 시 지킨 것** — 원본을 그대로 복제하고 결함 부분만 바꿨다. 어미·톤(`참석해주세요!` 등)은 손대지 않았다. 어미가 미정이기 때문이다.

### 코드 준비 완료 — 머지 금지

브랜치 `fix/remind-payment-status`, 커밋 `5ebd033`. `main`에 머지하면 그 순간 프로덕션 배포되므로 **승인 전까지 머지하지 않는다.**

### 남은 순서

| # | 할 일 | 누가 |
|:--:|---|---|
| 1 | Solapi 콘솔에서 V2 2건 **검수 요청** | 재윤 |
| 2 | 승인 대기 (영업일 1~3일) | — |
| 3 | `SOLAPI_TEMPLATE_REMIND` 값을 V2 ID로 교체 (Vercel env) | 재윤 |
| 4 | `fix/remind-payment-status` 머지 → 배포 | Claude |
| 5 | 다음 리마인드 발송 확인 후 구 템플릿 삭제 | — |

> **3과 4는 같이 나가야 한다.** env만 바꾸면 구 코드가 `#{결제상태}`를 안 보내고, 코드만 배포하면 구 템플릿에 변수가 없다.

---

## 정리

| # | 대상 | 상태 |
|:--:|---|---|
| 1 | MEETING_REMIND | ✅ V2 등록. 검수 요청 대기. 코드 브랜치 준비 |
| 2 | WAITLIST_REFUNDED | ✅ V2 등록. 검수 요청 대기. 코드 변경 없음 |
| 3 | WAITLIST_PROMOTED | 🚫 **진단 철회.** 수정하지 않음 |
